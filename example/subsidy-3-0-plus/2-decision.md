---
feature: subsidy-3-0-plus
stage: 2-decision
status: approved
owner: IVF後台
reviewers: [user]
updated: 2026-08-26
---
# 2. 收斂 — 試管補助 3.0 PLUS

## 確認紀錄
- 2026-08-25～26 討論已拍板；本檔把決策點收成方案，送 G1。

## Approaches Considered

### 決策點 1：誰走 PLUS 表
| 方案 | 摘要 | 優 | 劣 | 成本 | 依據 |
|---|---|---|---|---|---|
| A | 只看主檔 `apply_date` ≥ 115/9/1（2026-09-01）。沒有 today 開關。9/1 前立案整案留 3.0 | 測試區 9/1 前也能用申請日測；舊案不被重判 | 行政若改申請日會切表（實務說不會改） | 三處金額函式加切日 | 1-discussion OQ 切日 |
| B | 日曆 today ≥ 9/1 才啟用 | 上線前碼進不去 PLUS | 測試區 9/1 前測不了；跟「程式一上就啟用」衝突 | 低 | 討論已駁回 |
| C | 3.0 後第 2 次也走 PLUS（舊 But Example） | 接近 briefing 一例 | 9/1 前立案之後附表五會跳表 | 高（要重判） | 討論已駁回 |

### 決策點 2：金額三格
| 方案 | 摘要 | 優 | 劣 | 成本 | 依據 |
|---|---|---|---|---|---|
| A | 取卵／胚胎形成／植入三格。形成＝完整−取卵無法植入−僅植入。自動填可手改，存檔不蓋手改 | 第 2 次完整 15 不會像少一格；不好再收回兩格 | 滿 40 第 1 次形成會算出 1 萬；要比現況打開 readonly | 畫面＋DB 一欄＋三函式 | 1-discussion 三格 |
| B | 維持取卵＋植入兩格 | 改最少 | 第 2 次完整 6+4≠15 | 低 | 討論已駁回 |
| C | 三格都空白手填 | 最彈性 | 行政每次對表 | 低 | 討論改自動填 |

### 決策點 3：年齡與歸零
| 方案 | 摘要 | 優 | 劣 | 成本 | 依據 |
|---|---|---|---|---|---|
| A | 同一輪鎖兩格（次數 `first_time_subsidy_age`、金額 `first_subsidy_age`）。申請滿再來新開一筆，舊案不動 | 跟實務一致；國健署申請日不亂動 | 新案要自己建 | 不改舊列 | 1-discussion 歸零 |
| B | 同一案次數改回首次並重算年齡 | 少一筆主檔 | 跟「舊資料不再動」衝突 | 高 | 討論已駁回 |

### 決策點 4：本機怎麼給人測
| 方案 | 摘要 | 優 | 劣 | 成本 | 依據 |
|---|---|---|---|---|---|
| A | `ivf-platform-develop-dev`（27004）bind 改掛到該 feature 的 worktree，要測哪支就切掛載 | 主目錄可留在別的 feature；worktree 互不踩 | 同時只能掛一棵；切的時候要重掛容器 | 低 | docker inspect：現掛 `/Users/asheng/dev/ivf_platform` |
| B | 把 PLUS 合進主目錄的 `testing` 分支 | 跟以前 merge testing 一樣 | 主目錄現在在 `fix/erp-detection-official-prices`，會攪在一起 | 中 | 主目錄 `git status` |
| C | 另開第二個容器掛 worktree | 可同時測兩支 | 要再佔埠、兩套登入 | 中 | `[Assumption]` 尚未做 |

## 方案架構圖
```
apply_date
  < 2026-09-01 → 整案 3.0 兩格表（39 切）
  ≥ 2026-09-01 → PLUS 三格表（40 切）
    取卵 + 形成 + 植入 = 該次上限
    形成 = 完整 − 取卵無法植入 − 僅植入
27004 bind → 要測的 worktree（一次一棵）
```

## Decision
選 **1A + 2A + 3A + 4A**：PLUS 只看主檔申請日；三格自動可手改；申請滿新開一筆；測試區用切 worktree 掛載，不把碼合進正在改 ERP 的主目錄。

## Rejected
- 1B：today 開關，測試區 9/1 前測不了。
- 1C：舊案第 2 次跳 PLUS，已重鎖否決。
- 2B／2C：兩格或缺格、或全手填，已否決。
- 3B：同案改回首次，舊資料會被動。
- 4B：主目錄正在別的 feature，不合。
- 4C：先不開第二容器，除非 27004 不能停。

## Rationale
討論期每題都有使用者一句話；4A 對上「worktree 讓測試區可切」。正式區不動。

## Scope & Non-Goals
- 做：PLUS 表、三格、切日、附表五列形成胚胎 OPU、worktree 掛載切換。
- 不做：顆數硬擋、38 文案、低收入改、Revised、正式區、主目錄 ERP 分支。

## Risks & Mitigations
- 滿 40 第 1 次形成 1 萬看起來怪 → 先照算給人看，不好收回兩格。
- 切掛載會讓 27004 暫時看不到 ERP 分支 → 切回去即可；切前跟使用者講。
- 手改後被後端覆寫（現況）→ 規格寫「有手改就不要蓋」。

## Success Criteria
- 申請日 2026-09-01、未滿 40、第一次完整：三格 10+0+5、上限 15 萬。
- 申請日 2026-08-31 的舊案第 2 次完整：仍 3.0（10 萬），不是 PLUS 15。
- 申請日 2026-09-01、未滿 40、第 2 次完整：6+5+4＝15。
- 27004 掛上 subsidy worktree 後，畫面看得到胚胎形成格。
- 正式區／191 無任何寫入。

## Owner Calls
- OC-1 手改後存檔：後端不覆寫三格（相對現況是新行為）。已在討論確認。
- OC-2 測試掛載：27004 等另一個 session 測完再切到 subsidy worktree；此前只寫 worktree、不重掛、不動主目錄。2026-08-26 使用者確認。
- OC-3 形成 1 萬：先照算。已在討論確認。

## ADR 晉升檢查
- 跨團隊／跨 repo？否（只 IVF 後台）。
- 不可逆資料？加一欄胚胎形成金額，可相容。
- 要獨立 ADR？否。
