# STAGE1-REVIEW: 20260823-0030

來源:PR #18 owner comment(2026-08-22T16:37:15Z)
https://github.com/rick546986/dev-flow/pull/18#issuecomment-5381458104

回應:`docs/dev/stage1-loop/20260823-0001-claude.md`
branch:`claude/stage1-graph` @ c7bf22a
PR:#18 draft

驗證:CI EXTERNAL_RUNTIME 綠;REPO_REFERENCE 紅
(job 97056364969,run 32583549502)。mergeable_state=unstable。
沒有更舊的 `*-review.md`。本檔是第一則 STAGE1-REVIEW 的接續渠道。

本輪最多 3 條,假綠優先。不要拆剩餘 0–11。
不要為本機缺 python 開刀(CI 已有 python)。

## 1. P0 健康:REPO_REFERENCE 紅

先修這條再加產品刀。失敗組原文要貼進接續紀錄。
`devflow-check all` 跑了 1m22s,不是開頭就 REG_MISS。
不要為 `check-methodology-corrections` / `check-py-floor` 本機缺 python 開刀。

CI 失敗組:`contracts` → `contracts/memory` exit 1。
`test_skill_contract.DevTalkSkillContractTest` 3 案:

1. `test_later_commands_use_the_captured_session` —
   步 11 的 `talk end` 200 字內沒有 `$MEMORY_SESSION_ID`
2. `test_start_is_the_first_memory_action` —
   SKILL.md 沒有 `dev-memory.py talk start`
3. `test_zero_promoted_is_declared_legal` —
   SKILL.md 沒有 `promoted: 0`

根因:第一刀把指令正文搬進節點檔,入口摘要沒留契約字串。
修入口摘要,不改 `memory/` 程式。

## 2. P0 假綠:graph.yaml next 把暫留步跳掉

`N3-probe.next = N9-write-md`、`N9-write-md.next = N13-end`;
`via: skill-legacy-4-6` / `8-10` 只是字串,檢查器只比節點「下一跳」
第一個 N-id 與 yaml next 相同。牙齒把「跳過 4–6、8–10」鎖成綠。
守下一跳正本的人會跳驗收雛形/發散/盲點/複核/詞彙/html。

修法:next 不要跨過暫留步(via 做成真節點,或 next 仍指向暫留入口)。
本輪不要把 0–11 全拆完。

## 3. P1 假綠:--action 沒接到 runtime

`check-devtalk-graph.sh --action` 只在 `test-devtalk-graph.sh` 的
JSON fixture 被呼叫。SKILL / hooks 沒有在 /dev-talk 期間跑它。
agent 在 N3 再 talk start,產品檢查仍綠。

修法:至少讓入口必跑一次,或 hook 接 `--action`。
不要為了這條去拆剩下的步驟。

## 可合併

還沒。CI 一條紅、上面兩條產品假綠未修。
