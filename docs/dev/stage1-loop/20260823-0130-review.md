# STAGE1-REVIEW: 20260823-0130

來源:PR #18 owner comment(2026-08-22T17:34:03Z)
https://github.com/rick546986/dev-flow/pull/18#issuecomment-5381703692

回應:`docs/dev/stage1-loop/20260823-0046-claude.md`
branch:`claude/stage1-graph` @ 3ac7141
PR:#18 draft

驗證:CI 兩條綠(REPO_REFERENCE job 97062185420、EXTERNAL_RUNTIME job 97062185122)。
mergeable_state=clean。0030 三條都有先紅後綠(contracts/memory 3 FAIL→10/10;
graph 牙 14/16→17/17;next 經 skill-legacy-0-2 / 4-6 / 8-10;prebash 接 --action)。
沒誤動 memory/、Stage 7、Gauntlet。盲原則還在 SKILL。durable 寫入仍只在 N13 talk end。
guide-sync / selfclean / file-map 沒脫鉤。沒 merge、沒推 main。

0030 已收。本輪假綠優先。不要拆剩餘 0–11。不要為本機缺 python 開刀。

## 1. P0 假綠:N1 有 session 仍可 talk start

N1 完成條件把游標留在 N1-start。graph.yaml N1 allow talk_start,沒有
forbid talk_start_if_session。evaluate_action 只在 forbid 含
talk_start_if_session 時擋「有 session 再 start」。現有牙把游標寫到 N3
才紅;happy path(游標依完成條件停在 N1)第二下 start 仍綠。

修法:N1 加 forbid talk_start_if_session(與 N3 同款)。
負向牙:游標 N1-start + MEMORY_SESSION_ID + talk_start 必須紅。

## 2. P0 假綠:沒游標檔 = --action 永不擋

hooks/_prebash_impl.py 缺 .devtalk-cursor.json 就 return 放行。repo 沒有
寫入該檔的實作(N1 只散文「本機游標寫成 N1-start」)。預設 /dev-talk 走的
是這條放行路,0030 接上的 hook 咬不到。

修法二選一,不要改 .dev-flow 儲存、不要拆 0–11:

- 缺游標時若已有 OPEN session,talk start 仍擋(讀 session 狀態即可)
- 或補最小 writer,N1 必跑,牙齒驗 SKILL/N1 有呼叫

負向牙:沒有 cursor 檔 + 已有 session + talk start 必須紅。

## 3. P2 接續:補本檔

與本 comment 同文。

## 可合併

還沒。CI 綠、mergeable 乾淨、0046 已答完 0030,但上面兩條產品假綠 P0 未修。
