# Claude 回應:20260823-0947-review.md
- 回應的 review 檔名: 20260823-0947-review.md
- branch: claude/stage1-graph-2
- PR: https://github.com/rick546986/dev-flow/pull/21 (draft)
- 本輪類型: 回應 review(第二刀兩條)
- 新增 commit: 367fb07 fix(talk): 0947 — 遊標寫入與暫留步真節點檔；本檔補 SHA
- 本檔 raw URL: https://raw.githubusercontent.com/rick546986/dev-flow/claude/stage1-graph-2/docs/dev/stage1-loop/20260823-1006-claude.md
- PR comment: 已吃 owner STAGE1-REVIEW 0947(https://github.com/rick546986/dev-flow/pull/21#issuecomment-5383657050)
- review 讀取: newest=PR comment 20260823-0947(01:49Z)與 `docs/dev/stage1-loop/20260823-0947-review.md` 同文。沒有更新的 STAGE1-REVIEW

健康:`origin/main`(`33abfc3`)是 HEAD 祖先。
`git merge-tree --write-tree origin/main HEAD` exit 0。
本輪不開健康刀。不要為本機缺 `python` 開刀。

## 舊實作下先紅(TDD,改產品前)

對當時 live(`9f63860`,只有 0947 種子、產品仍是第一刀形狀)探測:

`--action` 沒游標檔:
1. write_code → rc=**2**(`error	action JSON 缺 cursor.node`)
2. talk_end → rc=**2**(同上)
3. write_knowledge → rc=**2**(同上)

SKILL.md / `nodes/N1-start.md`:**沒有** `--write-cursor`。
leftover 節點檔 S0/S1/S2/S4/S5/S6/S8/S9/S10:**MISSING**。
live `check-devtalk-graph.sh` 仍綠(`nodes_required=4`)。
prebash 沒游標 + talk end / write knowledge → rc=**0**(放行)。

加上 0947 牙齒後、改產品前,舊形狀會讓新牙紅(沒游標應 deny 卻 error/放行;SKILL/N1 無呼叫;缺 leftover 檔)。

## 採納並修好的 finding

### 0947-P0 遊標寫入
- 位置:`scripts/check-devtalk-graph.sh --write-cursor`;`nodes/N1-start.md`;`SKILL.md`;
  `hooks/_prebash_impl.py` `evaluate_action`
- 我查到的根因:N1 只散文「本機游標寫成 N1-start」。repo 沒有 writer。
  缺 `.devtalk-cursor.json` 時,`--action` 對 write_code / talk_end / write_knowledge
  回 error(exit 2);prebash 只對 talk_start 合成無 node payload,其餘放行。
- 舊實作下真的紅:上表三條 rc=2、SKILL/N1 無呼叫、prebash rc=0
- 修法:N1 / SKILL 呼叫 `--write-cursor N1-start "$MEMORY_SESSION_ID"`,
  寫 `.devtalk-cursor.json`(node + MEMORY_SESSION_ID)。檔仍 gitignore,不改 `.dev-flow`。
  沒游標檔時這三個 action → deny(exit 1)。prebash 沒游標仍送 --action。
- 測試:`test-devtalk-graph.sh` **33/33**(含 writer 真的寫出檔)

### 0947-P0 暫留步變真節點檔
- 位置:`skills/dev-talk/nodes/S0-scope.md` 等 9 檔;`graph.yaml`;
  `SKILL.md` 步 0/1/2/4/5/6/8/9/10 改摘要;`guides/guide-dev-talk.html` 原文
- 我查到的根因:`skill-legacy-0-2` / `4-6` / `8-10` 是團塊 hop,正本仍在 SKILL。
  next 只保證不跨團塊,單步仍可被跳過;SKILL 與節點會雙正本。
- 舊實作下真的紅:9 個 leftover 檔 MISSING;live 仍只要求 4 節點
- 修法:鏈是
  N1 → S0 → S1 → S2 → N3 → S4 → S5 → S6 → N9 → S8 → S9 → S10 → N13。
  每檔六節。graph next 跳過(N3→N9 或 N3→S5)→ 紅。SKILL 只留入口與摘要。
  乘客地圖仍 0–11 共 12 步。
- 測試:live graph `nodes_required=13`;guide-sync **12** 原文 + **12** 步;
  雙正本牙(N3 / S2)仍紅

### 0947 接續通道
- 本檔回應 `20260823-0947-review.md` 與 PR #21 comment 同文兩條。

## 查證後不成立的 finding
無。0947 兩條都成立,都修了。

## 刻意不做的 finding
- 不改 `guides/guide-dev-talk.html` 步數(仍 12)
- 不改 `html-shell.html`、不開真實 dogfood 討論檔
- 不改 memory/ 程式、Gauntlet、G3、`_templates/7-review.md`、`docs/dev/stage7-loop/`
- 不改 `.dev-flow` 儲存
- 不為本機缺 python 開刀(`command -v python` 空;`methodology` 已知會紅)
- 不 merge、不推 main

## 本輪未處理(下一輪候選,依優先序)
1. 游標寫入實作已在 N1 / leftover / N9。N3 / N13 的 做什麼沒強制呼叫 writer;
   若 agent 不更新游標,游標會停在上一 hop。本輪牙齒只驗 SKILL / N1
2. prebash 仍不把任意寫程式編成 `write_code`(`echo write_code` 字面仍放行)。
   `--action` JSON 沒游標 + write_code 已 deny
3. 本環境沒有 `python` 指令,`check-methodology-corrections` 會紅;
   不是本輪產品洞

## 需要 owner 裁決
無新項。0947 兩條照做。draft 還要不要合,是人審。本迴圈不按。

## 驗證結果
| 項 | 結果 |
|---|---|
| `scripts/test-devtalk-graph.sh`(改後) | **33/33** |
| `scripts/check-devtalk-graph.sh` | **綠**(`nodes_required=13`) |
| `scripts/check-devtalk-selfclean.sh` | **16** 檔全過 |
| `scripts/check-devtalk-guide-sync.sh` | 12 原文 + 12 步 |
| `scripts/check-file-map.sh` | scanned=**137**(無新必列檔;writer 掛在既有 check 腳本) |
| `memory` DevTalkSkillContractTest | **10/10**(含於 contracts/memory 550) |
| `hooks/selftest.sh` | **402/402** |
| `hooks/gate-consistency.sh` | **14/14** |
| `hooks/devflow-exec.sh doctor` | COMPATIBLE;gauntlet 1.3.3 |
| `scripts/check-gate-tokens.sh` | 全過 |
| `scripts/check-version-sync.sh` | 5 處 **1.3.3** |
| `scripts/check-readme-markers.sh` | 5/5 |
| `scripts/check-selfjudgment-tables.sh` | 36/36 |
| `scripts/check-spec-gate.sh`(範例) | 5/5 |
| `scripts/check-regex-charclass.sh` | 全過 |
| `scripts/test-architecture-guards.sh` | **134/134** |
| `scripts/devflow-check.sh contracts` | **5/5 綠**(gauntlet 68/68、obs 125、memory 550 + eval 23/23) |
| `scripts/devflow-check.sh methodology` | 本輪未重跑 `all`;已知缺 `python` 會紅。不開刀 |
| 平台 | 只證明 Linux。沒跑 macOS / Windows。**不要把上面的綠讀成 CI 已綠** |

## 給下一輪審查的輸入
- 最該被挑戰:writer 是 `check-devtalk-graph.sh --write-cursor`,不是獨立腳本。
  牙齒只驗 SKILL / N1 有呼叫。N3 / N13 沒強制寫游標。
- prebash 沒把寫程式編成 action;沒游標時 `--action` JSON 已咬 write_code。
- 我不打算改的方向:不改乘客地圖 12 步;不改 memory/ 儲存;不重開 Stage 2–7;不 merge
