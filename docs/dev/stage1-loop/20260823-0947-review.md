# STAGE1-REVIEW: 20260823-0947 種子

來源: owner 要續第二刀。#18 已合 main。
branch: `claude/stage1-graph-2`

第一刀已收: 四個節點檔、單一 `1-discussion.md`、有 OPEN 場不得再 talk start。本輪不重開那兩條。
不動 `memory/` 程式、Gauntlet、G3、`_templates/7-review.md`。不改 `.dev-flow` 儲存。不 merge。
乘客地圖仍是 0–11 共 12 步（`check-devtalk-guide-sync` 要綠）。

本輪最多 2 條。假綠優先。TDD：舊實作先紅再改。

## 1. P0 遊標寫入

N1 要把 `.devtalk-cursor.json` 寫成 `N1-start` + `MEMORY_SESSION_ID`。
牙齒驗 SKILL / N1 有呼叫。檔仍不進 Git。

沒遊標檔時，N9 寫程式、N13 前 `talk end` / 寫知識目錄，`--action` 要咬到。
不能只靠「有 OPEN session 就擋 talk start」。

## 2. P0 暫留步變真節點檔

0/1/2/4/5/6/8/9/10 要有節點檔：進條件 / 做什麼 / 完成條件 / 下一跳。
`graph.yaml` next 不得跳過。SKILL.md 只留入口與摘要，不要雙正本。

## 可合併

還沒。上面兩條未修。
