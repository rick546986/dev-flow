# Claude 回應:種子三條(無 *-review.md)
- 回應的 review 檔名: 無(docs/dev/stage1-loop/ 尚無 *-review.md;無較新的 STAGE1-REVIEW PR comment)。本輪做種子三條。
- branch: claude/stage1-graph
- PR: (push 後開 draft,見本檔後續補列)
- 本輪類型: 種子三條 / 第一刀
- 新增 commit: (本檔同批落地,SHA 見 git log)
- 本檔 raw URL: https://raw.githubusercontent.com/rick546986/dev-flow/claude/stage1-graph/docs/dev/stage1-loop/20260823-0001-claude.md
- PR comment: 無(本輪是實作,不是審查)
- review 讀取: newest=無, last=無;種子三條為準

健康:`origin/main`(`e0e58b2`)是 HEAD 祖先。main 上 CI 兩條在觸發時已 SUCCESS
(`devflow-ci` 32582485654 / `runtime-selftest` 32582485639)。健康綠,不佔整輪。

## 舊實作下先紅(TDD,改產品前)

對 `origin/main` 形狀(單一 `SKILL.md`、沒有 `graph.yaml` / `nodes/`)跑
`scripts/check-devtalk-graph.sh`:

- exit **1**
- failures=**7**
  1. 缺 `skills/dev-talk/graph.yaml`
  2. 缺 `nodes/N1-start.md`
  3. 缺 `nodes/N3-probe.md`
  4. 缺 `nodes/N9-write-md.md`
  5. 缺 `nodes/N13-end.md`
  6. P0-1 沒有 N9 覆寫契約,無法證明重跑落檔不會另存
  7. P0-3 沒有游標契約,無法證明重跑 N3 不會重開 talk start

同批先落地的負向牙 `scripts/test-devtalk-graph.sh` 在舊實作上已是 **14/14**
(對照組綠;舊 SKILL fixture 紅;缺進條件 / 缺完成條件 / next 不一致 / 雙正本 /
第二份 md / 另存 / N3 重開 session / N9 寫程式 / N9 talk end / N13 前寫知識 /
N1 talk start / N13 talk end)。

## 採納並修好的 finding

### 種子 P0-1 產物仍是一份
- 位置:`skills/dev-talk/graph.yaml` N9 `write` + `write_mode: overwrite`;
  `nodes/N9-write-md.md` 寫哪裡;`scripts/check-devtalk-graph.sh` 模擬重跑兩次
- 舊實作下真的紅:上表第 6 項 + fixture「另存」「第二份 md」
- 修法:N9 只覆寫 `docs/dev/<slug>/1-discussion.md`;檢查模擬重跑後
  `1-discussion*.md` 必須剛好一份;同 slug 目錄兩份直接紅;其他節點不得
  `allow write_discussion_md`
- 測試:live `check-devtalk-graph.sh` 綠;`test-devtalk-graph.sh` **14/14**

### 種子 P0-2 四個節點檔必須可單獨當入口
- 位置:`nodes/N1-start.md` / `N3-probe.md` / `N9-write-md.md` / `N13-end.md`;
  `graph.yaml` next;`SKILL.md` 步 3/7/11 改摘要
- 舊實作下真的紅:上表第 1–5 項 + fixture 缺進條件/完成條件/next/雙正本
- 修法:每個節點六節(進條件 / 讀什麼 / 寫哪裡 / 做什麼 / 完成條件 / 下一跳);
  graph next 與節點下一跳必須同一 id;N3 正文只在 `N3-probe.md`;
  SKILL 步 3 只留入口摘要。guide 步數仍是 0–11 共 12,只改三步「原文」引用
  以維持 `check-devtalk-guide-sync.sh` 綠
- 測試:同上 14/14;guide-sync 12 段原文 + 12 步

### 種子 P0-3 重跑不得重開 session
- 位置:`graph.yaml` allow/forbid;`scripts/check-devtalk-graph.sh --action`;
  fixture `scripts/fixtures/devtalk-graph/actions/*.json`
- 舊實作下真的紅:上表第 7 項 + 四條 action 負向
- 修法:N3 + 已有 MEMORY_SESSION_ID → deny `talk_start`;N9 forbid
  `write_code` / `talk_end` / `write_knowledge`;N13 之前 forbid
  `write_knowledge`;N1 無 session 才 allow `talk_start`;N13 才 allow `talk_end`。
  本機游標不進 Git(`.gitignore` 的 `.devtalk-cursor.json`);不改 `.dev-flow` 儲存
- 測試:四條 deny + 兩條 allow,都在 14/14 裡

## 查證後不成立的 finding
無。沒有既有 review 檔可推翻。

## 刻意不做的 finding
- 不把 0–11 全拆完(步 0/1/2/4/5/6/8/9/10 暫留 SKILL)
- 不改 `guides/guide-dev-talk.html` 步數(仍 12)
- 不改 `html-shell.html`
- 不開真實 `docs/dev/<新slug>/1-discussion.md` dogfood
- 不改 memory/ 程式、Gauntlet、G3、`_templates/7-review.md`、`docs/dev/stage7-loop/`
- 不 merge、不推 main

## 本輪未處理(下一輪候選,依優先序)
1. 未拆步驟(0/1/2/4/5/6/8/9/10)何時抽成節點,本輪不裁
2. 本機游標只有 gitignore 宣告,沒有讀寫實作(本輪不改 `.dev-flow` 儲存)
3. 本環境沒有 `python` 指令,`check-methodology-corrections` / `check-realworld`
   的 renderer 紅;`check-py-floor.sh` exit 2。不是本輪產品洞

## 需要 owner 裁決
無新項。四節點切法已在種子裁定,照做。

## 驗證結果
| 項 | 結果 |
|---|---|
| `scripts/check-devtalk-graph.sh`(舊實作) | **exit 1 / 7 項**(改前) |
| `scripts/check-devtalk-graph.sh`(新實作) | **綠** |
| `scripts/test-devtalk-graph.sh` | **14/14** |
| `scripts/check-devtalk-selfclean.sh` | 7 檔全過(含 graph + 四節點) |
| `scripts/check-devtalk-guide-sync.sh` | 12 原文 + 12 步 |
| `scripts/check-file-map.sh` | scanned=**137** |
| `scripts/test-architecture-guards.sh` | **134/134**(對照 14/14、負向 120/120) |
| `hooks/selftest.sh` | **402/402**(含 devtalk-guard) |
| `hooks/gate-consistency.sh` | **14/14** |
| `hooks/devflow-exec.sh doctor` | COMPATIBLE;gauntlet 1.3.3 |
| `scripts/check-gate-tokens.sh` | 全過(未增刪粗體 token) |
| `scripts/check-version-sync.sh` | 5 處 **1.3.3** |
| `scripts/check-readme-markers.sh` | 5/5 |
| `scripts/check-selfjudgment-tables.sh` | 36/36 |
| `scripts/check-spec-gate.sh`(範例+全形) | 各 5/5 |
| `scripts/check-regex-charclass.sh` | 全過 |
| `scripts/check-methodology-corrections.sh` | **105/106**:renderer 缺 `python` |
| `scripts/check-realworld.sh` | **136/137**:同一 renderer 缺 `python` |
| `scripts/check-py-floor.sh` | **exit 2**:環境沒有 3.9–3.11 |
| `scripts/devflow-check.sh methodology` | **exit 1**:第一張網就是 renderer,後面沒跑;本輪產品檢查已單獨跑綠 |
| 平台 | 只證明 Linux。沒跑 macOS / Windows |

## 給下一輪審查的輸入
- 最該被挑戰:SKILL 步 3/7/11 改成摘要後,guide「原文」跟著改短 —— 步數沒變,
  但乘客手冊不再內嵌 N3 全文。若要 guide 仍展示逼問三律,應改指向節點檔,
  不要把正文搬回 SKILL(否則雙正本牙會紅)。
- 我不打算改的方向:不一次拆完 0–11;不改 `.dev-flow` 儲存;不重開 Stage 2–7;
  不 merge
