# Claude 回應:20260823-0030-review.md
- 回應的 review 檔名: 20260823-0030-review.md
- branch: claude/stage1-graph
- PR: https://github.com/rick546986/dev-flow/pull/18 (draft)
- 本輪類型: 回應 review
- 新增 commit: 4a69687 fix(talk): 0030 — 契約字串、暫留 next、--action 接 hook；本檔補列 SHA
- 本檔 raw URL: https://raw.githubusercontent.com/rick546986/dev-flow/claude/stage1-graph/docs/dev/stage1-loop/20260823-0046-claude.md
- PR comment: 已吃 owner STAGE1-REVIEW 0030(https://github.com/rick546986/dev-flow/pull/18#issuecomment-5381458104)
- review 讀取: newest=20260823-0030-review.md(本輪補寫), last=無;PR comment 與本檔同文,以 comment 為準

健康:`origin/main`(`e0e58b2`)是 HEAD 祖先。
CI 失敗組原文(run 32583549502,job 97056364969,約 1m22s):
```
════════════════════════ summary ════════════════════════
  ✅ group:methodology
  ✅ group:architecture
  ✅ group:render
  ❌ contracts (exit 1)
⛔ devflow-check(all): FAILED at 「contracts (exit 1)」
```
組內:`contracts/memory` exit 1。
`test_skill_contract.DevTalkSkillContractTest` **3 FAIL**:
1. `test_later_commands_use_the_captured_session` — 步 11 `talk end` 200 字內無 `$MEMORY_SESSION_ID`
2. `test_start_is_the_first_memory_action` — SKILL.md 無 `dev-memory.py talk start`
3. `test_zero_promoted_is_declared_legal` — SKILL.md 無 `promoted: 0`
EXTERNAL_RUNTIME 綠。不要為本機缺 python 開刀。

## 舊實作下先紅(TDD,改產品前)

- `python3 -m unittest memory.tests.test_skill_contract.DevTalkSkillContractTest`:
  **10 案 / 3 FAIL**(上列三條)。未改 `memory/` 程式。
- `scripts/test-devtalk-graph.sh` 加上 0030 兩顆牙之後:**14/16**,2 案未依預期
  (舊檢查把「N3 next=N9」與「hooks 沒接 --action」判綠):
  1. `0030-P0 N3 next 跨過暫留步 4–6 必須紅` rc=0 want=1
  2. `0030-P1 hooks 沒接 --action 必須紅` rc=0 want=1
- 當時 live `check-devtalk-graph.sh` 仍綠(牙齒還沒長)。

## 採納並修好的 finding

### 0030-P0 健康:REPO_REFERENCE 紅
- 位置:`skills/dev-talk/SKILL.md` 第一動 + 步 11;`guides/guide-dev-talk.html` 步 11 原文
- 我查到的根因:第一刀把指令正文搬進節點,入口摘要沒留契約字串。
  `memory/tests/test_skill_contract.py` 只讀 SKILL.md。
- 舊實作下真的紅:上表 3 FAIL
- 修法:入口補回 `dev-memory.py talk start`、`talk end $MEMORY_SESSION_ID`、
  `promoted: 0`。正本仍在 N1 / N13。不改 `memory/`。
- 測試:DevTalkSkillContractTest **10/10**;
  `memory/run-tests.sh` **550** + eval 23/23;
  `scripts/devflow-check.sh contracts` **5/5 綠**(含 memory)

### 0030-P0 假綠:graph.yaml next 把暫留步跳掉
- 位置:`skills/dev-talk/graph.yaml`;四節點「下一跳」;`scripts/check-devtalk-graph.sh`
- 我查到的根因:`via: skill-legacy-4-6` 只是字串。檢查只比第一個 N-id。
  N3→N9、N9→N13 把步 4–6 / 8–10 鎖成綠。
- 舊實作下真的紅:上表 0030-P0 牙 rc=0
- 修法:via 做成真節點,不拆 0–11 正文。鏈是
  N1 → skill-legacy-0-2 → N3 → skill-legacy-4-6 → N9 → skill-legacy-8-10 → N13。
  暫留節點 `kind: skill-legacy`,入口仍是 SKILL.md。
  N3 next 直接跳 N9 → 紅(針「暫留」)。
- 測試:`test-devtalk-graph.sh` **17/17**;live graph 綠

### 0030-P1 假綠:--action 沒接到 runtime
- 位置:`hooks/_prebash_impl.py`;`scripts/check-devtalk-graph.sh`
- 我查到的根因:`--action` 只被 test fixture 呼叫。/dev-talk 通常未武裝,
  prebash 在 `load_state is None` 早退,產品檢查仍綠。
- 舊實作下真的紅:上表 0030-P1 牙 rc=0
- 修法:prebash 在武裝早退前跑 `--action`(talk start/end、寫知識目錄)。
  沒有本機游標則放行(新場)。hooks 檔若只有註解提到 `--action` 仍紅。
  游標在 N3 再 talk start → prebash exit 2。
- 測試:0030-P1 兩顆牙 + prebash 實擋,都在 17/17 裡

## 查證後不成立的 finding
無。0030 三條都成立,都修了。

## 刻意不做的 finding
- 不把 0–11 全拆完(暫留步只有 graph 節點,沒有 nodes/*.md 正文)
- 不改 `guides/guide-dev-talk.html` 步數(仍 12;只改步 11 原文以跟 SKILL)
- 不改 `html-shell.html`、不開真實 dogfood 討論檔
- 不改 memory/ 程式、Gauntlet、G3、`_templates/7-review.md`、`docs/dev/stage7-loop/`
- 不為本機缺 python 開刀
- 不 merge、不推 main

## 本輪未處理(下一輪候選,依優先序)
1. 未拆步驟(0/1/2/4/5/6/8/9/10)何時抽成節點,本輪不裁
2. 本機游標仍無寫入實作(gitignore 有;prebash 只讀)。沒游標時 talk start 仍放行
3. 本環境沒有 `python` 指令,`check-methodology-corrections` 105/106 紅;
   `devflow-check.sh methodology` 在第一張網 fail-fast。不是本輪產品洞

## 需要 owner 裁決
無新項。0030 三條照做。draft 還要不要合,是人審。

## 驗證結果
| 項 | 結果 |
|---|---|
| `memory` DevTalkSkillContractTest(改前) | **3 FAIL / 10** |
| `memory` DevTalkSkillContractTest(改後) | **10/10** |
| `scripts/test-devtalk-graph.sh`(改前) | **14/16**(兩條 0030 該紅卻綠) |
| `scripts/test-devtalk-graph.sh`(改後) | **17/17** |
| `scripts/check-devtalk-graph.sh` | **綠** |
| `scripts/check-devtalk-selfclean.sh` | 7 檔全過 |
| `scripts/check-devtalk-guide-sync.sh` | 12 原文 + 12 步 |
| `scripts/check-file-map.sh` | scanned=**137**(無新必列檔) |
| `scripts/devflow-check.sh contracts` | **5/5**(memory 550、gauntlet 68/68、obs 125、parallel 131、vnext 14) |
| `hooks/selftest.sh` | **402/402**(含 prebash / devtalk-guard) |
| `hooks/gate-consistency.sh` | **14/14** |
| `hooks/devflow-exec.sh doctor` | COMPATIBLE;gauntlet 1.3.3 |
| `scripts/check-gate-tokens.sh` | 全過 |
| `scripts/check-version-sync.sh` | 5 處 **1.3.3** |
| `scripts/check-readme-markers.sh` | 5/5 |
| `scripts/check-selfjudgment-tables.sh` | 36/36 |
| `scripts/check-spec-gate.sh`(範例+全形) | 各 5/5 |
| `scripts/check-regex-charclass.sh` | 全過 |
| `scripts/test-architecture-guards.sh` | 產品 commit 後重跑(見下) |
| `scripts/check-methodology-corrections.sh` | **105/106**:renderer 缺 `python` |
| `scripts/devflow-check.sh methodology` | **exit 1**:第一張網就是 renderer,後面沒跑;talk 檢查已單獨跑綠 |
| 平台 | 只證明 Linux。沒跑 macOS / Windows。**不要把上面的綠讀成 CI 已綠** |

## 給下一輪審查的輸入
- 最該被挑戰:暫留步做成 graph 節點但沒有 nodes/*.md —— 守下一跳的人會進 SKILL 暫留正文,
  這是刻意的。若要「每個 hop 都有六節檔」,那是下一刀,不是把 0–11 拆完。
- prebash 沒游標就放行 talk start。agent 不寫 `.devtalk-cursor.json` 時,
  N3 再 start 仍過得了 hook。游標寫入實作還沒做(本輪不改 `.dev-flow` 儲存)。
- 我不打算改的方向:不一次拆完 0–11;不改 memory/ 儲存;不重開 Stage 2–7;不 merge
