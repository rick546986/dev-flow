# Claude 回應:Stage 1 第三刀(寫程式編成與遊標 hop)
- 回應的 review 檔名: 本輪 automation prompt(第三刀三條)。殘來自 1042 / 0947 / 0230-P1
- branch: claude/stage1-graph-3
- PR: (push 後補)
- 本輪類型: 第三刀實作(P0-1 write_code 編成 / P0-2 進節點寫遊標 / P1 guide 對上 13 節點)
- 新增 commit: (push 後補)
- 本檔 raw URL: (push 後補)
- review 讀取: newest 檔名仍是 `20260823-1042-claude.md` + `20260823-0947-review.md`。#18／#21 已合 main(`29eb2fc`)。本輪依 owner 第三刀 prompt 開刀,不重做遊標檔格式、不重切 13 節點、不改乘客地圖 12 步

健康:`origin/main`(`29eb2fc`)是本 branch 起點。
不要為本機缺 `python` / 缺 3.9 開刀(`check-py-floor` 本機 exit 2;CI 有 3.9)。

## 舊實作下先紅(TDD,改產品前)

`scripts/test-devtalk-graph.sh` **43/54**(11 紅):

1. G-0 對照組紅:fixture 節點「做什麼」缺 `--write-cursor <id>`
2. P0-1 prebash 游標 N9:`python3 -c open/write` / `python -c open` / `>` / `>>` / `tee` 五條 **rc=0**(放行,want=2)
3. P0-1 prebash 沒游標+OPEN session:同上五條 **rc=0**

live 舊實作同時紅:

- `check-devtalk-graph.sh`:N3-probe / N13-end「做什麼」缺 `--write-cursor`(2 項)
- `check-devtalk-guide-sync.sh`:guide 仍寫「只拆了四個」「正文還在 SKILL.md」

只讀(`cat`/`rg`/`ls`)、`talk turn`/`talk propose`、`> /dev/null` 舊實作已是 rc=0,保持。
P1 突變牙(把那兩句塞回 guide)舊實作已紅。

## 採納並修好的 finding

### P0-1 寫程式編成 write_code
- 位置:`hooks/_prebash_impl.py` `_looks_like_write_code` + `_devtalk_graph_action`
- 我查到的根因:`--action` 對 `write_code` 會 deny,但 prebash 只編成 talk_start / talk_end / write_knowledge。`python3 -c "open(...).write(...)"` / `>` / `tee` 字面活在測 JSON,runtime 咬不到直接改檔
- 舊實作下真的紅:上列 10 條 prebash rc=0
- 修法:未武裝、在 `load_state` 早退之前。本機有 `.devtalk-cursor.json` 或已有 OPEN talk session,才把會改檔的 shell 編成 `write_code` 送 `--action`。只認 `python3|-c`/`python -c` 含 `open(` 或 `.write(`、`>`/`>>` 寫到檔(`/dev/null` 與 `>&` 除外)、`tee` 寫到檔。不是沙盒、不擴黑名單。X-5b 武裝期 `.devflow` 偽造邊界不動
- 測試:游標 N9 五條擋;沒游標+OPEN session 五條擋;只讀與 talk turn/propose / `> /dev/null` 不誤編

### P0-2 進節點就寫遊標
- 位置:`check-devtalk-graph.sh` 每個 REQUIRED_NODES「做什麼」必須含 `--write-cursor <id>`;live `N3-probe.md` / `N13-end.md`
- 我查到的根因:N1／N9／S 節點已有呼叫;N3／N13 沒有。半路重跑會拿舊 node
- 舊實作下真的紅:live graph 2 項;G-0 fixture 缺行
- 修法:N3／N13 補同一行 writer。N1／N9 保持。檔仍 gitignore,不改 `.dev-flow`
- 測試:N3、N13、S0-scope 缺這行必須紅

### P1 talk 導覽開頭對上 13 節點
- 位置:`guides/guide-dev-talk.html` ② 開頭;`check-devtalk-guide-sync.sh` 加針
- 我查到的根因:guide-sync 只對步數與「原文」逐字,「只拆了四個／正文還在 SKILL.md」假綠
- 舊實作下真的紅:加上針之後 live guide-sync 兩句都紅
- 修法:鏈改成 `N1-start → S0 → S1 → S2 → N3-probe → S4 → S5 → S6 → N9-write-md → S8 → S9 → S10 → N13-end`。表每步點名節點檔。不改 SKILL 正文、不改乘客地圖 12 步
- 測試:guide 出現那兩句必須紅;live 12 原文 + 12 步

## 查證後不成立的 finding
無。本輪三條都成立,都修了。

## 刻意不做的 finding
- 不重做遊標檔格式、不重切 13 節點、不改乘客地圖 12 步
- 不做完整沙盒、不把 write_code 編成通用編譯器
- 不改 Stage 6 武裝期 X-5b(`.devflow` 偽造信任邊界)
- 不改 `html-shell.html`、不開真實 dogfood 討論檔
- 不改 memory/ 程式、Gauntlet、G3、`_templates/7-review.md`、`docs/dev/stage7-loop/`
- 不改 `.dev-flow` 儲存
- 不追加 HISTORY.md
- 不改契約字串意思(`talk start` / `talk end` / `promoted: 0`)
- 不為本機缺 python / 缺 3.9 開刀
- 不 merge、不推 main

## 本輪未處理(下一輪候選,依優先序)
1. write_code 編成是最小集合。heredoc / `python3 script.py` 直寫 / `sed -i` 仍咬不到。假綠再補一刀,不要一次發明通用編譯器
2. 本環境沒有 `python` 3.9,`check-py-floor` 本機 exit 2;不是本輪產品洞(CI 有 3.9)
3. durable 寫入仍只在 talk end;本輪沒改 memory/

## 需要 owner 裁決
無新項。draft 還要不要合,是人審。本迴圈不按。

## 驗證結果
| 項 | 結果 |
|---|---|
| `scripts/test-devtalk-graph.sh`(改前) | **43/54**(11 紅;先紅) |
| `scripts/test-devtalk-graph.sh`(改後) | **54/54** |
| `scripts/check-devtalk-graph.sh` | **綠**(`nodes_required=13`) |
| `scripts/check-devtalk-selfclean.sh` | **16** 檔全過 |
| `scripts/check-devtalk-guide-sync.sh` | 12 原文 + 12 步;兩句假綠針在 |
| `scripts/check-file-map.sh` | scanned=**139**(無新必列檔) |
| `hooks/selftest.sh` | **402/402** |
| `hooks/gate-consistency.sh` | **14/14** |
| `scripts/devflow-check.sh methodology` | **11/11 綠** |
| `scripts/devflow-check.sh contracts` | **5/5 綠** |
| `scripts/devflow-check.sh render` | **2/2 綠** |
| `scripts/devflow-check.sh architecture` | 本機 `check-py-floor` exit 2(缺 3.9)。其餘 architecture 項綠。不開刀 |
| `scripts/devflow-check.sh all` | 本機因 py-floor 記紅;CI 有 3.9,應兩條都綠 |
| 平台 | 只證明 Linux。沒跑 macOS / Windows。**不要把上面的綠讀成 CI 已綠** —— CI 要等 push |

## 給下一輪審查的輸入
- 最該被挑戰:write_code 編成只認三種字面。agent 用 `python3 script.py` 或 heredoc 仍繞得過。這是本輪明示的最小刀,不是沙盒
- 遊標 hop 牙齒驗 N3／N13／S0「做什麼」有呼叫;其他 S 節點 live 已有,fixture 也補了
- 我不打算改的方向:不改乘客地圖 12 步;不改 memory/ 儲存;不重開 Stage 2–7;不 merge
