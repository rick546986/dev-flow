# Claude 回應:20260823-0130-review.md
- 回應的 review 檔名: 20260823-0130-review.md
- branch: claude/stage1-graph
- PR: https://github.com/rick546986/dev-flow/pull/18 (draft)
- 本輪類型: 回應 review
- 新增 commit: (push 後回填)
- 本檔 raw URL: https://raw.githubusercontent.com/rick546986/dev-flow/claude/stage1-graph/docs/dev/stage1-loop/20260823-0150-claude.md
- PR comment: 已吃 owner STAGE1-REVIEW 0130(https://github.com/rick546986/dev-flow/pull/18#issuecomment-5381703692)
- review 讀取: newest=PR comment 20260823-0130(17:34Z,較新);本輪補寫 20260823-0130-review.md 與 comment 同文

健康:`origin/main`(`e0e58b2`)是 HEAD 祖先。
上一輪 CI 兩條綠(REPO_REFERENCE job 97062185420、EXTERNAL_RUNTIME job 97062185122)。
本輪不開健康刀。不要為本機缺 `python` 開刀。

## 舊實作下先紅(TDD,改產品前)

`scripts/test-devtalk-graph.sh` 加上 0130 四顆牙之後:**17/21**,4 案未依預期
(舊檢查把「N1 有 session 再 start」與「沒游標就放行」判綠):

1. `0130-P0 游標在 N1-start 且已有 session → talk start 必須紅` rc=0 want=1
   (`allow	N1-start 允許 talk_start`)
2. `0130-P0 沒游標檔但已有 session → talk start 必須紅` rc=2 want=1
   (`error	action JSON 缺 cursor.node 或 action`)
3. `0130-P0 N1 缺 talk_start_if_session 必須紅` rc=0 want=1
   (live 檢查當時還不要求 N1 這條 forbid)
4. `0130-P0 prebash 沒游標+OPEN session talk start` start_rc=0 rc=0 want=2
   (`talk start` 已建立 `ses_01M0N9AMYGAXAVSTZ9ATP4A8P1`,hook 仍放行)

當時 live `check-devtalk-graph.sh` 仍綠(牙齒還沒長)。

## 採納並修好的 finding

### 0130-P0 假綠:N1 有 session 仍可 talk start
- 位置:`skills/dev-talk/graph.yaml` N1;`nodes/N1-start.md`;
  `scripts/check-devtalk-graph.sh` evaluate_action / live 檢查
- 我查到的根因:N1 完成條件把游標留在 N1-start。graph 只 allow talk_start,
  沒有 forbid talk_start_if_session。evaluate_action 只在 forbid 含該鍵
  時擋「有 session 再 start」。舊牙把游標寫到 N3 才紅。
- 舊實作下真的紅:上表第 1、3 項
- 修法:N1 加 forbid talk_start_if_session(與 N3 同款)。live 檢查缺它就紅。
  節點進條件寫明已有 session 不得再 talk start。不拆 0–11。
- 測試:`test-devtalk-graph.sh` **21/21**;n1-restart.json deny

### 0130-P0 假綠:沒游標檔 = --action 永不擋
- 位置:`hooks/_prebash_impl.py`;`scripts/check-devtalk-graph.sh --action`
- 我查到的根因:缺 `.devtalk-cursor.json` 就 return。repo 沒有游標 writer,
  預設 /dev-talk 走這條放行路。0030 接上的 hook 咬不到。
- 舊實作下真的紅:上表第 2、4 項
- 修法:選「讀 session 狀態」。不改 `.dev-flow` 儲存、不改 SQLite schema。
  沒游標且 action=talk_start 時跑既有 `dev-memory.py session list`;
  已有 OPEN understanding session → 合成無 node 的 --action payload,
  evaluate_action deny。session list 失敗(缺套件 / 未 setup)→ None,
  新場 talk start 仍放行。
- 測試:no-cursor-open-session.json deny;prebash + 隔離 `AGENTMEM_HOME`
  真的 `talk start` 再建第二下 start → exit 2。都在 21/21 裡

### 0130-P2 接續通道檔
- 寫了 `docs/dev/stage1-loop/20260823-0130-review.md`,與 PR comment 三條同文。

## 查證後不成立的 finding
無。0130 三條都成立,都修了。

## 刻意不做的 finding
- 不把 0–11 全拆完
- 不補游標 writer(選讀 session 狀態;本機游標仍不進 Git)
- 不改 `guides/guide-dev-talk.html` 步數(仍 12)
- 不改 `html-shell.html`、不開真實 dogfood 討論檔
- 不改 memory/ 程式、Gauntlet、G3、`_templates/7-review.md`、`docs/dev/stage7-loop/`
- 不為本機缺 python 開刀
- 不 merge、不推 main

## 本輪未處理(下一輪候選,依優先序)
1. 未拆步驟(0/1/2/4/5/6/8/9/10)何時抽成節點,本輪不裁
2. 本機游標仍無寫入實作(gitignore 有;N1 仍是散文「寫成 N1-start」)。
   沒游標時靠 OPEN session 擋第二下 start;agent 不寫游標時,
   N3/N9 的 --action 仍咬不到(本輪只修 talk start 這條洞)
3. 本環境沒有 `python` 指令,`check-methodology-corrections` 會紅;
   不是本輪產品洞

## 需要 owner 裁決
無新項。0130 三條照做。draft 還要不要合,是人審。

## 驗證結果
| 項 | 結果 |
|---|---|
| `scripts/test-devtalk-graph.sh`(改前) | **17/21**(四條 0130 該紅卻綠/錯碼) |
| `scripts/test-devtalk-graph.sh`(改後) | **21/21** |
| `scripts/check-devtalk-graph.sh` | **綠** |
| `scripts/check-devtalk-selfclean.sh` | 7 檔全過 |
| `scripts/check-devtalk-guide-sync.sh` | 12 原文 + 12 步 |
| `scripts/check-file-map.sh` | scanned=**137**(無新必列檔) |
| `memory` DevTalkSkillContractTest | **10/10** |
| `hooks/selftest.sh` | **402/402**(含 prebash / devtalk-guard) |
| `hooks/gate-consistency.sh` | **14/14** |
| `hooks/devflow-exec.sh doctor` | COMPATIBLE;gauntlet 1.3.3 |
| `scripts/check-gate-tokens.sh` | 全過 |
| `scripts/check-version-sync.sh` | 5 處 **1.3.3** |
| `scripts/check-readme-markers.sh` | 5/5 |
| `scripts/check-selfjudgment-tables.sh` | 36/36 |
| `scripts/check-spec-gate.sh`(範例+全形) | 各 5/5 |
| `scripts/check-regex-charclass.sh` | 全過 |
| `scripts/test-architecture-guards.sh` | **134/134**(對照 14/14、負向 120/120) |
| 平台 | 只證明 Linux。沒跑 macOS / Windows。**不要把上面的綠讀成 CI 已綠** |

## 給下一輪審查的輸入
- 最該被挑戰:沒游標時只擋 talk start。N9 寫程式 / N13 前寫知識仍要靠游標檔
  才進 --action。若下一刀要補 writer,牙齒應驗 SKILL/N1 有呼叫,且不要改
  `.dev-flow` 儲存。
- 我不打算改的方向:不一次拆完 0–11;不改 memory/ 儲存;不重開 Stage 2–7;不 merge
