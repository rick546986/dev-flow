# Claude 回應:STAGE2-REVIEW 第一刀已咬 → 第二刀
- 回應的 review 檔名: PR #25 owner comment 13:37Z「第一刀已咬」「可開第二刀」。無對應 `*-review.md`
- branch: claude/stage2-graph
- PR: https://github.com/rick546986/dev-flow/pull/25 (draft)
- 本輪類型: 第二刀
- 新增 commit: 52a67fe feat(flow): Stage 2 第二刀 — legacy 1/2/4/5/6 成真節點；本檔補列 SHA / PR
- 本檔 raw URL: https://raw.githubusercontent.com/rick546986/dev-flow/claude/stage2-graph/docs/dev/stage2-loop/20260823-2144-claude.md
- PR comment: 無(本輪是實作,不是審查)
- review 讀取: newest=PR #25 owner comment 13:37Z「已咬 / 可開第二刀」。以較新的 PR comment 為準

健康:`origin/main`(`c73cd76`,#23 已合)是 HEAD 祖先。
`git merge-tree --write-tree origin/main HEAD` exit 0。
#18 / #21 / #23 已 MERGED。不重做第 1 站。不 merge、不推 main。
PR #25 CI 雙綠(第一刀)。本輪不開健康刀。

## 舊實作下先紅(TDD,改產品前)

對第一刀形狀(四真節點 + `skill-legacy-1-2` / `4-6`)先改檢查器契約再跑
`scripts/check-devstage2-graph.sh`:

- exit **1**
- failures=**14**
  1. 缺 `nodes/S1-approaches.md`
  2. 缺 `nodes/S2-stress.md`
  3. 缺 `nodes/S4-oc.md`
  4. 缺 `nodes/S5-adr.md`
  5. 缺 `nodes/S6-selfcheck.md`
  6. `skill-legacy-1-2` 仍是團塊
  7. `skill-legacy-4-6` 仍是團塊
  8. N1 next 仍是 `skill-legacy-1-2`,必須是 `S1-approaches`
  9–13. graph 缺 S1 / S2 / S4 / S5 / S6
  14. N3 next 仍是 `skill-legacy-4-6`,必須是 `S4-oc`

同批既有牙 `scripts/test-devstage2-graph.sh` 在產品未改時 **8/9**
(G-0 對照組綠不起來,舊 SKILL / 缺進條件 / 缺完成條件 / 第二份 md / 另存 /
未 approved 寫檔 / N3 可寫 / `--write-cursor` 仍過)。

## 採納並修好的 finding

### 第二刀:legacy 1／2／4／5／6 成真節點
- 位置:`skills/dev-flow/stage2/graph.yaml`;
  `nodes/S1-approaches.md` / `S2-stress.md` / `S4-oc.md` / `S5-adr.md` / `S6-selfcheck.md`;
  `scripts/check-devstage2-graph.sh`;`scripts/test-devstage2-graph.sh`
- 舊實作下真的紅:上表 14 項 + G-0 紅
- 修法:鏈是
  `N1-handoff → S1-approaches → S2-stress → N3-write-md → S4-oc → S5-adr → S6-selfcheck → N7-g1 → N8-end`。
  每個真節點「做什麼」呼叫 `--write-cursor <本節點 id>`。
  N7／N8／S1 缺行必須紅。`skill-legacy` 團塊必須紅。
  N1 已核准仍不得 `write_decision`(寫檔在 N3)。
  檔 `.devstage2-cursor.json` 不進 Git。乘客地圖仍 0–7。
  不改模板正文。不接 prebash。
- 測試:live `check-devstage2-graph.sh` 綠(`nodes_required=9`);
  `test-devstage2-graph.sh` **15/15**

### review 殘:N1 已核准仍禁寫 — 順手釘
- 位置:`check-devstage2-graph.sh --action`;牙「N1 已核准仍寫 2-decision 必須紅」
- 舊實作下真的紅:第一刀牙齒只測未 approved;已核准路徑沒有獨立牙
- 修法:對照組(已核准)再餵 N1 `write_decision` → deny。理由改成
  「N1-handoff 不得寫 2-decision(寫檔在 N3)」
- 測試:含在 15/15

## 查證後不成立的 finding
無。13:37 comment 叫開第二刀、不要重做第一刀。第一刀鏈與牙都還在,只把
legacy hop 換成真節點檔。

## 刻意不做的 finding
- 不接 `hooks/_prebash_impl.py`(第三刀)
- 不改 `guides/guide-dev-flow.html` 第 2 站開頭(第三刀)
- 不改 `_templates/2-decision.md` 正文(乘客清單仍是頂註 0–7)
- 不改 `check-devtalk-graph.sh` 那套
- 不改 memory/、Stage 7、Gauntlet、G3、`.dev-flow/` 儲存
- 不追加 HISTORY.md
- 不重做第 1 站、不重做第一刀
- 不自己開第三刀
- 不 merge、不推 main

## 本輪未處理(下一輪候選,依優先序)
1. 第三刀:prebash 編成 `write_decision` / 擋 `4-spec`;guide 第 2 站對上九節點鏈。
   等第二刀 review 說咬到
2. `--action` 還只活在檢查腳本(第三刀)
3. 本環境缺 `python` 指令時 renderer 會紅;不是本輪產品洞

## 需要 owner 裁決
無新項。第二刀切法已在種子與 13:37 review 裁定,照做。

## 驗證結果
| 項 | 結果 |
|---|---|
| `scripts/check-devstage2-graph.sh`(第一刀形狀) | **exit 1 / 14 項**(改前) |
| `scripts/check-devstage2-graph.sh`(新實作) | **綠**(`nodes_required=9`) |
| `scripts/test-devstage2-graph.sh`(改前) | **8/9**(G-0 紅) |
| `scripts/test-devstage2-graph.sh`(改後) | **15/15** |
| `scripts/check-devtalk-graph.sh` | **綠**(`nodes_required=13`;未改) |
| `scripts/test-devtalk-graph.sh` | **91/91**(未改) |
| `scripts/check-file-map.sh` | scanned=**141**(未增腳本) |
| `hooks/selftest.sh` | **402/402** |
| `hooks/gate-consistency.sh` | **14/14** |
| 平台 | 只證明 Linux。沒跑 macOS / Windows |

## 給下一輪審查的輸入
- 最該被挑戰:S4／S5／S6 在 N3 之後、graph 仍 forbid `write_decision`。
  第三刀若把「寫 2-decision.md」一律編成 `write_decision` 且游標不在 N3 → deny,
  這三步就只能讀、不能改同一檔。要嘛第三刀放寬 S4–S6,要嘛把清點／ADR／自檢
  的改檔看成退回 N3 覆寫。
- 我不打算改的方向:不接 prebash;不改模板正文;不改乘客 0–7;
  不改 `.dev-flow` 儲存;不重開 Stage 1 / 7;不 merge
