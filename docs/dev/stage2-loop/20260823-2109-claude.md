# Claude 回應:Stage 2 第一刀種子
- 回應的 review 檔名: 無(`docs/dev/stage2-loop/` 先前不存在;無 STAGE2-REVIEW)。本輪做第一刀。
- branch: claude/stage2-graph
- PR: https://github.com/rick546986/dev-flow/pull/25 (draft)
- 本輪類型: 種子 / 第一刀
- 新增 commit: 38d1e6c feat(flow): Stage 2 第一刀 — 四真節點 + skill-legacy；本檔補列 SHA / PR
- 本檔 raw URL: https://raw.githubusercontent.com/rick546986/dev-flow/claude/stage2-graph/docs/dev/stage2-loop/20260823-2109-claude.md
- PR comment: 無(本輪是實作,不是審查)
- review 讀取: newest=無, last=無;第一刀種子為準

健康:`origin/main`(`c73cd76`,#23 已合)是 HEAD 祖先。
#18 / #21 / #23 已 MERGED。不重做第 1 站。不 merge、不推 main。

## 舊實作下先紅(TDD,改產品前)

對 current main 形狀(單一 `skills/dev-flow/SKILL.md`、沒有 `stage2/graph.yaml` / `nodes/`)
先落地檢查腳本再跑 `scripts/check-devstage2-graph.sh`:

- exit **1**
- failures=**6**
  1. 缺 `skills/dev-flow/stage2/graph.yaml`
  2. 缺 `nodes/N1-handoff.md`
  3. 缺 `nodes/N3-write-md.md`
  4. 缺 `nodes/N7-g1.md`
  5. 缺 `nodes/N8-end.md`
  6. P0 沒有 N3 覆寫契約,無法證明重跑落檔不會另存

同批先落地的負向牙 `scripts/test-devstage2-graph.sh` 在產品未改時已是 **9/9**
(對照組綠;舊 SKILL fixture 紅;缺進條件 / 缺完成條件 / 第二份 md / 另存 /
N1 未 approved 寫 2-decision / N3 已核准可寫 / `--write-cursor` 真的寫檔)。

## 採納並修好的 finding

### 第一刀:四真節點 + 中間 skill-legacy
- 位置:`skills/dev-flow/stage2/graph.yaml`;
  `nodes/N1-handoff.md` / `N3-write-md.md` / `N7-g1.md` / `N8-end.md`;
  `scripts/check-devstage2-graph.sh`;`scripts/test-devstage2-graph.sh`
- 舊實作下真的紅:上表 6 項 + fixture 五顆牙
- 修法:鏈是 `N1-handoff → skill-legacy-1-2 → N3-write-md → skill-legacy-4-6 → N7-g1 → N8-end`。
  暫留步是真 hop,不是 `via` 字串(第 1 站 0030 假綠不重演)。
  N3 只覆寫 `docs/dev/<slug>/2-decision.md`(`write_mode: overwrite`)。
  `--action write_decision` 在 1-discussion 未 approved 時 deny。
  `--write-cursor` 寫 `.devstage2-cursor.json`(gitignore,不進 Git)。
  不接 prebash。
- 測試:live `check-devstage2-graph.sh` 綠(`nodes_required=4`);
  `test-devstage2-graph.sh` **9/9**

### SKILL 第 2 站只加一行指標
- 位置:`skills/dev-flow/SKILL.md` 階段表第 2 列
- 修法:加「節點鏈見 `skills/dev-flow/stage2/`」。gate 欄原文不動。
- 測試:`hooks/gate-consistency.sh` **14/14**

## 查證後不成立的 finding
無。沒有既有 review 檔可推翻。

## 刻意不做的 finding
- 不把 legacy 1／2／4／5／6 拆成真節點檔(第二刀)
- 不接 `hooks/_prebash_impl.py`(第三刀)
- 不改 `guides/guide-dev-flow.html` 第 2 站開頭(第三刀;本輪只加檔案地圖兩列)
- 不改 `_templates/2-decision.md` 正文(乘客清單仍是頂註 0–7)
- 不改 `check-devtalk-graph.sh` 那套
- 不改 memory/、Stage 7、Gauntlet、G3、`.dev-flow/` 儲存
- 不追加 HISTORY.md
- 不重做第 1 站
- 不 merge、不推 main

## 本輪未處理(下一輪候選,依優先序)
1. 第二刀:legacy 1／2／4／5／6 成真節點。等第一刀合進這分支且 review 說咬到
2. 第三刀:prebash 編成 write_decision / 擋 4-spec;guide 第 2 站對上九節點鏈
3. 本環境缺 `python` 指令時 renderer 會紅;不是本輪產品洞

## 需要 owner 裁決
無新項。四真節點切法已在種子裁定,照做。

## 驗證結果
| 項 | 結果 |
|---|---|
| `scripts/check-devstage2-graph.sh`(舊實作) | **exit 1 / 6 項**(改前) |
| `scripts/check-devstage2-graph.sh`(新實作) | **綠**(`nodes_required=4`) |
| `scripts/test-devstage2-graph.sh` | **9/9** |
| `scripts/check-devtalk-graph.sh` | **綠**(`nodes_required=13`;未改) |
| `scripts/test-devtalk-graph.sh` | **91/91**(未改) |
| `scripts/check-file-map.sh` | scanned=**141** |
| `scripts/test-architecture-guards.sh` | **134/134**(對照 14/14、負向 120/120) |
| `hooks/selftest.sh` | **402/402** |
| `hooks/gate-consistency.sh` | **14/14** |
| 平台 | 只證明 Linux。沒跑 macOS / Windows |

## 給下一輪審查的輸入
- 最該被挑戰:第一刀把步 7 拆成 N7(送審)與 N8(三連動),模板頂註步 7 原文沒動。
  若乘客手冊要把「送審」與「三連動」看成一步,應改指標,不要改模板正文。
- 我不打算改的方向:不一次拆完 0–7;不接 prebash;不改 `.dev-flow` 儲存;
  不重開 Stage 1 / 7;不 merge
