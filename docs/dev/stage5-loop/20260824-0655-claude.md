# Stage 5 第三刀接續紀錄(2026-08-24 06:55 UTC)

依序判定 → 第 4 站三刀已合進 main(`8cd17d9`,PR #31);第 5 站在
`claude/stage5-graph`,owner 在 PR #36 於 06:34 寫 STAGE5-REVIEW「第二刀已咬,
指紋污染已修,**可開第三刀**」→ **同一分支開第三刀**。不開新分支、不開第 6 站、
不重做第 1／2／3／4 站。

## 本刀咬了什麼(列檔)

| 檔 | 改了什麼 |
|---|---|
| `scripts/check-devstage5-graph.sh` | 兩顆新牙:`check_action_runtime_wired()`(hooks 必須真的呼叫 `check-devstage5-graph.sh --action`,只寫在註解不算)、`check_guide()`(`<h3 id="stage5">` 開頭必須對上八節點鏈;出現「Stage 5 還在單一 SKILL」必須紅) |
| `scripts/test-devstage5-graph.sh` | 45 → **63** 案(下限 45 → **61**):guide 三案、hooks 三案、prebash 實跑十案、`devflow-exec.sh start` 釘樁兩案 |
| `hooks/_prebash_impl.py` | 新增 `_devstage5_graph_action()`,掛在 `load_state` 早退**之前**,排在第 4 站之後 |
| `guides/guide-dev-flow.html` | `#stage5` 正文開頭補節點鏈正本;附錄檔案地圖兩列補上 prebash 接線與 exec 釘樁 |

`skills/dev-flow/stage5/graph.yaml` 與八個節點檔**一個字沒動** —— 第二刀的鏈
owner 已經咬過,第三刀只接 runtime 與導覽。

## 先紅後綠的數字

先寫牙(`ff346a1`),產品還沒進來:

```
scripts/check-devstage5-graph.sh   → exit 1、2 項
  - P0 --action 沒接到 runtime:hooks 沒有呼叫 check-devstage5-graph.sh --action
  - P0 guide 第 5 站開頭缺節點:N1-handoff,S1-slice,…,N6-end
scripts/test-devstage5-graph.sh    → 59/63,4 案紅,全部紅在 prebash 未接
```

再寫產品(`335e1f6`)→ `failures=0`、**63/63**。

治具有一處要交代:`put_parser()` 會生出 `hooks/`,而新那顆「`--action` 必須接到
runtime」正是掃 `hooks/`,所以 parser 那三案本來會連帶紅在跟自己無關的牙上。
一個案例只准改壞**一件事**,因此 parser 治具順手補上一份已接線的
`_prebash_impl.py` stub。

## 編成的形狀(不是沙盒)

本機有 `.devstage5-cursor.json` 才編成。指令字面寫 `5-tasks.md`(`>`／`>>`／
`tee`／`open(`)→ `write_tasks`;寫 `6-implementation-notes.md` → `write_notes`。
裁決一律外包給 `check-devstage5-graph.sh --action`,prebash 自己不判。
**沒游標不攔截** —— 這是編成,不是沙盒。

四份游標各認自己的檔:第 4 站也編成 `5-tasks.md`,但它讀的是
`.devstage4-cursor.json`,兩邊不互相拆掉。第 1 站 `write_code`、第 2／3 站的
`write_spec` 編成一律零改動。prebash 實跑十案裡有兩案專門釘這件事(只有第 5 站
游標時,`4-spec.md` 與 `2-decision.md` 不被第 5 站編成)。

## `devflow-exec.sh start`:釘住,不重寫

派工單要的是「未 approved → `start` 必須擋;**現有 start 已擋就寫測試釘住,
不准重寫**」。`_exec_impl.py` 的 `spec_gate()` 本來就在 `head_sha()` 之前拒掉
未 approved 的 4-spec,所以本刀對那支 CLI 一個字都沒改,只補兩案在 tmp 樹實跑:

- 4-spec `status: draft` → rc=1、訊息含 `approved`
- 4-spec approved 但沒有 `5-tasks.md` → rc=1、訊息含 `5-tasks.md`

兩案都額外斷言**拒啟不得留下 `.devflow/`**。tmp 樹跑之前先確認它不在任何 git
repo 裡(`exec` 的 ROOT = git toplevel,失敗才退回 pwd)—— 否則會誤打正式樹。

## 全套回歸

```
check/test-devstage5-graph          ✅ PASS + 63/63
check/test-devtalk-graph            ✅(未改)
check/test-devstage2-graph          ✅(未改)
check/test-devstage3-graph          ✅(未改)
check/test-devstage4-graph          ✅(未改)
devflow-check.sh all                ✅ exit 0 —— 四組全 REPO_REFERENCE_PASS
  methodology 19 / contracts 5 / architecture 20 / render 2
hooks/selftest.sh                   ✅ 402/402
hooks/gate-consistency.sh           ✅ 14/14
```

`test-architecture-guards` 收尾指紋未變(第二刀那個 `.pyc` 污染仍是修好的)。
`check-py-floor` 這次是**真編譯模式**:補裝了 `markdown-it-py==4.0.0` 與一支真的
Python 3.9(`DEVFLOW_PY_FLOOR_BIN`),90 個 `.py` 在 3.9 下皆可解析。

## 刻意不做

- 不改 `skills/dev-flow/stage5/graph.yaml` 與八個節點檔、不改 `skills/dev-flow/SKILL.md`
  (第一刀那行指標已經在了)。
- 不改 `_templates/` 任何正文(乘客清單正本仍是 `_templates/5-tasks.md` 頂註 0–6)。
- 不改 `hooks/devflow-exec.sh`／`_exec_impl.py`、`hooks/contract_ref.py`、
  `tests/parallel-stage6/contract_ref.py` 的 `parse_5_tasks`、
  `scripts/check-task-slicing.sh`(不改成 exit 1)、
  `scripts/check-spec-gate.sh`、`scripts/build-gate-twin.py`、
  `docs/dev/tools/build-gate-twin.py`、`hooks/_stage3_impl.py` —— 一律只呼叫。
- 不重做第 1／2／3／4 站:那四支 `_devstage*_graph_action`／`_devtalk_graph_action`、
  各站 check + test、guide `#stage1`–`#stage4` 全部零改動。
- 不動 `memory/`、Gauntlet、G3 Evidence 契約、`.dev-flow/`、平行引擎、
  `_guard_impl.py`。圍欄①②③ 一個字沒動。
- 不追加 `HISTORY.md`、不 merge、不推 main、不開第 6／7 站。

## 等什麼

推 `claude/stage5-graph`,更新 draft PR #36。**停**,等半點 CI 與 STAGE5-REVIEW。
第 5 站三刀齊了,下一步是 owner／半點審 merge;合進 main 之後才開
`claude/stage6-graph` 第一刀。
