# Stage 5 第二刀接續紀錄(2026-08-24 03:57 UTC)

owner 在 PR #36 寫 STAGE5-REVIEW「第一刀已咬……**可開第二刀**」。本輪依序判定 →
第 4 站三刀已合進 main(`8cd17d9`,PR #31)、第 5 站第一刀在 `claude/stage5-graph`
但**還沒三刀齊** → 同一分支做**第二刀**,不開第三刀、不開第 6 站。

## 本刀咬了什麼(列檔)

新增:

| 檔 | 是什麼 |
|---|---|
| `skills/dev-flow/stage5/nodes/S1-slice.md` | 切 T:tracer bullet 排序,每個 T 答得出可觀測行為 |
| `skills/dev-flow/stage5/nodes/S2-fields.md` | 填欄:Covers／Files／Verify + Intent／Boundaries,續行不踩保留欄名 |
| `skills/dev-flow/stage5/nodes/S3-deps.md` | 依賴:`Blocked-by` 拓撲序,parallel 選配欄位照既有契約 |
| `skills/dev-flow/stage5/nodes/S4-selfcheck.md` | 自檢:呼叫既有 `parse_5_tasks` 判四必填欄 |
| `scripts/fixtures/devstage5-graph/good/…/nodes/S1-slice.md` 等四份 | 治具的同名節點檔 |
| `scripts/fixtures/devstage5-graph/actions/s1｜s2｜s4-write-tasks-ok.json` | S 節點 + 4-spec approved 的 allow 案 |
| `scripts/fixtures/devstage5-graph/actions/s1-write-tasks-pending.json` | S 節點未過 G2 的 deny 案 |
| `scripts/fixtures/devstage5-graph/actions/s3-write-notes.json` | S 節點 `write_notes` 的 deny 案 |
| `scripts/fixtures/devstage5-graph/actions/legacy-gone.json` | 第一刀的 `skill-legacy-1-3` 節點名(鏈裡沒了 → exit 2) |

改動:

| 檔 | 改了什麼 |
|---|---|
| `scripts/check-devstage5-graph.sh` | 契約翻第二刀:八真節點鏈、`kind: skill-legacy` 必須紅、`TASKS_ALLOWED_NODES` 五節點逐一驗 write／overwrite／模擬重跑、四必填欄探現有 parser、S4 必須點名 `parse_5_tasks` |
| `scripts/test-devstage5-graph.sh` | 27 → **44** 案,下限 8 → **44** |
| `skills/dev-flow/stage5/graph.yaml` | 鏈改成八真節點,兩個 `skill-legacy` 團塊拆掉 |
| `skills/dev-flow/stage5/nodes/N1-handoff.md` | 下一跳 `skill-legacy-1-3` → `S1-slice` |
| `skills/dev-flow/stage5/nodes/N4-write-md.md` | 下一跳 `skill-legacy-4` → `S4-selfcheck` |
| `skills/dev-flow/stage5/nodes/N5-twin.md` | 進條件改成「`S4-selfcheck` 完成:`parse_5_tasks` 的 `errors` 為空」 |
| `guides/guide-dev-flow.html` | 只有附錄「檔案地圖」那兩列的描述對上第二刀 |

鏈(`graph.yaml` 正本):

```
N1-handoff → S1-slice → S2-fields → S3-deps → N4-write-md → S4-selfcheck
→ N5-twin → N6-end
```

`S1-slice`／`S2-fields`／`S3-deps`／`S4-selfcheck` 與 `N4-write-md` 共寫同一份
`docs/dev/<slug>/5-tasks.md`(`write_mode: overwrite`、`allow: write_tasks`);
`N1-handoff`／`N5-twin`／`N6-end` 一律沒放寬。`write_notes` 全鏈仍沒有 allow ——
5-tasks 未定案不准開第 6 站。每個真節點的「做什麼」最後一行都
`--write-cursor <自己的 id>`。

## 先紅後綠的數字

先改檢查器契約(`5e0ff23`),產品還是第一刀的樹 —— 跑
`scripts/check-devstage5-graph.sh`:

```
[graph] nodes_required=8 failures=13
❌ FAIL:13 項 → exit 1
```

13 項:缺 `S1-slice`／`S2-fields`／`S3-deps`／`S4-selfcheck` 四個真節點檔(4)、
`skill-legacy-1-3` 與 `skill-legacy-4` 仍是團塊(2)、鏈外節點(1)、
`graph.yaml` 缺那四個節點(4)、`N1-handoff` 與 `N4-write-md` 的 `next` 對不上(2)。

再寫產品(`fba164f`)→ `failures=0`,exit 0。

負向牙 `scripts/test-devstage5-graph.sh` = **44/44**(下限寫死 `< 44 → exit 2`)。
第二刀新增的槍:`kind: skill-legacy` 回頭必須紅、把 1／2 收回團塊必須紅、
`S1-slice`／`S3-deps`／`S4-selfcheck`／`N5-twin` 各缺 `--write-cursor` 一槍、
缺 `S3-deps.md` 一槍、`S4-selfcheck` 沒點名 `parse_5_tasks` 一槍、
`N5-twin` `allow: write_tasks` 一槍、`S2-fields` 缺 `allow` 一槍、
S 節點的 allow／未過 G2／`write_notes` 各一槍、第一刀節點名跑 `--action` → exit 2。

四必填欄那顆牙不是散文:治具裡放兩份 `hooks/devflow-lib.py` stub,
**放水版**(缺欄不報)必須紅、**忠實版**(缺欄報錯)必須綠 —— 證明檢查器是在讀
現有 parser 的輸出,不是自己另寫一套解析。live 樹上它同時探
`hooks/devflow-lib.py` 與 `tests/parallel-stage6/contract_ref.py`,兩支都零改動。

全套回歸(第 1／2／3／4 站零改動,仍綠):

```
test/check-devstage5-graph          ✅ 44/44 + PASS
test/check-devtalk-graph            ✅(未改)
test/check-devstage2-graph          ✅ 29/29(未改)
test/check-devstage3-graph          ✅ 30/30(未改)
test/check-devstage4-graph          ✅ 60/60(未改)
devflow-check.sh methodology        ✅ REPO_REFERENCE_PASS(19 組全過)
devflow-check.sh architecture       check-file-map(157 列 / forward 147 / reverse 149)
                                    與 test-architecture-guards 皆 ✅
hooks/selftest.sh                   ✅ 402/402
hooks/gate-consistency.sh           ✅ 14/14
```

`architecture/check-py-floor` 在這台 exit 2 = **直譯器缺席**(沒有 3.9–3.11),
與本刀無關;`check-methodology-corrections` 的 renderer 定點需要
`scripts/requirements-methodology-render.txt` 釘的 `markdown-it-py==4.0.0`,
本機補裝後全綠(repo 零改動)。

## 下一刀是第三刀

- `hooks/_prebash_impl.py` 在 `load_state` 早退之前接 `_devstage5_graph_action`:
  本機有 `.devstage5-cursor.json` 才編成,指令字面寫 `5-tasks.md`
  (`>`／`>>`／`tee`／`open(`)→ `write_tasks`;沒游標不攔截(不是沙盒)。
- 未 approved 時 `devflow-exec.sh start`／寫 `6-notes` 必須擋 —— 現有 `start`
  已經擋就寫測試釘住,不重寫那支工具。
- guide `#stage5` 正文節點鏈對上本刀這條八節點鏈;五份游標不要互相拆掉。
- 不重做 `write_code`、不改第 2／3／4 站的編成。

## 刻意不做

- **不接 prebash**:`hooks/_prebash_impl.py` 零改動,`--action` 仍只活在檢查腳本。
- **不改 guide `#stage5` 正文節點鏈**:只動附錄檔案地圖兩列(第三刀才改正文)。
- **不開第三刀、不開第 6／7 站**:第 5 站三刀未合進 main 之前不開
  `claude/stage6-graph`。
- 不改 `_templates/` 任何正文 —— 乘客清單正本是 `_templates/5-tasks.md` 頂註 0–6,
  四個新節點檔都只指過去、不重抄步原文。
- 不改 `parse_5_tasks`(`hooks/devflow-lib.py`／`tests/parallel-stage6/contract_ref.py`)、
  不把 `scripts/check-task-slicing.sh` 改成 exit 1、不改 `scripts/check-spec-gate.sh`、
  `scripts/build-gate-twin.py`、`docs/dev/tools/build-gate-twin.py`、
  `hooks/_stage3_impl.py` —— 一律只呼叫。
- 不重做第 1／2／3／4 站:`skills/dev-talk/`、`skills/dev-flow/stage2/`、`stage3/`、
  `stage4/`、那幾支 check + test、`_devtalk_graph_action`／`_devstage2_graph_action`／
  `_devstage3_graph_action`／`_devstage4_graph_action`、guide `#stage1`–`#stage4`
  全部零改動。
- 不動 `memory/`、Gauntlet、G3 Evidence 契約、`devflow-exec.sh`、`_guard_impl.py`、
  `.dev-flow/`、平行引擎。圍欄①②③ 一個字沒動。
- 不追加 `HISTORY.md`、不 merge、不推 main。
- 沒有 `via` 字串當 hop;第一刀的 `kind: skill-legacy` 暫留 hop 到本刀已經拆成
  真節點檔,契約也翻成「仍是 skill-legacy 必須紅」。

## 等什麼

推 `claude/stage5-graph`,更新 draft PR #36。**停**,等 STAGE5-REVIEW
「已咬／可開第三刀」。不要一次開完三刀。
