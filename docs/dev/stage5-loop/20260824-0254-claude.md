# Stage 5 第一刀接續紀錄(2026-08-24 02:54 UTC)

第 4 站三刀(`1c4c258` / `145b196` / `bc1b05c`)已合進 main(`8cd17d9`,PR #31),
owner 在 #31 寫「可開下一站第一刀」。本輪依序判定 → 開 `claude/stage5-graph`,
只做第 5 站**第一刀**。

## 本刀咬了什麼(列檔)

新增:

| 檔 | 是什麼 |
|---|---|
| `scripts/check-devstage5-graph.sh` | Stage 5 節點鏈的機械契約(live check / `--action` / `--write-cursor`) |
| `scripts/test-devstage5-graph.sh` | 上面那支的負向牙,27 案 |
| `scripts/fixtures/devstage5-graph/good/` | 最小合法樹(四真節點 + 兩暫留 hop + 兩個 slug) |
| `scripts/fixtures/devstage5-graph/old-skill/` | 舊實作:有 SKILL、沒有 `stage5/graph.yaml` |
| `scripts/fixtures/devstage5-graph/actions/` | 9 份 `--action` JSON |
| `skills/dev-flow/stage5/graph.yaml` | 下一跳唯一正本 |
| `skills/dev-flow/stage5/nodes/N1-handoff.md` | 接手盤點(進條件 = 4-spec approved,G2 過) |
| `skills/dev-flow/stage5/nodes/N4-write-md.md` | 定稿落檔(唯一寫檔節點,`allow: write_tasks`) |
| `skills/dev-flow/stage5/nodes/N5-twin.md` | 產執行板(只呼叫現有 `build-gate-twin.py`) |
| `skills/dev-flow/stage5/nodes/N6-end.md` | owner 自定案 approved(第 5 站無 G) |

改動(只有註冊與一行指標):

| 檔 | 改了什麼 |
|---|---|
| `scripts/devflow-check.sh` | `group_methodology` 在 stage4 兩行後面加兩行(會執行的行,不是註解) |
| `guides/guide-dev-flow.html` | 附錄「檔案地圖」補兩列(`check-file-map.sh` 雙向盤點) |
| `scripts/check-file-map.sh` | `EXPECTED_MAPPED_FILES` 145 → 147 |
| `scripts/test-architecture-guards.sh` | 靜態互釘同步 145 → 147 |
| `.gitignore` | 加 `.devstage5-cursor.json` |
| `skills/dev-flow/SKILL.md` | 第 5 站那列只加「節點鏈見 `skills/dev-flow/stage5/`」 |

鏈(`graph.yaml` 正本):

```
N1-handoff → skill-legacy-1-3 → N4-write-md → skill-legacy-4 → N5-twin → N6-end
```

暫留步是 `kind: skill-legacy` 真節點,`entry: _templates/5-tasks.md` + `steps`,
**沒有 via**。單一產物 `docs/dev/<slug>/5-tasks.md`,`write_mode: overwrite`。
只有 `N4-write-md` `allow: write_tasks`;`write_notes` 全鏈都沒 allow。

## 先紅後綠的數字

先寫牙(`4007cc6`),產品還沒進來 —— 對舊實作(main 的樹,沒有
`skills/dev-flow/stage5/graph.yaml`)跑 `scripts/check-devstage5-graph.sh`:

```
[graph] nodes_required=4 failures=10
❌ FAIL:10 項 → exit 1
```

10 項涵蓋七項必紅:①沒有 `stage5/graph.yaml` ②四個真節點缺「進條件」/「完成條件」
③同 slug 第二份 `5-tasks*.md` ④`write_mode≠overwrite` ⑤缺 `N4-write-md`
⑥未過 G2(4-spec 不是 approved)卻 `write_tasks` ⑦5-tasks 未定案卻 `write_notes`。

再寫產品(`cd2799d`)→ `failures=0`,exit 0。

負向牙 `scripts/test-devstage5-graph.sh` = **27/27**(下限寫死 `< 8 → exit 2`)。
含 `G-0` 對照組綠、`G-old` 舊實作紅(輸出含「沒有 graph」)、
`G-cursor --write-cursor N1-handoff` 寫出 `.devstage5-cursor.json` 且
`node=N1-handoff`、`G-action N4-write-md + 4-spec approved → write_tasks` 綠、
`N1`／`N5-twin`／`N6-end`／`skill-legacy-1-3` 做 `write_tasks` 各一槍紅、
未過 G2 紅、`write_notes` 兩槍紅、鏈外節點 exit 2、舊實作跑 `--action` exit 2。

全套回歸(第 1／2／3／4 站零改動,仍綠):

```
test/check-devstage5-graph          ✅
test/check-devtalk-graph            ✅(未改)
test/check-devstage2-graph          ✅(未改)
test/check-devstage3-graph          ✅(未改)
test/check-devstage4-graph          ✅(未改)
devflow-check.sh methodology        ✅ REPO_REFERENCE_PASS(19 組全過)
devflow-check.sh architecture       check-file-map / test-architecture-guards 皆 ✅
hooks/selftest.sh                   ✅ 402/402
hooks/gate-consistency.sh           ✅ 14/14
```

`architecture/check-py-floor` 在本機 exit 2 = **直譯器缺席**(這台沒有 3.9–3.11),
`devflow-check.sh` 本來就把它記紅、組內繼續,與本刀無關。

## 下一刀是第二刀

乘客步 1／2／3／4 變真節點,每個 `--write-cursor`,`kind: skill-legacy` 翻成
必須紅。鏈預計:

```
N1-handoff → S1-slice → S2-fields → S3-deps → N4-write-md → S4-selfcheck
→ N5-twin → N6-end
```

S1–S4 與 N4 共覆寫同一份 `5-tasks.md`(`allow: write_tasks`),不放寬
`N1`／`N5-twin`／`N6-end`。四必填欄(Covers／Files／Verify／Blocked-by)缺一
必須紅 —— 呼叫**現有** `hooks/contract_ref.py` 的 parser,不改它。
`scripts/check-task-slicing.sh` 仍是 warning-only,不改成 exit 1。

第三刀:`hooks/_prebash_impl.py` 在 `load_state` 早退之前接
`_devstage5_graph_action`(本機有 `.devstage5-cursor.json` 才編成;沒游標不攔截)、
未 approved 時 `devflow-exec.sh start`／寫 6-notes 必須擋(現有 `start` 已擋就
寫測試釘住,不重寫)、guide `#stage5` 正文對上第二刀那條鏈。

## 刻意不做

- **不接 prebash**:`hooks/_prebash_impl.py` 零改動,`--action` 本刀只活在檢查腳本。
- **不改 guide `#stage5` 正文節點鏈**:只補附錄檔案地圖兩列(第三刀才改正文)。
- **不開第 6／7 站**:第 5 站三刀未合進 main 之前不開 `claude/stage6-graph`。
- 不改 `_templates/` 任何正文 —— 乘客清單正本是 `_templates/5-tasks.md` 頂註 0–6,
  四個節點檔都只指過去、不重抄步原文。
- 不改 `hooks/contract_ref.py`、不把 `scripts/check-task-slicing.sh` 改成 exit 1、
  不改 `scripts/check-spec-gate.sh`、`scripts/build-gate-twin.py`、
  `docs/dev/tools/build-gate-twin.py`、`hooks/_stage3_impl.py` —— 一律只呼叫。
- 不重做第 1／2／3／4 站:`skills/dev-talk/`、`skills/dev-flow/stage2/`、
  `stage3/`、`stage4/`、五支 check + 四支 test、`_devtalk_graph_action`／
  `_devstage2_graph_action`／`_devstage3_graph_action`／`_devstage4_graph_action`、
  guide `#stage1`–`#stage4` 全部零改動。
- 不動 `memory/`、Gauntlet、G3 Evidence 契約、`devflow-exec.sh`、`_guard_impl.py`、
  `.dev-flow/`、平行引擎。圍欄①②③ 一個字沒動。
- 不追加 `HISTORY.md`、不 merge、不推 main。
- 不用 `via` 字串當 hop(第 1 站 0030 假綠的來源);暫留步是 `kind: skill-legacy`
  真節點。

## 等什麼

推 `claude/stage5-graph`,開／更新 draft PR。**停**,等 STAGE5-REVIEW
「已咬／可開第二刀」。不要一次開完三刀。
