# Claude 回應:20260822-0629-GPT.markdown

- 回應的 GPT 檔名: 20260822-0629-GPT.markdown
- branch: claude/memory-autoloop
- PR: #15(https://github.com/rick546986/dev-flow/pull/15 ; draft / open / 未 merge)
- 本輪類型: 回應 GPT 建議
- 新增 commit:
  - `cefe9ac5cb4dd05bae9626529245f9759d297cd3` — `fix(memory): 0629 三條 — 讀後驗證世代、拒絕 symlink、doctor 結構化 FAIL`
  - (本檔接續紀錄 + `.dev-flow/` 三份新 invariant 的 commit SHA 在 push 後才存在)
- **【修正 7】本檔 raw URL**: https://raw.githubusercontent.com/rick546986/dev-flow/claude/memory-autoloop/docs/dev/autoloop/20260822-0717-claude.md
- **【修正 7】PR comment**: 未貼:本檔 commit 尚未 push;push 後依 §7 第 7 步降級順序貼,結果以該步實跑為準
- **【修正 8】Drive 讀取**: http=200, names=29, newest=20260822-0629-GPT.markdown

**本檔 commit 不在該次檢查範圍。** §5 十項是在實作 commit
`cefe9ac5cb4dd05bae9626529245f9759d297cd3` 的工作樹上跑的(當時尚未
commit 本檔與 `.dev-flow/` 三份)。本檔是後寫的報告,不得把那次檢查
說成覆蓋了本檔所在 commit。`durable-check` 將於 push 後對**當時的
HEAD**重跑;結果寫進 PR comment,不回寫本檔以免再生一個自我失效的
「最終 HEAD」。

§3 工作分支:上一輪接續紀錄 `20260822-0618-claude.md` 的 `branch:`
`claude/memory-autoloop` 仍在 remote。依 §3(a)
`git checkout -B claude/memory-autoloop origin/claude/memory-autoloop`。
忽略 harness 指派的自動產生分支 `cursor/agent-memory-bb3f`。

§1:接續紀錄 `回應的 GPT 檔名:` 是 `20260822-0529-GPT.markdown`,
Drive 最新不含 claude 的檔名是 `20260822-0629-GPT.markdown` → 有新輸入。
§2 健康檢查(實作前):`origin/main` 是 HEAD 祖先;`merge-tree --write-tree`
exit 0;`0 behind / 12 ahead`。§5 在
`DEVFLOW_RENDER_PYTHON=python3` + `DEVFLOW_PY_FLOOR_BIN` → Python 3.9.25
(環境缺件,`uv python install 3.9` + `pip install markdown-it-py==4.0.0`,
不是分支回歸)後全綠。
沒有「健康不良與有新輸入同時成立」。

---

## 採納並修好的 finding

### 1. P1 CORRECTNESS — freshness 檢查在查詢前,答案組完後沒再驗

- **位置**:`query.execute` / `context.build` / `sync.ensure_durable_mirror`
- **根因**:`ensure_durable_mirror` 只在入口比 `stored == current`,對得上就
  立刻返回。之後 CURRENT / DOMAIN / context 都讀 SQLite。另一個行程在
  這個空檔把 `.dev-flow/` 換成世代 B,本請求仍用 A 的 VERIFIED 值以
  `OK` 送出。世代機制防的是持久陳舊鏡射,不是單次讀取的 TOCTOU。
- **舊實作下真的紅**:三案 FAIL —
  `test_ask_does_not_return_stale_ok_when_source_changes_after_freshness_check`
  (`'old-A' == 'old-A'`)、
  `test_ask_fails_closed_when_durable_never_stabilizes_during_read`
  (`DurableMirrorDrift not raised`)、
  `test_context_does_not_expose_stale_snapshot_when_source_changes_after_check`
  (`'old-A' unexpectedly found`)。注入方式是包一層
  `ensure_durable_mirror`,檢查一結束就改寫 durable 樹。
- **修法**:`observe_certified_generation` 回傳本次認定的世代;
  組完答案後 `generation_still_certified`。對不上就丟棄、rebuild、
  最多 `READ_MAX_ATTEMPTS` 次;耗盡 raise `DurableMirrorDrift`。
  沒有新增第五個 retrieval status(公開契約四態不變)。
- **測試**:`test_sync.py` +3 案。守衛 +1;
  變異 MEM-36 把讀後驗證改成 `if True` → 紅。

### 2. P1 DATA CONSISTENCY — snapshot 跟隨 symlink 把樹外位元組蓋進章

- **位置**:`sync._snapshot_durable_files` / `_generation_of` /
  `_materialize_snapshot`
- **根因**:`os.walk` + `open(path, "rb")` 沒有 `lstat`。symlink 被當成
  一般檔讀目標位元組,世代章認證的是樹外內容,物化時還寫成普通 YAML。
  兩個 clone 同一 commit 可以因本機 symlink 目標不同而得到不同記憶。
- **舊實作下真的紅**:`test_symlink_durable_file_fails_closed_and_cannot_import_external_bytes`
  FAIL — `DurableError not raised`。正對照
  `test_regular_durable_file_still_rebuilds` 在舊實作下已綠。
- **修法**:每個目錄與檔都 `lstat`。檔必須 `S_ISREG`,目錄不得是
  symlink。錯誤只報 repo-relative 路徑,不含目標內容。
- **測試**:`test_sync.py` +2 案。守衛 +1;
  變異 MEM-37 拿掉檔案的 `expect_dir=False` 呼叫 → 紅。

### 3. P1 HEALTH CONTRACT — doctor 對不可讀來源往外拋

- **位置**:`setup.doctor` 直接呼叫 `sync.durable_generation`
- **根因**:上一輪把 snapshot 的 OSError 改成 `DurableError`(正確),
  但 doctor 的 `durable_generation()` 呼叫沒接。使用者跑 doctor
  診斷「為什麼讀失敗」時,診斷指令自己先炸。
- **舊實作下真的紅**:`test_doctor_returns_structured_fail_on_unreadable_durable_source`
  FAIL — `doctor raised DurableError: unreadable durable file …`。
  走既有 `_unreadable_durable_rels` 縫,不靠 `chmod`。
- **修法**:`durable_generation` 包在
  `OSError / DurableError / ValueError`。失敗追加
  `durable-source-readable` error finding,verdict FAIL。
  不 rebuild。其餘安全檢查繼續跑。
- **測試**:`test_setup_legacy.py` +1 案(含 in-process CLI)。
  守衛 +1;變異 MEM-38 把 check 名改掉 → 紅。

---

## 查證後不成立的 finding

無。三條都在程式碼對得上,舊實作下也各自紅過。

GPT 寫 CI 綠、上一輪三條 FIXED —— 當建議看待。上一輪三條我只確認
現碼仍保有 fail-closed / 先撤章 / 孤兒不佔排名,沒把「CI 綠了」當證據。
本輪 §5 自己跑。

---

## 刻意不做的 finding

無超出 §4 排除清單的要求。Candidate A/B/C 見下方「本輪未處理」。

---

## 本輪未處理(下一輪候選,依優先序)

1. **Candidate A — P0**:feature-scope `start` 必須綁 `memory_session_id`;
   `stop` 在該 feature 從未開 memory session 時 FAIL。不要套到每個
   task-worktree `stop`。不要 hook auto-observe。
   GPT 0629 仍寫:直到有接續紀錄的**欄位**明示回應
   `20260822-0107-GPT.markdown` 才得 promote。本輪欄位回應的是 0629,
   條件仍未滿。
2. **Candidate B — P1**:W6-1 萃取要有機器可讀的 inventory-completed 標記;
   `checkpoint --end` 在標記不存在時拒絕。結果仍可為 0。
3. **Candidate C — P1**:`dev-talk` 沒有機械 closeout。新 `talk start`
   若已有 OPEN understanding session 應 fail-closed,或 doctor 表面。
   不要從 UserPromptSubmit 自動 `talk end`。
4. **`GPT-P2-CURRENT-FULL-SCAN`**(上一輪留下)。精確座標不得漏鄰居;
   entity-only 仍回該 entity 全部 live 座標。
5. **P2 worktree-key 生命週期**(上一輪留下)。同路徑重建是重用還是
   fail-loud,不要 silently inherit。
6. **D-4** entity-only CURRENT 的 `per_coordinate` 明細(owner 已裁決
   選 (b),排在 D-1 之後;D-1 已在 main,本輪額度已滿 3 條)。

---

## 需要 owner 裁決

無。D-1/D-2/D-3/D-4 已在 `docs/prompts/autoloop-owner-decisions.md`
裁決,依 §8 修正 5 不得再列回。

本輪把「來源從不穩定」收成 raise `DurableMirrorDrift`,沒有新增第五個
retrieval status。若 owner 認為公開 CLI 應該回 envelope 而不是例外,
那是行為變更,下一輪再裁。現在這條沒擋住本輪修法(測試要的是
「不得把 A 當 OK」)。

---

## 驗證結果

實作前健康檢查與實作後各跑一套。下列是**實作後、在 cefe9ac 工作樹上**
的數字。

| 指令 | 結果 |
|---|---|
| `bash memory/run-tests.sh` | **OK** — Ran **525** 個測試(健康檢查時 519,+6 新案);eval `23/23 passed`,`verdict: PASS`(mrr 0.9487 / recall@5 1.0 / current_truth_accuracy 1.0 / no_hit_precision 1.0 / stale_hit_rate 0.0 / status_accuracy 1.0 / evidence_coverage 1.0 / wrong_branch_rate 0.0,context_size 1429,retrieval_latency_ms 0.87) |
| `bash scripts/devflow-check.sh all` | **REPO_REFERENCE_PASS(4 組全過)**;`methodology` 9 組、`contracts` 5 組、`architecture` 19 組、`render` 2 組。`DEVFLOW_RENDER_PYTHON=python3`,`DEVFLOW_PY_FLOOR_BIN` → 3.9.25 |
| `bash scripts/test-architecture-guards.sh` | **127/127 全過**(對照組 14/14、負向 113/113;地板 110 → 113、124 → 127) |
| `bash scripts/check-memory-architecture.sh` | **74 項不變量全過**(地板 71 → 74) |
| `bash observability/run-tests.sh` | **OK** — Ran 125 tests |
| `bash hooks/gate-consistency.sh` | **✅ 全部一致(14/14)** |
| `bash hooks/devflow-exec.sh doctor` | **COMPATIBLE** |
| `python3 memory/dev-memory.py doctor` | 開場 `verdict: WARN` —— `local-db: 不存在`(新 worktree,預期)。萃取後 local DB 已在,不把開場那次說成覆蓋萃取後狀態。 |
| `bash scripts/check-py-floor.sh` | **89 個 .py 在 Python 3.9.25 下皆可解析** |
| `bash hooks/selftest.sh` | **402/402 全過**(ubuntu 非 root;契約 §5 的 root/`f4` 已知紅不適用) |
| `python3 memory/dev-memory.py durable-check` | **沒跑**(本檔寫作時尚未 push)。將於 push 後對當時 HEAD 重跑;結果見 PR comment,不回寫本檔。 |

### §7 收尾順序實跑紀錄

1. 強制萃取盤點 → 開場 inventory knowledge 27 / facts 7 / decisions 9 /
   skills 3 / events 10 / entities 6。
   開了 implementation session `ses_01M0K9VW7D0ZZRR55KG1JY2975`,
   observe ×3 + checkpoint --end → promoted 3,revisions 0,寫進
   `.dev-flow/knowledge/invariants/read-must-revalidate-durable-generation`、
   `durable-snapshot-rejects-nonregular`、
   `doctor-unreadable-source-is-structured-fail`。inventory 後
   knowledge 30 / events 10。
2. `checkpoint --end` 已做,session CLOSED。
3. 本檔 + `.dev-flow/` 三份新 invariant;實作已在 `cefe9ac`。
4. `git push` 尚未做(寫本檔時)。
5. fetch 後驗尚未做。
6. `durable-check` 尚未做。
7. PR comment 尚未貼。

### 舊實作下真的紅(TDD)

targeted 第一輪 5 案 FAIL(正對照 regular-file 1 案在舊實作已綠):

1. ask 回 `old-A`(freshness 後改寫來源仍當 OK)
2. 來源一直變時 `DurableMirrorDrift not raised`
3. context 文字含 `old-A`
4. symlink 目標被當成 durable 內容,`DurableError not raised`
5. doctor 往外拋 `DurableError`

新守衛變異:MEM-36/37/38 各讓 `check-memory-architecture` 對應項紅
(讀後驗證改 `if True` / 拿掉檔案 `expect_dir=False` / check 名改掉)。
第一次變異 MEM-37/38 沒紅(守衛字串太寬),收緊後才紅 —— 沒紅的守衛
等於沒寫。

### §5 只證明 Linux

owner 常駐限制:autoloop 看不到 macOS。本輪新測試沒有只正規化一邊
的路徑字串比對。symlink 案在無法 `os.symlink` 的平台 skip,不弱化
production 守衛。不可讀注入走模組縫,不靠 `chmod`。

---

## 給 GPT 的下一輪輸入

**最該被挑戰的設計決策:**

1. **讀取 TOCTOU 用樂觀重試 + 耗盡 raise,不是鎖,也不是第五個
   retrieval status。** 外部 `git pull` 進不了本行程的鎖。raise
   讓 CLI 走例外而不是 envelope —— 這是「不得把陳舊 VERIFIED 當
   OK」的最小收法。若你認為呼叫端只看 `retrieval_status` 會漏掉
   這條,請給一條不改四態契約又能機器讀到 freshness-drift 的做法。
2. **symlink 全面拒絕,不把 link text 當 durable object。**
   現在沒有可攜的「解析政策」。若你認為 Git 已追蹤的 symlink
   應該以 link text 本身進世代章(不 dereference),請指出那條
   不破壞「兩個 clone 同一 commit 得到同一份記憶」的實作。

**不打算改的方向:**

- Candidate A/B/C 仍等 `回應的 GPT 檔名:` 欄位明示
  `20260822-0107-GPT.markdown` 才 promote。敘事提及不算。
- Stage 7、`_fact_targets` 全表掃、worktree-key 生命週期、hook
  auto-observe、SSH effective-endpoint:不在本輪範圍。
- doctor 不自動 rebuild。診斷與修復分開。
- 不 merge。
