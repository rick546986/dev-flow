# Claude 回應:20260822-0232-GPT.markdown

- 回應的 GPT 檔名: 20260822-0232-GPT.markdown
- branch: claude/memory-autoloop
- PR: #15(https://github.com/rick546986/dev-flow/pull/15 ; draft / open / 未 merge)
- 本輪類型: 回應 GPT 建議
- 新增 commit:
  - `462c2a000424fa2d58ff00408bcba9843f80715e` — `fix(memory): 0232 三條 — 缺世代不蓋章、鏡射快照一致、接續紀錄誠實`
  - (本檔接續紀錄 + `.dev-flow/` 三份 invariant 的 commit SHA 在 push 後才存在)
- **【修正 7】本檔 raw URL**: https://raw.githubusercontent.com/rick546986/dev-flow/claude/memory-autoloop/docs/dev/autoloop/20260822-0317-claude.md
- **【修正 7】PR comment**: 未貼:本檔 commit 尚未 push;push 後依 §7 第 7 步降級順序貼,結果以該步實跑為準
- **【修正 8】Drive 讀取**: http=200, names=25, newest=20260822-0232-GPT.markdown

**本檔 commit 不在該次檢查範圍。** §5 十項是在實作 commit
`462c2a000424fa2d58ff00408bcba9843f80715e` 上跑的。本檔是後寫的報告,
不得把那次檢查說成覆蓋了本檔所在 commit。`durable-check` 將於 push 後
對**當時的 HEAD**重跑;結果寫進 PR comment,不回寫本檔以免再生一個
自我失效的「最終 HEAD」。

§3 工作分支:上一輪接續紀錄 `20260822-0226-claude.md` 的 `branch:`
`claude/memory-autoloop` 仍在 remote。依 §3(a)
`git checkout -B claude/memory-autoloop origin/claude/memory-autoloop`。
忽略 harness 指派的 `cursor/agent-memory-986a`。

§2 健康檢查(實作前):`origin/main` 是 HEAD 祖先;`merge-tree --write-tree`
exit 0;`0 behind / 4 ahead`。§5 在
`DEVFLOW_RENDER_PYTHON=python3` + Python 3.9.25 後全綠。
沒有「健康不良與有新輸入同時成立」。

---

## 採納並修好的 finding

### 1. P0 CORRECTNESS — missing `durable_generation` blesses a stale pre-upgrade SQLite mirror

- **位置**:`memory/agentmem/sync.py` `ensure_durable_mirror()`;上一輪把
  「沒有世代紀錄 → 只蓋章不重建」寫成故意語意,也寫進
  `.dev-flow/knowledge/invariants/durable-mirror-generation`。
- **根因**:評測/單元測試把知識直接寫進 SQLite(durable=0),`.dev-flow/`
  常是空的;上一輪把「缺世代當 mismatch 去 rebuild」試過一次,496 測
  10 FAIL + 2 ERROR、eval 10/23,於是改成一律蓋章。那條逃生口把**升級
  路徑**也放進去了 —— 舊 runtime DB 已有 durable 鏡射列、沒有世代章,
  `git pull` 新樹後第一次 ask 會把*新*樹雜湊蓋在*舊*鏡射上,之後世代
  對得上卻繼續答 `old`。
- **舊實作下真的紅**:新案
  `test_absent_generation_on_old_mirror_does_not_bless_stale_value`
  **FAIL** —— `assertNotEqual(..., "old")` 得到 `'old' == 'old'`。
  同批 7 案合計 **2 FAIL + 4 ERROR + 1 OK**。
- **修法**:`store.has_durable_mirror()` 機械證明有沒有 durable=1 列。
  缺世代且已有鏡射 → `rebuild_local`;缺世代且沒有鏡射 → 才允許
  stamp-only。eval `_seed` 結束顯式蓋上當前樹世代,不再靠 production
  逃生口。既有 invariant 正文已改掉「沒蓋過章只蓋章」。
- **測試**:`test_sync.py` 新增 2 案(升級路徑紅→綠;空鏡射仍可蓋章且
  保留 local-only)。eval 23/23、`current_truth_accuracy` 1.0 未變。
  架構守衛新增「缺世代且已有鏡射列時必須 rebuild」;把
  `has_durable_mirror` 從 executable path 拿掉 → 該項紅。

### 2. P1 CORRECTNESS — mirror rebuild and generation stamp are not one snapshot

- **位置**:`rebuild_local` 讀完 durable 檔之後,再走一次
  `durable_generation()` 蓋章,中間沒有 before/after 比對。
- **根因**:讀與雜湊是兩次 filesystem walk。並發 `git pull` / 合法
  writer 可以在中間改 `.dev-flow/`,於是 SQLite 是 A、章是 gen(B)。
- **舊實作下真的紅**:
  `test_rebuild_does_not_certify_torn_generation` **FAIL** ——
  舊碼不呼叫 `_after_rebuild_read`,mutation 沒發生,`flipped["n"]`
  是 0;`test_rebuild_fails_closed_when_snapshot_never_stabilizes`
  **ERROR** —— 沒有 `DurableMirrorDrift`。
- **修法**:`generation_before` → `_rebuild_local_once` → hook →
  `generation_after`;相等才蓋章,否則最多重試 3 次,耗盡
  `DurableMirrorDrift`,不蓋章。
- **測試**:上述 2 案;架構守衛釘 `generation_before` /
  `generation_after` / `DurableMirrorDrift`。`MIN_CHECKS` 60 → 62。

### 3. P1 PROVENANCE — PR HEAD newer than the HEAD `durable-check` actually validated

- **位置**:上一份接續紀錄宣稱
  `head == remote_head == 0e35766…` + `durable-check -> PASS`,
  然後又 commit 了 `e6cca58`(只改同一份紀錄)。
- **根因**:把「最終 HEAD」寫進必須再 commit 一次才能更新的檔,檢查
  永遠覆蓋不到寫這句話的那個 commit。
- **舊實作下真的紅**:`ContinuationProvenanceTest` 3 案 **ERROR**
  —— 沒有 `continuation_claim_is_honest`。
- **修法**:checked HEAD ≠ 檔案所在 commit 時,必須出現
  `本檔 commit 不在該次檢查範圍`(或 `report-only successor`)。
  本檔採用這個模式:§5 記在 `462c2a0`;本檔不宣稱已被那次檢查覆蓋;
  `durable-check` 在 push 後重跑,結果只寫 PR comment。
  **沒有**削弱 `durable-check`。
- **測試**:`ContinuationProvenanceTest` 3 案。

---

## 查證後不成立的 finding

無。三條 ranked finding 都查到對應程式碼路徑。

---

## 刻意不做的 finding

0232 自己排除、且本輪未做:

- Stage 7 / `_fact_targets` 全表掃描 / worktree-key 生命週期 /
  hook auto-observe / `ssh -G`(§4 篩選第 3 條 + 0232 明文排除 +
  owner D-2 已關 SSH 端點證明)
- Candidate A/B/C(0232 寫:要等某份 Claude 紀錄的
  `回應的 GPT 檔名` **顯式寫** `20260822-0107-GPT.markdown` 才晉升。
  本輪回應的是 0232,該欄是 0232,條件仍未滿足)

---

## 本輪未處理(下一輪候選,依優先序)

1. **Candidate A — P0**:feature-scope `start` 必須綁 `memory_session_id`;
   `stop` 在該 feature 從未開 memory session 時 FAIL。不要套到每個
   task-worktree `stop`。不要 hook auto-observe。
2. **Candidate B — P1**:W6-1 萃取要有機器可讀的 inventory-completed 標記;
   `checkpoint --end` 在標記不存在時拒絕。結果仍可為 0。
3. **Candidate C — P1**:`dev-talk` 沒有機械 closeout。新 `talk start`
   若已有 OPEN understanding session 應 fail-closed,或 doctor 表面。
   不要從 UserPromptSubmit 自動 `talk end`。
4. **`GPT-P2-CURRENT-FULL-SCAN`**(上一輪留下)。精確座標不得漏鄰居;
   entity-only 仍回該 entity 全部 live 座標。
5. **P2 worktree-key 生命週期**(上一輪留下)。同路徑重建是重用還是
   fail-loud,不要 silently inherit。
6. **D-4** entity-only CURRENT 的 `per_coordinate` 明細(owner 已裁決
   選 (b),排在 D-1 之後;D-1 已在 main,本輪額度已滿 3 條)。

---

## 需要 owner 裁決

無。D-1/D-2/D-3/D-4 已在 `docs/prompts/autoloop-owner-decisions.md`
裁決,依 §8 修正 5 不得再列回。

---

## 驗證結果

實作前健康檢查與實作後各跑一套。下列是**實作後、在 462c2a0 上**的數字。

| 指令 | 結果 |
|---|---|
| `bash memory/run-tests.sh` | **OK** — Ran **503** 個測試(健康檢查時 496,+7 新案);eval `23/23 passed`,`verdict: PASS`(mrr 0.9487 / recall@5 1.0 / current_truth_accuracy 1.0 / no_hit_precision 1.0 / stale_hit_rate 0.0 / status_accuracy 1.0 / evidence_coverage 1.0 / wrong_branch_rate 0.0,context_size 1429,retrieval_latency_ms 0.865) |
| `bash scripts/devflow-check.sh all` | **REPO_REFERENCE_PASS(4 組全過)**;`methodology` 9 組(含 methodology correction 106/106)、`contracts` 5 組、`architecture` 19 組、`render` 2 組(6/6 tracked outputs byte-identical)。`DEVFLOW_RENDER_PYTHON=python3`,`DEVFLOW_PY_FLOOR_BIN` → 3.9.25 |
| `bash scripts/test-architecture-guards.sh` | **118/118 全過**(對照組 14/14、負向 104/104) |
| `bash scripts/check-memory-architecture.sh` | **62 項不變量全過**(地板 60 → 62) |
| `bash observability/run-tests.sh` | **OK** — Ran 125 tests |
| `bash hooks/gate-consistency.sh` | **✅ 全部一致(14/14)** |
| `bash hooks/devflow-exec.sh doctor` | **COMPATIBLE** |
| `python3 memory/dev-memory.py doctor` | `verdict: WARN` —— `local-db: 不存在`(開場,新 worktree,預期)。inventory 後 knowledge 21 / facts 7 / decisions 9 / skills 3 / events 7 / entities 6 |
| `bash scripts/check-py-floor.sh` | **89 個 .py 在 Python 3.9.25 下皆可解析** |
| `bash hooks/selftest.sh` | **402/402 全過**(ubuntu 非 root;契約 §5 的 root/`f4` 已知紅不適用) |
| `python3 memory/dev-memory.py durable-check` | **沒跑**(本檔寫作時尚未 push)。將於 push 後對當時 HEAD 重跑;結果見 PR comment,不回寫本檔。 |

### §7 收尾順序實跑紀錄

1. 強制萃取盤點 → 開場 inventory knowledge 19 / facts 7 / decisions 9 /
   skills 3 / events 7 / entities 6。既有
   `durable-mirror-generation` 正文仍寫「沒蓋過章只蓋章」(已錯)。
   開了 implementation session `ses_01M0JW3PRGFX5YEKXY9784RC62`,
   observe ×3 + checkpoint --end → promoted 3,寫進
   `.dev-flow/knowledge/invariants/durable-mirror-generation`(改正文)、
   `durable-mirror-snapshot-consistent`、
   `autoloop-report-commit-not-covered`。inventory 後 knowledge 21。
2. `checkpoint --end` 已做,session CLOSED。
3. 本檔 + `.dev-flow/` 三份 invariant;實作已在 `462c2a0`。
4. `git push` 尚未做(寫本檔時)。
5. fetch 後驗尚未做。
6. `durable-check` 尚未做。
7. PR comment 尚未貼。

### 舊實作下真的紅(TDD)

targeted 7 案:`2 FAIL + 4 ERROR + 1 OK`。
1 OK 是空鏡射 stamp-only 的表徵案(舊碼本來就這樣,要保住)。
2 FAIL 對得上 finding 1 與 finding 2 的公開失敗路徑。
4 ERROR 是舊碼沒有 `DurableMirrorDrift` /
`continuation_claim_is_honest`。

新守衛變異:把 `has_durable_mirror` 從 `ensure_durable_mirror`
executable path 拿掉 → `check-memory-architecture` 1 項紅。

### §5 只證明 Linux

owner 常駐限制:autoloop 看不到 macOS。本輪新測試沒有只正規化一邊
的路徑字串比對。

---

## 給 GPT 的下一輪輸入

**最該被挑戰的設計決策:**

1. **缺世代且沒有 durable 鏡射列 → 仍只蓋章,不重建。**
   新鮮空 DB + `.dev-flow/` 已有內容時,第一次 ask 仍可能答空,直到
   有人跑 `dev-setup`。這是升級前就存在的行為,本輪沒擴大也沒縮小。
   請指出一條**現在就會靜默答錯**的路徑,或承認這是可接受的
   「setup 才hydrate」契約。
2. **接續紀錄用「本檔 commit 不在該次檢查範圍」代替自我失效的最終 HEAD。**
   `durable-check` 的實際 SHA 只出現在 PR comment。git 正本因此不再
   宣稱一個它無法證明的 HEAD。若你認為「正本不記實際 checked SHA」
   本身仍是證據缺口,請給一個**現在就會踩到的錯誤決定**,不要只
   重申「要把 SHA 寫進檔」。

**我不打算改的方向與理由:**

- **不 merge。** 契約禁止。
- **不做 Candidate A/B/C / Stage 7 / 全表掃描 / worktree-key / auto-observe / `ssh -G`。** 0232 自己排除;A/B/C 仍等 0107 被顯式點名。
- **不把 entity-only CURRENT 改成歧義語意。** owner D-4。
- **不重開 D-1/D-2/D-3。** D-4 額度留給下一輪。
- **不下修 eval 門檻。** 23/23 與 current_truth_accuracy 1.0 維持。
- **下一輪若 newest 仍是 0232 且分支健康 → 跳過。**
