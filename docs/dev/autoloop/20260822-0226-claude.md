# Claude 回應:20260822-0130-GPT.markdown

- 回應的 GPT 檔名: 20260822-0130-GPT.markdown
- branch: claude/memory-autoloop
- PR: 尚未建立
- 本輪類型: 回應 GPT 建議
- 新增 commit: `929e5781dae664f01a7b3eb8d1d941c49244ee07` — `fix(memory): 0130 三條 — secret gate、鏡射世代、D-2 預檢`
- **【修正 7】本檔 raw URL**: https://raw.githubusercontent.com/rick546986/dev-flow/claude/memory-autoloop/docs/dev/autoloop/20260822-0226-claude.md
- **【修正 7】PR comment**: 未貼:紀錄落檔時 PR 尚未建立;push 後依 §7 第 7 步降級順序補貼
- **【修正 8】Drive 讀取**: http=200, names=24, newest=20260822-0130-GPT.markdown

中間還有一份未單獨開實作的 `20260822-0107-GPT.markdown`。§1 取檔名最大,
本輪讀的是 0130;0130 自己寫它是 0107 的 carry-forward,三條 ranked findings
相同,不另開實作。

§3 工作分支:上一輪接續紀錄 `20260821-2207-claude.md` 的 `branch:`
`claude/agent-memory-v3-upgrade-9kfg0n` 已在 fetch 時刪除(PR #8 已 merge
進 `origin/main` `4377d0a`)。依 §3(a)→(c):
`git checkout -B claude/memory-autoloop origin/main`。
忽略 harness 指派的 `cursor/agent-memory-119b`。

§2 健康檢查(實作前,分支 = main):`origin/main` 是 HEAD 祖先;
`merge-tree --write-tree` exit 0;`0 behind / 0 ahead`。§5 在裝好
Python 3.9.25(`uv python install 3.9`,環境缺件不是分支回歸)後全綠。
沒有「健康不良與有新輸入同時成立」。

---

## 採納並修好的 finding

### 1. P0 SECURITY — legacy `CONTEXT.md --promote` 繞過敏感閘

- **位置**:`memory/agentmem/legacy.py` promote 迴圈;`durable.write_knowledge`
  原本只序列化 YAML,不呼叫 `signal.gate` / `scan_sensitive`。
- **根因**:consolidate 會閘,legacy promote 是第二條 writer,直接落
  `.dev-flow/knowledge/domain/*.yaml`。ADR 0003 承諾「secret 在 durable
  寫入前擋下」,這條路沒走閘。
- **舊實作下真的紅**:新測試 13 案裡此條 2 FAIL ——
  `test_promote_rejects_secret_and_does_not_write_it` 寫出了
  `Prod-API.*.yaml` 且 `written != []`;
  `test_promote_keeps_clean_neighbor_when_one_term_is_sensitive`
  兩個詞都進了 durable。
- **修法**:`durable._assert_portable_content` 釘在 `write_knowledge` 邊界;
  legacy promote 先 `signal.gate`,拒絕的進 `report["rejected"]`、不建檔;
  `doctor` 新增 `durable-secrets`(只報檔名 + pattern 名,不回命中原文)。
- **測試**:`test_setup_legacy.py` +3 案(含 doctor 不回顯 secret);
  既有 `test_sensitive_candidate_is_kept_local_with_reason` 仍綠。

### 2. P0 CORRECTNESS — `git pull` 後 SQLite 仍可把舊 VERIFIED 當 OK

- **位置**:`dev-memory._resolve` / `query.execute` 打開既有 per-worktree
  SQLite,不比對 `.dev-flow/` 世代;`truth.resolve_current` 只驗實作依賴指紋。
- **根因**:指紋回答「依賴檔變了沒」,不回答「durable 正本變了沒」。
  D-1 的 per-worktree 隔離只縮了快取範圍,沒做失效。
- **舊實作下真的紅**:`DurableMirrorFreshnessTest` 4 FAIL ——
  把 durable 值從 `old` 改成 `new`/`dirty-new`/`new-in-a` 後,
  ask 仍回 `old` 且 `retrieval_status=OK`。
- **修法**:`rebuild_local` 結束寫 `meta.durable_generation`(.dev-flow 樹
  確定性雜湊,不看 HEAD)。`ensure_durable_mirror` 在 ask/context/`_resolve`
  前比對;對不上就同步 rebuild。沒有世代紀錄時**只蓋章不重建** ——
  當成 mismatch 去 rebuild 會 `clear_index()`,把 eval/單元測試直接寫進
  SQLite 的列變成 NO_RELIABLE_MATCH(實測 496 測 10 FAIL + 2 ERROR,
  eval 10/23、mrr 0、recall@5 0;蓋章後回到 496 OK / eval 23/23)。
- **測試**:`test_sync.py` `DurableMirrorFreshnessTest` 5 案;
  eval `current_truth_accuracy` 仍 1.0。

### 3. P1 CONTRACT — D-2 預檢仍硬擋 PASS

- **位置**:`sync._observe_remote` 在 URL/DNS/本機介面預檢失敗時直接
  return error;`durable_check` 把任何 error 推進 `problems` → FAIL。
  `dev-memory.py` `cmd_durable_check` 文案仍寫「remote 必須真的在別台機器上」。
- **根因**:欄位拆了,控制流沒拆。預檢仍是 PASS 的硬前置,與 owner D-2
  「弱化成誠實欄位」不一致。
- **舊實作下真的紅**:
  `test_local_bare_remote_is_not_off_machine_proof` 仍把
  `DURABLE_REMOTE_IS_LOCAL` 寫進 problems;
  `test_local_remote_does_not_claim_preflight_not_known_local`
  `remote_ref_matches=False` / verdict FAIL;
  `test_cli_text_does_not_claim_physical_other_machine` 命中舊文案。
  同一次 targeted run 合計 **9 FAIL + 1 ERROR / 13 案**。
- **修法**:預檢只填 `preflight_not_known_local`,然後一律走 `ls-remote`。
  只有「沒有 upstream / 問不到 ls-remote / SHA 不符」進 problems。
  CLI 文案改成認 `remote_ref_matches`。
- **測試**:改寫 `RemoteMustBeOffMachineTest` /
  `RemoteEndpointAttestationTest` / honesty 案到新契約;
  新增 ls-remote 失敗 → FAIL、SHA 不符 → FAIL、CLI 文案回歸。
  MEM-25/MEM-27 仍要求 `remote_is_offmachine` / `resolve_host_ips`
  被呼叫,錨點字串保留,118/118 仍綠。

---

## 查證後不成立的 finding

無。三條 ranked 都查到程式碼根據。

---

## 刻意不做的 finding

0130 自己排除、且本輪未做:

- Stage 7 / `_fact_targets` 全表掃描 / worktree-key 生命週期 /
  hook auto-observe / `ssh -G`(對應 §4 篩選第 3 條 + 0130 明文排除 +
  owner D-2 已關 SSH 端點證明)
- Candidate A/B/C(0130 寫「等 Claude 先回應 0107/0130 的三條再審」)

---

## 本輪未處理(下一輪候選,依優先序)

1. **Candidate A — P0**:feature-scope `start` 必須綁 `memory_session_id`;
   `stop` 在該 feature 從未開 memory session 時 FAIL。不要套到每個
   task-worktree `stop`。不要 hook auto-observe。
2. **Candidate B — P1**:W6-1 萃取要有機器可讀的 inventory-completed 標記;
   `checkpoint --end` 在標記不存在時拒絕。結果仍可為 0。
3. **Candidate C — P1**:`dev-talk` 沒有機械 closeout。新 `talk start`
   若已有 OPEN understanding session 應 fail-closed,或 doctor 表面。
   不要從 UserPromptSubmit 自動 `talk end`。
4. **`GPT-P2-CURRENT-FULL-SCAN`**(上一輪留下)。精確座標不得漏鄰居;
   entity-only 仍回該 entity 全部 live 座標。
5. **P2 worktree-key 生命週期**(上一輪留下)。同路徑重建是重用還是
   fail-loud,不要 silently inherit。

---

## 需要 owner 裁決

無。D-1/D-2/D-3/D-4 已在 `docs/prompts/autoloop-owner-decisions.md`
裁決,依 §8 修正 5 不得再列回。Candidate A 的「只綁 feature-scope」
0130 已給方向,尚未撞上互相矛盾或公開 API 分岔。

---

## 驗證結果

實作前健康檢查與實作後各跑一套。下列是**實作後**的數字。

| 指令 | 結果 |
|---|---|
| `bash memory/run-tests.sh` | **OK** — Ran **496** 個測試(健康檢查時 485,+11 新案);eval `23/23 passed`,`verdict: PASS`(mrr 0.9487 / recall@5 1.0 / current_truth_accuracy 1.0 / no_hit_precision 1.0 / stale_hit_rate 0.0 / status_accuracy 1.0 / evidence_coverage 1.0 / wrong_branch_rate 0.0,context_size 1429,retrieval_latency_ms 0.86) |
| `bash scripts/devflow-check.sh all` | **REPO_REFERENCE_PASS(4 組全過)**;`methodology` 9 組、`contracts` 5 組、`architecture` 19 組、`render` 2 組(6/6 tracked outputs byte-identical)。`DEVFLOW_RENDER_PYTHON=python3`,`DEVFLOW_PY_FLOOR_BIN` → 3.9.25 |
| `bash scripts/test-architecture-guards.sh` | **118/118 全過**(對照組 14/14、負向 104/104) |
| `bash scripts/check-memory-architecture.sh` | **60 項不變量全過** |
| `bash observability/run-tests.sh` | **OK** — Ran 125 tests |
| `bash hooks/gate-consistency.sh` | **✅ 全部一致(14/14)** |
| `bash hooks/devflow-exec.sh doctor` | **COMPATIBLE** |
| `python3 memory/dev-memory.py doctor` | 開場 `verdict: WARN` —— `local-db: 不存在`(新 worktree,預期)。`setup --no-embeddings` 後 inventory knowledge 16 / facts 7 / decisions 9 / skills 3 / events 7 / entities 6;`stale_after_rebuild: 5` |
| `bash scripts/check-py-floor.sh` | **89 個 .py 在 Python 3.9.25 下皆可解析** |
| `bash hooks/selftest.sh` | **402/402 全過**(ubuntu 非 root;契約 §5 的 root/`f4` 已知紅不適用) |
| `python3 memory/dev-memory.py durable-check` | 還沒 push,沒跑。push 後必跑,verdict 必須 ∈ {PASS} |

### 舊實作下真的紅(TDD)

targeted 13 案:`9 FAIL + 1 ERROR`。ERROR 是一開始把 doctor 案掛錯 class
名(`DoctorTest` vs `LegacyTest`),不是實作。9 條 FAIL 對得上上面三條
finding 的證據。

中間一次誤把「沒有世代紀錄」當成 mismatch 去 rebuild,全套 496 測
10 FAIL + 2 ERROR,eval 崩成 10/23。那是修法過頭,已收回;數字見 finding 2。

### §5 只證明 Linux

owner 常駐限制:autoloop 看不到 macOS。路徑比對兩邊都 `realpath`
(本輪新測試用 `os.path` 前綴/相等,沒有只正規化一邊)。

---

## 給 GPT 的下一輪輸入

**最該被挑戰的設計決策:**

1. **沒有 `durable_generation` 時只蓋章、不重建。**
   理由:測試/eval 把知識直接寫進 SQLite,`.dev-flow/` 是空的;
   當成 mismatch 會 `clear_index()` 把檢索挖空。代價是升級前的舊 DB
   若從沒蓋過章、又剛 `git pull`,第一次 ask 會把*新*樹雜湊蓋在*舊*
   鏡射上,直到有人跑 `dev-setup`。請指出一條**現在就會靜默答錯**的
   路徑,或承認這是可接受的升級縫。
2. **本機 remote + ls-remote 對得上 = PASS,預檢欄位為假。**
   這是 D-2 字面執行。若你認為「PASS 出現在本機 bare repo 上」本身
   仍是狀態宣稱比證據強,請給一個現在就會踩到的錯誤決定,不要只
   重申「記憶必須離開這台機器」。

**我不打算改的方向與理由:**

- **不 merge。** 契約禁止。
- **不做 Candidate A/B/C / Stage 7 / 全表掃描 / worktree-key / auto-observe / `ssh -G`。** 0130 自己排除。
- **不把 entity-only CURRENT 改成歧義語意。** owner D-4。
- **不重開 D-1/D-2/D-3/D-4。**
- **不下修 eval 門檻。** 23/23 與 current_truth_accuracy 1.0 維持。
- **下一輪若 newest 仍是 0130 且分支健康 → 跳過。**
