# Claude 回應:20260822-1830-review.md
- 回應的 review 檔名: 20260822-1830-review.md
- branch: claude/stage7-g3-hardening
- PR: https://github.com/rick546986/dev-flow/pull/16
- 本輪類型: 回應 review
- 新增 commit: c9b20b3 fix(g3): Required 空值即紅；層名改全等比對
- 本檔 raw URL: https://raw.githubusercontent.com/rick546986/dev-flow/claude/stage7-g3-hardening/docs/dev/stage7-loop/20260822-1846-claude.md
- PR comment: 未貼:尚未 push
- review 讀取: newest=20260822-1830-review.md, last=20260822-1630-review.md

最新 `*-claude.md` 是 1749,回應的是 `20260822-1630-review.md`。
git 上最新 `*-review.md` 仍是 1630。owner 10:32Z 在 PR #16 寫
`STAGE7-REVIEW: 20260822-1830`(三條),P2 要求同步成契約檔。本輪寫了
`docs/dev/stage7-loop/20260822-1830-review.md`,並自己重現 P0/P1。
健康:`origin/main`(`4377d0a`)是 HEAD 祖先;`merge-tree --write-tree` exit 0;
PR #16 CI 兩項 SUCCESS(run 32566045001 / 32566044982)。健康綠,不佔整輪。
WRITE-TEST 那則 PR comment 不是 STAGE7-REVIEW,忽略。

## 採納並修好的 finding

### 1830-P0 Required layers 空值不得當零層放行
- 位置:`scripts/devflow-evidence-gauntlet.sh` E7 / `parse_profile_layers`
- 我查到的根因:1630 只擋「欄不在」。欄在、值空或只有空白時
  `required_present=True`、`spec_required=[]`,E7 必跑迴圈是空的。
- 舊實作下真的紅:`test-evidence-gauntlet` **59/62**。空值:exit 0 / **27 checks**。
  只有空白:exit 0 / **27 checks**。`無` / `none` 仍綠。
- 修法:欄在但 token 為零 → E7 紅。只有「無」/none/n-a 是明示零層。
  Gauntlet **1.3.2 → 1.3.3**,五處版本錨 lockstep。
- 測試:`scripts/test-evidence-gauntlet.sh` 1830-P0 兩案。修後 **62/62**。

### 1830-P1 Required 層名必須全等,不得 substring
- 位置:同一支 gauntlet E7;`wanted.lower() in row[0].lower()`
- 我查到的根因:Required `unit` 被 evidence `unit-smoke` pass 滿足。
- 舊實作下真的紅:exit 0 / **28 checks**。全等 `unit` 與 `Unit (fast)` 舊實作已綠。
- 修法:`canon_layer_name` strip、去掉尾端括號、大小寫不敏感全等。
  Conditional 用同一 matcher,避免同一洞留在另一條迴圈。
- 測試:1830-P1 三案(substring 紅 / 全等綠 / 尾端括號綠)。

### 1830-P2 契約通道檔
- 寫了 `docs/dev/stage7-loop/20260822-1830-review.md`,內容與 PR comment 三條一致。

變異:把 `if review_mode and required_field_empty` 改 `if False and …`,
`layer_name_eq` 改回 substring → **59/62**(空值兩案回到 exit 0 / 27;
substring 回到 exit 0 / 28);還原後 62/62。

## 查證後不成立的 finding
無。三條都重現(P2 是寫檔,不是缺陷)。

## 刻意不做的 finding
- Windows 真機、STATUS 單寫者、第二個範例、engine-fence-masking 收尾文書、需求討論九條、記憶 / PR #15、重開 1–3、拿掉 G3
- Conditional「未列入 = 未觸發」仍靠 Reviewer(已裁,不列回)
- 未改 `docs/dev/STATUS.md`
- 未新增粗體 G3 token
- 未把 Gauntlet 改成去跑專案測試
- 非 `--review-file` 的 `--profile` 仍可指向任意 4-spec
- 空值規則只套 `--review-file`(與 1630 缺欄同一範圍)

## 本輪未處理(下一輪候選,依優先序)
1. Conditional「未列入 = 未觸發」仍靠 Reviewer
2. 本環境 `check-py-floor` 沒 3.9–3.11(exit 2)。main 上同一環境也會這樣
3. 本環境沒有 `python` 指令,`devflow-check.sh all` 的 methodology/render 預設
   `DEVFLOW_RENDER_PYTHON=python` 會紅;指定 `python3` 後綠。CI 有 `python`

## 需要 owner 裁決
無新項。1830 三條做完,不列回。

## 驗證結果
| 項 | 結果 |
|---|---|
| `scripts/test-evidence-gauntlet.sh` | 舊 **59/62**;修後 **62/62**。變異 59/62 |
| `scripts/check-stage67-enforcement.sh` | **60/60** |
| `scripts/check-version-sync.sh` | 5 處 **1.3.3** |
| `scripts/check-gate-tokens.sh` | 全過(未增刪粗體 token) |
| `hooks/gate-consistency.sh` | **14/14** |
| `hooks/selftest.sh` | **402/402**(本環境;提示的 f4 兩案紅未重現) |
| `hooks/devflow-exec.sh doctor` | COMPATIBLE;gauntlet 1.3.3 |
| `observability/run-tests.sh` | 125/125 |
| `scripts/check-methodology-corrections.sh` | 改 7-review 後 guide 先 104/106;render --write 後 **106/106**(`DEVFLOW_RENDER_PYTHON=python3`) |
| `scripts/render-methodology-corrections.sh --check` | **6/6** |
| `scripts/test-architecture-guards.sh` | **119/119**(對照 14/14、負向 105/105) |
| `scripts/check-py-floor.sh` | **exit 2**:環境沒有 3.9–3.11。與主機 main 相同 |
| `scripts/devflow-check.sh all` | **exit 1**。methodology 9/9、contracts 5/5、render 2/2 綠(`DEVFLOW_RENDER_PYTHON=python3`)。architecture fail-fast 在 py-floor exit 2。本機 all 不能當 CI 綠的證明 |
| `scripts/devflow-evidence-gauntlet.sh --help` | 用法錯誤 exit 2(腳本沒有 --help) |
| 平台 | 只證明 Linux。沒跑 macOS / Windows |

## 給下一輪審查的輸入
- 最該被挑戰:空值規則只套 `--review-file`。純 evidence 檔帶空的 Required layers 仍當零層。
- 我不打算改的方向:不重開 1–3;G3 留著;example / fixtures 仍可用顯式歷史 SHA;Gauntlet 不跑專案測試;非 review-file 的 `--profile` 仍自由
