# Stage 7 第一刀接續紀錄(2026-08-24 12:10 UTC)

第 6 站三刀已合進 main(`a6c2f21`,PR #37)。
本輪依序判定 → 開 `claude/stage7-graph`,只做第 7 站**第一刀**。
第 4／5／6 站三刀早已在 main,不再重做。

## 本刀咬了什麼(列檔)

新增:

| 檔 | 是什麼 |
|---|---|
| `scripts/check-devstage7-graph.sh` | Stage 7 節點鏈的機械契約(live check / `--action` / `--write-cursor`) |
| `scripts/test-devstage7-graph.sh` | 上面那支的負向牙,33 案 |
| `scripts/fixtures/devstage7-graph/good/` | 最小合法樹(五真節點 + 一暫留 hop + fixture/pending 兩個 slug) |
| `scripts/fixtures/devstage7-graph/old-skill/` | 舊實作:有 SKILL、沒有 `stage7/graph.yaml` |
| `scripts/fixtures/devstage7-graph/actions/` | 12 份 `--action` JSON |
| `skills/dev-flow/stage7/graph.yaml` | 下一跳唯一正本 |
| `skills/dev-flow/stage7/nodes/N0-role.md` | 角色+閱讀順序(禁讀 6-notes Self-Review;owner 自審無限制聲明節=未審) |
| `skills/dev-flow/stage7/nodes/N1-matrix.md` | 自建 coverage matrix(本刀不落檔) |
| `skills/dev-flow/stage7/nodes/N3-axes.md` | 雙軸審(本刀不落檔) |
| `skills/dev-flow/stage7/nodes/N4-author.md` | 對照作者並覆寫同一份 `7-review.md` |
| `skills/dev-flow/stage7/nodes/N5-verdict.md` | G3 判定(PASS 條件仍是現有 G3,不重寫工具) |

改動(只有註冊與一行指標):

| 檔 | 改了什麼 |
|---|---|
| `scripts/devflow-check.sh` | `group_methodology` 在 stage6 兩行後面加兩行(會執行的行,不是註解) |
| `guides/guide-dev-flow.html` | 附錄「檔案地圖」補兩列(`check-file-map.sh` 雙向盤點) |
| `scripts/check-file-map.sh` | `EXPECTED_MAPPED_FILES` 149 → 151 |
| `scripts/test-architecture-guards.sh` | 靜態互釘同步 149 → 151 |
| `.gitignore` | 加 `.devstage7-cursor.json` |
| `skills/dev-flow/SKILL.md` | 第 7 站那列只加「節點鏈見 `skills/dev-flow/stage7/`」 |

鏈(`graph.yaml` 正本):

```
N0-role → N1-matrix → skill-legacy-2 → N3-axes → N4-author → N5-verdict
```

暫留步是 `kind: skill-legacy` 真節點,`entry: _templates/7-review.md`
+ `steps: "2"`(模板 2／2b／2c／2d／2e),**沒有 via**。
單一產物 `docs/dev/<slug>/7-review.md`,`write_mode: overwrite`。
只有 `N4-author` `allow: write_review` 與 `read_notes`(N4 才准讀 6-notes,對應 review-unlock)。
`write_notes` 本刀一律 deny。

## 先紅後綠的數字

先寫牙,產品還沒進來 —— 對舊實作(main 的樹,沒有
`skills/dev-flow/stage7/graph.yaml`)跑 `scripts/check-devstage7-graph.sh`:

```
[graph] nodes_required=5 failures=13
❌ FAIL:13 項 → exit 1
```

13 項涵蓋必紅:①沒有 `stage7/graph.yaml` ②五個真節點缺「進條件」/「完成條件」
③同 slug 第二份 `7-review*.md` ④`write_mode≠overwrite` ⑤缺 `N4-author`
⑥未過 G2 卻 `write_review` ⑦審查期 `write_notes` ⑧`7-self-review.md`／`7-review-*.md`
⑨N0 讀 6-notes Self-Review。

再寫產品 → `failures=0`,exit 0。

負向牙 `scripts/test-devstage7-graph.sh` = **33/33**(下限寫死 `< 8 → exit 2`)。
含 `G-0` 對照組綠、`G-old` 舊實作紅(輸出含「沒有 graph」)、
`G-cursor --write-cursor N0-role` 寫出 `.devstage7-cursor.json` 且
`node=N0-role`、`G-action N4-author + 4-spec approved → write_review` 綠、
`N4-author read_notes` 綠、N0／N1／N3／N5／skill-legacy-2 做 `write_review` 各一槍紅、
未過 G2 紅、`write_notes` 紅、鏈外節點 exit 2、舊實作跑 `--action` exit 2。

## 下一刀是第二刀

每個真節點 `--write-cursor`(本刀已有)。`kind: skill-legacy` 翻成必須紅。
有游標才編成寫 7-review。圍欄③不要改鬆。guide `#stage7` 對上鏈。不要重開 hardening。

第三刀:prebash 接 `_devstage7_graph_action`(本機有 `.devstage7-cursor.json`
才編成;沒游標不攔截)+ guide `#stage7` 正文對上鏈。
不准改 Gauntlet、Evidence 契約、空欄擋、層名全等、「出貨樹=審過的樹」、Final Fresh 綁 SHA。

## 刻意不做

- **不接 prebash**:`hooks/_prebash_impl.py` 零改動,`--action` 本刀只活在檢查腳本。
- **不改 guide `#stage7` 正文節點鏈**:只補附錄檔案地圖兩列(後兩刀才改正文)。
- 不改 `_templates/` 任何正文 —— 乘客清單正本是 `_templates/7-review.md` 頂註 0–5。
- 不改 Gauntlet、Evidence 契約、空欄擋、層名全等、「出貨樹=審過的樹」、Final Fresh 綁 SHA。
- 不重做第 1／2／3／4／5／6 站。
- 不動 `memory/`、`.dev-flow/`、`devflow-exec.sh`、圍欄③。
- 不追加 `HISTORY.md`、不 merge、不推 main。
- 不用 `via` 字串當 hop;暫留步是 `kind: skill-legacy` 真節點。

## 等什麼

推 `claude/stage7-graph`,開 draft PR。**停**,等 STAGE7-REVIEW
「已咬／可開第二刀」。不要一次開完三刀。
