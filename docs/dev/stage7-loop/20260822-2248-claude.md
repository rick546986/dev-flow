# Claude 回應:20260822-2230-review.md
- 回應的 review 檔名: 20260822-2230-review.md
- branch: claude/stage7-g3-hardening
- PR: https://github.com/rick546986/dev-flow/pull/16
- 本輪類型: 回應 review
- 新增 commit: b70425b docs(stage7-loop): 2230 契約檔與 2248 接續紀錄
- 本檔 raw URL: https://raw.githubusercontent.com/rick546986/dev-flow/claude/stage7-g3-hardening/docs/dev/stage7-loop/20260822-2248-claude.md
- PR comment: 未貼:push 後補
- review 讀取: newest=20260822-2230-review.md, last=20260822-2130-review.md

git 上最新 `*-review.md` 原本是 2130,接續紀錄也指 2130。沒有更新的契約檔。
owner 14:33Z 在 PR #16 寫 `STAGE7-REVIEW: 20260822-2230`(三條)。本輪寫了
`docs/dev/stage7-loop/20260822-2230-review.md`,並自己重跑驗證。
健康:`origin/main`(`4377d0a`)是 HEAD 祖先;`merge-tree --write-tree` exit 0;
PR #16 CI 兩項 SUCCESS(run 32577282839 / 32577282837;HEAD `bd1d350`)。健康綠,不佔整輪。

## 採納並修好的 finding

### 2230-P0 四個假綠已有牙齒,本輪不開新產品刀
- 位置:`scripts/test-evidence-gauntlet.sh` 1830/2030/2130;`scripts/devflow-check.sh` group_architecture
- 我查到的根因:不是洞。`e978baa` 在 HEAD(`bd1d350`)祖先。DIST 正案在
  (`散發副本 Required unit 對 evidence unit pass → 綠`)。py-floor 缺席是
  `floor_rc -eq 2` 記紅繼續,`-eq 1` 仍 return 1。自己跑,不是信 comment。
- 舊實作下真的紅:本輪不改產品碼,沒有「加上新案後舊實作紅」的數字。
  四顆牙本機全綠:
  - 空欄:母版 + DIST 各 1 案 E7
  - substring `unit` vs `unit-smoke`:母版 + DIST 各 1 案 E7
  - DIST 只打母版:2030 三負向打 `$DIST_TOOL`
  - DIST 只有紅案:2130 正案打 `$DIST_TOOL` 預期 exit 0
- 修法:不開新刀。只寫契約檔。
- 測試:`scripts/test-evidence-gauntlet.sh` **68/68**(含上述四組)。
  CI `REPO_REFERENCE` SUCCESS on `bd1d350`(run 32577282839),自己查,不是信連結。

### 2230-P1 不要把「所有 exit 2 繼續」做成下一刀
- 位置:`scripts/devflow-check.sh` `group_architecture` / `check-gate-twin`
- 我查到的根因:本機 `architecture` 過了 py-floor 缺席之後,下一張網是
  `check-gate-twin` exit 2(`缺相依 markdown-it-py`)。這是環境,不是出貨假綠。
  把所有 exit 2 改成組內繼續,下一輪會再追下一顆缺套件,不是 G3 洞。
- 舊實作下真的紅:不適用。本輪刻意不做。
- 修法:不改 aggregator。接續紀錄單獨跑 stage67 / architecture-guards。
- 測試:單獨跑 `check-stage67-enforcement` **60/60**;
  `test-architecture-guards` **119/119**(對照 14/14、負向 105/105)。

### 2230-P2 契約通道檔
- 寫了 `docs/dev/stage7-loop/20260822-2230-review.md`,內容與 PR comment 三條一致。

## 查證後不成立的 finding
無。P0 是「已修好、不要再開刀」;自己重跑後成立。P1 是禁令,不是缺陷。P2 是寫檔。

## 刻意不做的 finding
- 不把「所有 exit 2 繼續」做成下一刀(2230-P1 明令)
- 不開新的產品刀(2230-P0 明令)
- Windows 真機、STATUS 單寫者、第二個範例、engine-fence-masking 收尾文書、需求討論九條、記憶 / PR #15、重開 1–3、拿掉 G3
- 未改 `docs/dev/STATUS.md`
- 未改 gauntlet 語意,未升 1.3.3
- 未裝 `markdown-it-py` / 3.9–3.11 / `python` 來把本機 `all` 刷綠

## 本輪未處理(下一輪候選,依優先序)
1. Conditional「未列入 = 未觸發」仍靠 Reviewer(種子以來未裁;2230 不開新刀,不收)
2. 本環境沒有 `python` 指令,`devflow-check.sh all` 的 methodology/render 預設
   `DEVFLOW_RENDER_PYTHON=python` 會紅;也沒 `markdown_it`
3. 無第三條產品洞。下一輪若沒有新 review,應跳過。

## 需要 owner 裁決
無新項。2230 三條做完(查證 + 寫檔 + 不開刀),不列回。
P0 已說四個假綠有牙齒、不開新刀 —— 這支 draft 還要不要合,是人審,不是本迴圈。

## 驗證結果
| 項 | 結果 |
|---|---|
| `scripts/test-evidence-gauntlet.sh` | **68/68** |
| `scripts/check-integration-regression-guard.sh` | **41/41** |
| `scripts/check-stage67-enforcement.sh` | **60/60**(單獨跑) |
| `scripts/test-architecture-guards.sh` | **119/119**(對照 14/14、負向 105/105;單獨跑) |
| `scripts/check-version-sync.sh` | 5 處 **1.3.3** |
| `scripts/check-gate-tokens.sh` | 全過(未增刪粗體 token) |
| `hooks/gate-consistency.sh` | **14/14** |
| `hooks/selftest.sh` | **402/402**(本環境;提示的 f4 兩案紅未重現) |
| `hooks/devflow-exec.sh doctor` | COMPATIBLE;gauntlet 1.3.3 |
| `observability/run-tests.sh` | 125/125 |
| `scripts/check-methodology-corrections.sh` | **105/106**:renderer 缺 `python` / `markdown_it`。本輪沒動 7-review |
| `scripts/check-py-floor.sh` | **exit 2**:環境沒有 3.9–3.11。腳本本身未改 WARN |
| `scripts/devflow-check.sh all` | **exit 1**。contracts 5/5 綠(含 gauntlet **68/68**、memory 485、eval 23/23)。methodology 缺 `python`;architecture 先 parity 41/41、py-floor 記紅繼續、再 gate-twin exit 2;render 缺 `python`(exit 127) |
| `scripts/devflow-evidence-gauntlet.sh --help` | 用法錯誤 exit 2(腳本沒有 --help) |
| 平台 | 只證明 Linux。沒跑 macOS / Windows。**不要把上面的綠讀成 CI 已綠** —— 本輪只加契約檔,CI 要等 push |

## 給下一輪審查的輸入
- 最該被挑戰:本輪零產品碼。若 2230 的意思是「停、等人審能不能合」,下一輪沒有新 review 就該跳過,不要自己找第三刀。
- 我不打算改的方向:不把所有 exit 2 改組內繼續;不重開 1–3;G3 留著;Gauntlet 不跑專案測試;不 merge
