# Claude 回應:20260822-1630-review.md
- 回應的 review 檔名: 20260822-1630-review.md
- branch: claude/stage7-g3-hardening
- PR: https://github.com/rick546986/dev-flow/pull/16
- 本輪類型: 回應 review
- 新增 commit: PENDING
- 本檔 raw URL: https://raw.githubusercontent.com/rick546986/dev-flow/claude/stage7-g3-hardening/docs/dev/stage7-loop/20260822-1749-claude.md
- PR comment: 未貼:先 commit
- review 讀取: newest=20260822-1630-review.md, last=STAGE7-REVIEW-20260822-1230

沒有任何既有 `*-review.md`(上一輪 1550 只吃 PR comment)。owner 09:23Z 在 PR #16
寫 `STAGE7-REVIEW: 20260822-1630`,P2 要求同步成契約檔。本輪寫了
`docs/dev/stage7-loop/20260822-1630-review.md`,並自己重現 P0/P1。
健康:`origin/main`(`4377d0a`)是 HEAD 祖先;`merge-tree --write-tree` exit 0;
PR #16 CI 兩項 SUCCESS(run 32560767548 / 32560767552)。健康綠,不佔整輪。

## 採納並修好的 finding

### 1630-P0 Profile 缺 Required layers 欄不得當零層放行
- 位置:`scripts/devflow-evidence-gauntlet.sh` E7 / `parse_profile_layers`
- 我查到的根因:有 `## Verification Profile` 但沒有 `Required layers` 那列時
  `spec_required=[]`,E7 必跑迴圈是空的。`good` 形 7-review 仍綠。
- 舊實作下真的紅:`test-evidence-gauntlet` **52/57**。缺欄:exit 0 / **27 checks**。
  `Required layers:無` 被當層名 → exit 1。`none` 同。
- 修法:欄必須在;缺欄 E7 紅。`無` / `none` / `n-a` 是明示零層。Gauntlet **1.3.1 → 1.3.2**,
  五處版本錨 lockstep。
- 測試:`scripts/test-evidence-gauntlet.sh` 1630-P0 三案。修後 **57/57**。
  變異:把 `if review_mode and required_field_missing` 與 `profile_cross` 改 `if False`
  → **54/57**(缺欄回到 exit 0 / 27 checks;跨 feature 回到 exit 0);還原後 57/57。

### 1630-P1 --profile 不得指向別份 feature
- 位置:同一支 gauntlet,`--review-file` 解析 PROFILE
- 我查到的根因:`--profile` 覆寫 sibling。`profile-unverified/7-review` 對自己的
  sibling 會 E7 紅,但 `--profile profile-pass/4-spec.md` 仍綠。
- 舊實作下真的紅:跨 feature exit 0 / **31 checks**。無 sibling + `--profile` 別份
  exit 0 / **29 checks**(1230 當逃生口的那條)。
- 修法:`--review-file` 只准 sibling `4-spec.md`,或同一 `docs/dev/<slug>/4-spec.md`。
  跨份不讀對方層、E7 紅。`--require-layer` 仍可加嚴同一份。
- 測試:1630-P1 兩案 + 1230 無 sibling `--profile` 改為預期紅 + sibling 顯式 `--profile` 綠。

### 1630-P2 契約通道檔
- 寫了 `docs/dev/stage7-loop/20260822-1630-review.md`,內容與 PR comment 三條一致。

## 查證後不成立的 finding
無。三條都重現(P2 是寫檔,不是缺陷)。

## 刻意不做的 finding
- Windows 真機、STATUS 單寫者、第二個範例、engine-fence-masking 收尾文書、需求討論九條、記憶 / PR #15、重開 1–3、拿掉 G3
- Conditional「未列入 = 未觸發」仍靠 Reviewer(已裁,不列回)
- 未改 `docs/dev/STATUS.md`
- 未新增粗體 G3 token
- 未把 Gauntlet 改成去跑專案測試
- `--profile` 在非 `--review-file`(純 evidence 檔)仍可指向任意 4-spec

## 本輪未處理(下一輪候選,依優先序)
1. Conditional「未列入 = 未觸發」仍靠 Reviewer
2. 本環境 `check-py-floor` 沒 3.9–3.11(exit 2)。main 上同一環境也會這樣
3. 本環境沒有 `python` 指令,`devflow-check.sh all` 的 methodology/render 預設
   `DEVFLOW_RENDER_PYTHON=python` 會紅;指定 `python3` 後綠。CI 有 `python`

## 需要 owner 裁決
無新項。1630 三條做完,不列回。

## 驗證結果
| 項 | 結果 |
|---|---|
| `scripts/test-evidence-gauntlet.sh` | 舊 **52/57**;修後 **57/57**。變異 54/57 |
| `scripts/check-stage67-enforcement.sh` | **60/60** |
| `scripts/check-version-sync.sh` | 5 處 **1.3.2** |
| `scripts/check-gate-tokens.sh` | 全過(未增刪粗體 token) |
| `hooks/gate-consistency.sh` | **14/14** |
| `hooks/selftest.sh` | **402/402**(本環境;提示的 f4 兩案紅未重現) |
| `hooks/devflow-exec.sh doctor` | COMPATIBLE;gauntlet 1.3.2 |
| `observability/run-tests.sh` | 125/125 |
| `scripts/check-methodology-corrections.sh` | 改 7-review 後 guide 先 104/106;render --write 後 **106/106**(`DEVFLOW_RENDER_PYTHON=python3`) |
| `scripts/render-methodology-corrections.sh --check` | **6/6**(同上) |
| `scripts/test-architecture-guards.sh` | **119/119**(對照 14/14、負向 105/105) |
| `scripts/check-py-floor.sh` | **exit 2**:環境沒有 3.9–3.11。與主機 main 相同 |
| `scripts/devflow-check.sh all` | **exit 1**。methodology 預設 `python` 不存在 → 1;指定 python3 後 methodology 9/9、render 2/2 綠。contracts 5/5 綠。architecture fail-fast 在 py-floor exit 2。本機 all 不能當 CI 綠的證明 |
| `scripts/devflow-evidence-gauntlet.sh --help` | 用法錯誤 exit 2(腳本沒有 --help) |
| 平台 | 只證明 Linux。沒跑 macOS / Windows |

## 給下一輪審查的輸入
- 最該被挑戰:`--profile` 跨份時不回退 sibling,直接 E7。人若以為旗標無效就走 sibling,會誤以為「沒套到」而不是「被擋」。
- 我不打算改的方向:不重開 1–3;G3 留著;example / fixtures 仍可用顯式歷史 SHA;Gauntlet 不跑專案測試;非 review-file 的 `--profile` 仍自由
