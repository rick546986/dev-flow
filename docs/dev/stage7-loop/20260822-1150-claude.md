# Claude 回應:SEED
- 回應的 review 檔名: SEED
- branch: claude/stage7-g3-hardening
- PR: https://github.com/rick546986/dev-flow/pull/16
- 本輪類型: branch 健康維護
- 新增 commit: <本 commit 落地後回填>
- 本檔 raw URL: https://raw.githubusercontent.com/rick546986/dev-flow/claude/stage7-g3-hardening/docs/dev/stage7-loop/20260822-1150-claude.md
- PR comment: 未貼:本 follow-up 指示不用 comment 工具;git 是正本
- review 讀取: newest=SEED, last=SEED

1144 那輪 lockstep 案數之後 CI 仍紅。沒有新 `*-review.md`。健康修復續跑。

## 採納並修好的 finding

### CI-2 整合回歸順序釘還在掃 Exit Checklist
- 位置:`scripts/check-integration-regression-guard.sh` `order_violation`
- 我查到的根因:P0-1 把程序搬到步 2c,Exit 那條改成「已在 Final Fresh 之前完成」,不再寫腳本檔名。守衛還用 `^- \[ \] \(條件式\)整合回歸` 抓 Exit 條目,於是永遠「條目內沒提到 devflow-integration-regression.sh」。1144 只修了案數,這條被 architecture fail-fast 藏在後面,案數綠了才現形。
- 舊實作下真的紅:本機與 CI `check-integration-regression-guard` **1/41**(`7-review Exit Checklist 條目順序正確`)。run [32549841970](https://github.com/rick546986/dev-flow/actions/runs/32549841970)
- 修法:`order_violation` 改抓步 2c(剝 blockquote 後 `2c.**整合回歸**` 到 `2d.`)。負向樣本改成 2c 形狀。MIN_CHECKS 仍 41。
- 測試:修後本機 **41/41**。三個負向(順序調換 / 缺動作行 / 動作詞被掏)仍紅。

## 查證後不成立的 finding
無

## 刻意不做的 finding
- 沒改 `devflow-integration-regression.sh` 頂註那句「Exit Checklist」(正副本 parity,動了要兩份一起改;不是這次紅因)
- 沒加「Exit 不得再出現腳本檔名」新檢查(會逼 MIN_CHECKS 41→42 + 靜態互釘 lockstep;步 2c 找不到工具就會紅,夠擋搬回去)
- 記憶 / PR #15 / STATUS.md / merge / 重開 1–3

## 本輪未處理(下一輪候選,依優先序)
1. 顯式 `--source-sha` ≠ HEAD 仍綠(等 owner)
2. Conditional 未列入 = 未觸發
3. 本環境 `check-py-floor` 沒 3.9–3.11(exit 2)

## 需要 owner 裁決
無新項。

## 驗證結果
| 項 | 結果 |
|---|---|
| `scripts/check-integration-regression-guard.sh` | 修前 1/41 紅;修後 **41/41** |
| `scripts/test-architecture-guards.sh` | 本 follow-up 沒重跑整包(沒動 EXPECTED_*;MIN_CHECKS 仍 41) |
| 其餘 §5 | 沿用 1144;本輪只動這一支守衛 |
| 平台 | 只證明 Linux |

## 給下一輪審查的輸入
- 最該被挑戰:architecture 組 fail-fast —— 修第一個紅看不到後面的紅。1144 以為案數是唯一洞。
- 我不打算改的方向:不把順序釘搬回 Exit;G3 留著
