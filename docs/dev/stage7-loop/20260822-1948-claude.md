# Claude 回應:20260822-1930-review.md
- 回應的 review 檔名: 20260822-1930-review.md
- branch: claude/stage7-g3-hardening
- PR: https://github.com/rick546986/dev-flow/pull/16
- 本輪類型: 回應 review / branch 健康維護
- 新增 commit: 待回填
- 本檔 raw URL: https://raw.githubusercontent.com/rick546986/dev-flow/claude/stage7-g3-hardening/docs/dev/stage7-loop/20260822-1948-claude.md
- PR comment: 未貼:先 push 再貼
- review 讀取: newest=20260822-1930-review.md, last=20260822-1830-review.md

git 上最新 `*-review.md` 原本是 1830,接續紀錄也指 1830。沒有更新的契約檔。
owner 11:33Z 在 PR #16 寫 `STAGE7-REVIEW: 20260822-1930`(三條)。本輪寫了
`docs/dev/stage7-loop/20260822-1930-review.md`,並自己重現 P0/P1。
健康:`origin/main`(`4377d0a`)是 HEAD 祖先;`merge-tree --write-tree` exit 0;
PR #16 CI `REPO_REFERENCE` FAILURE(run 32568554135)。健康紅,與 1930 是同一件事
(1846 變異沒還原到散發副本)。本輪修這個,不另開新 finding。

## 採納並修好的 finding

### 1930-P0 散發副本仍是 `if False`,空值假綠
- 位置:`docs/dev/tools/devflow-evidence-gauntlet.sh` E7;`scripts/` 母版已是
  `if review_mode and required_field_empty`
- 我查到的根因:1846 變異驗證改了散發副本的條件,只把母版還原。
  `test-evidence-gauntlet.sh` 的 `TOOL=` 只打母版,所以接續紀錄能寫 62/62。
  本機 `devflow-check.sh all` 在 py-floor / 沒有 `python` 指令時 fail-fast,
  永遠跑不到 CI 那條 parity。CI 有 3.9–3.11 與 `python`,parity 才現形。
- 舊實作下真的紅:散發副本空值 **exit 0 / 27 checks**;只有空白 **exit 0 / 27**。
  母版同案已是 exit 1 / 28。CI `check-integration-regression-guard` **1/41**
  (`內容不同:devflow-evidence-gauntlet.sh`)。
- 修法:`cp scripts/devflow-evidence-gauntlet.sh docs/dev/tools/`。不改母版語意。
- 測試:既有 1830-P0 兩案仍 62/62。還原後散發副本空值 **exit 1 / 28**。

### 1930-P1 散發副本 `layer_name_eq` 仍是 substring
- 位置:同一支散發副本;`canon_layer_name` 寫了但 matcher 沒用。
- 我查到的根因:同上,兩處 diff 就是 1846 記下的那兩刀變異。
- 舊實作下真的紅:散發副本 Required `unit` + evidence `unit-smoke` **exit 0 / 28**。
  母版同案 exit 1 / 28。
- 修法:跟著母版,`canon_layer_name` 全等。
- 測試:1830-P1。還原後散發副本 substring **exit 1 / 28**。

### 1930-P2 契約通道檔
- 寫了 `docs/dev/stage7-loop/20260822-1930-review.md`,內容與 PR comment 三條一致。

變異:把散發副本再改回 `if False` → parity **1/41** 紅;還原後 **41/41**。

## 查證後不成立的 finding
owner 寫「HEAD 的 gauntlet 仍是 `if False`」。**母版不是**,散發副本才是。
兩份只差這兩處。P0/P1 對採用專案入口成立,對母版測試入口不成立。

## 刻意不做的 finding
- Windows 真機、STATUS 單寫者、第二個範例、engine-fence-masking 收尾文書、需求討論九條、記憶 / PR #15、重開 1–3、拿掉 G3
- 未改 `docs/dev/STATUS.md`
- 未改母版 gauntlet 語意,未升 1.3.3
- 未把 `test-evidence-gauntlet.sh` 改成雙打母版+散發(額度滿;parity 已接住)
- 未把本機缺 `python` / 缺 3.9–3.11 改成不 fail-fast(環境,不是本迴圈)

## 本輪未處理(下一輪候選,依優先序)
1. Conditional「未列入 = 未觸發」仍靠 Reviewer
2. 本環境 `check-py-floor` 沒 3.9–3.11(exit 2)。main 上同一環境也會這樣
3. 本環境沒有 `python` 指令,`devflow-check.sh all` 的 methodology/render 預設
   `DEVFLOW_RENDER_PYTHON=python` 會紅;本輪也沒 `markdown_it`

## 需要 owner 裁決
無新項。1930 三條做完,不列回。

## 驗證結果
| 項 | 結果 |
|---|---|
| 散發副本 1830-P0/P1(修前) | 空值 exit 0 / 27;空白 exit 0 / 27;substring exit 0 / 28 |
| 散發副本 1830-P0/P1(修後) | 空值 exit 1 / 28;substring exit 1 / 28 |
| `scripts/test-evidence-gauntlet.sh` | **62/62** |
| `scripts/check-integration-regression-guard.sh` | 修前 CI **1/41**;修後本機 **41/41**;變異 1/41 |
| `scripts/check-stage67-enforcement.sh` | **60/60** |
| `scripts/check-version-sync.sh` | 5 處 **1.3.3** |
| `scripts/check-gate-tokens.sh` | 全過(未增刪粗體 token) |
| `hooks/gate-consistency.sh` | **14/14** |
| `hooks/selftest.sh` | **402/402**(本環境;提示的 f4 兩案紅未重現) |
| `hooks/devflow-exec.sh doctor` | COMPATIBLE;gauntlet 1.3.3 |
| `observability/run-tests.sh` | 125/125 |
| `scripts/check-methodology-corrections.sh` | **105/106**:renderer 缺 `python` / `markdown_it`。本輪沒動 7-review |
| `scripts/render-methodology-corrections.sh --check` | 缺 `markdown_it`,exit 1 |
| `scripts/test-architecture-guards.sh` | **119/119**(對照 14/14、負向 105/105) |
| `scripts/check-py-floor.sh` | **exit 2**:環境沒有 3.9–3.11。與主機 main 相同 |
| `scripts/devflow-check.sh all` | **exit 1**。contracts 5/5 綠(含 gauntlet 62/62、memory 485、eval 23/23)。methodology 缺 `python`;architecture fail-fast 在 py-floor exit 2,所以本機 all **仍然跑不到** parity。parity 是另外單跑的 41/41 |
| `scripts/devflow-evidence-gauntlet.sh --help` | 用法錯誤 exit 2(腳本沒有 --help) |
| 平台 | 只證明 Linux。沒跑 macOS / Windows。**不要把上面的綠讀成 CI 已綠** —— CI 要等本輪 push |

## 給下一輪審查的輸入
- 最該被挑戰:1846 寫「變異還原後 62/62」時,測試只打母版、本機 all 因 py-floor fail-fast 看不到 parity。這是「宣稱比證據強」的形狀,跟本迴圈在修的洞同類。要不要讓 `test-evidence-gauntlet.sh` 雙打兩份,或讓本機 all 在 py-floor 缺席時仍跑完後面的 architecture。
- 我不打算改的方向:不重開 1–3;G3 留著;example / fixtures 仍可用顯式歷史 SHA;Gauntlet 不跑專案測試
