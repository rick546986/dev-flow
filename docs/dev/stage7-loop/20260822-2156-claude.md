# Claude 回應:20260822-2130-review.md
- 回應的 review 檔名: 20260822-2130-review.md
- branch: claude/stage7-g3-hardening
- PR: https://github.com/rick546986/dev-flow/pull/16
- 本輪類型: 回應 review
- 新增 commit: 待補
- 本檔 raw URL: https://raw.githubusercontent.com/rick546986/dev-flow/claude/stage7-g3-hardening/docs/dev/stage7-loop/20260822-2156-claude.md
- PR comment: 未貼:尚未 push
- review 讀取: newest=20260822-2130-review.md, last=20260822-2030-review.md

git 上最新 `*-review.md` 原本是 2030,接續紀錄也指 2030。沒有更新的契約檔。
owner 13:34Z 在 PR #16 寫 `STAGE7-REVIEW: 20260822-2130`(三條)。本輪寫了
`docs/dev/stage7-loop/20260822-2130-review.md`,並自己重現 P0/P1。
健康:`origin/main`(`4377d0a`)是 HEAD 祖先;`merge-tree --write-tree` exit 0;
PR #16 CI 兩項 SUCCESS(run 32574368795 / 32574368656)。健康綠,不佔整輪。

## 採納並修好的 finding

### 2130-P0 散發副本要有正案,不能只負向
- 位置:`scripts/test-evidence-gauntlet.sh` 2030 的 DIST 三案
- 我查到的根因:三案全是「要紅」。把散發副本改成 `--review-file` 一律
  `sys.exit(1)` 印 E7,三案仍過,母版測試 **66/66**。parity 對「入口壞掉但仍
  能紅」沒牙齒。
- 舊實作下真的紅:一律 E7 後既有測試仍 **66/66**。加上正案後變異 **67/68**
  (正案 exit 1,預期 0)。三負向仍綠。
- 修法:複用 `$TMP/req-exact`,散發副本 `Required unit` + evidence `unit`
  pass 必須綠。不改 gauntlet 語意,不升版本。
- 測試:2130-P0。修後 **68/68**。變異(散發副本一律 E7)→ **67/68**。

### 2130-P1 py-floor 缺席記紅、組內繼續
- 位置:`scripts/devflow-check.sh` `group_architecture`
- 我查到的根因:2030 只把 parity 挪到 py-floor 前。`run … || return 1` 對
  exit 2 仍 fail-fast。本環境沒 3.9–3.11,architecture 在 py-floor 後
  **0** 次後續檢查。`check-stage67-enforcement` / `test-architecture-guards`
  本機 aggregator 看不到。
- 舊實作下真的紅:2130-P1 源碼檢查紅。`devflow-check.sh architecture` 在
  py-floor 後只印摘要,沒有 gate-tokens 及之後的標籤。加上兩案後舊實作
  **67/68**。
- 修法:exit 2(直譯器缺席)該項記紅、組內繼續;exit 1(語法超下限)仍
  fail-fast。組尾 `[ -z "$FAILED" ] || return 1`,缺席不會被後面的綠蓋掉。
  **不**把 `check-py-floor.sh` 改 WARN,缺席仍 exit 2。
- 測試:2130-P1。修後 **68/68**。變異(換回 `|| return 1`)→ **67/68**。
  行為:`architecture` 過了 py-floor 之後跑到 gate-tokens / design-contract /
  adr / stale-paths / history,再卡在 `check-gate-twin` exit 2(缺
  `markdown-it-py`)。stage67 / test-architecture-guards 在 aggregator 裡
  仍被下一張網擋住;兩支單獨跑是 **60/60** 與 **119/119**。

### 2130-P2 契約通道檔
- 寫了 `docs/dev/stage7-loop/20260822-2130-review.md`,內容與 PR comment 三條一致。

## 查證後不成立的 finding
無。三條都重現(P2 是寫檔,不是缺陷)。

## 刻意不做的 finding
- Windows 真機、STATUS 單寫者、第二個範例、engine-fence-masking 收尾文書、需求討論九條、記憶 / PR #15、重開 1–3、拿掉 G3
- 未改 `docs/dev/STATUS.md`
- 未改 gauntlet 語意,未升 1.3.3
- 未把所有 exit 2(含 gate-twin 缺套件)改成組內繼續。P1 只收 py-floor 直譯器缺席
- 未把 py-floor 缺席改 WARN
- 未裝 `markdown-it-py` / 3.9–3.11 來把本機 `all` 刷綠

## 本輪未處理(下一輪候選,依優先序)
1. `check-gate-twin` 缺 `markdown-it-py` 仍 exit 2 + 組內 fail-fast。本機
   aggregator 過了 py-floor 之後仍看不到 stage67 / test-architecture-guards
2. Conditional「未列入 = 未觸發」仍靠 Reviewer
3. 本環境沒有 `python` 指令,`devflow-check.sh all` 的 methodology/render 預設
   `DEVFLOW_RENDER_PYTHON=python` 會紅;本輪也沒 `markdown_it`

## 需要 owner 裁決
無新項。2130 三條做完,不列回。
要不要把「exit 2 = 檢查本身沒證據」推廣到整組 architecture(不只 py-floor),
讓本機真的跑到 stage67 / 負向守衛 —— 寫在下一節,不當本輪裁決缺口。

## 驗證結果
| 項 | 結果 |
|---|---|
| DIST 一律 E7(修前,既有測試) | **66/66**;三負向仍綠 |
| 加上 2130 兩案後舊實作 | **67/68**(P1 源碼檢查紅) |
| `scripts/test-evidence-gauntlet.sh` | 修後 **68/68**。變異 DIST 一律 E7 **67/68**;變異序 **67/68** |
| `scripts/devflow-check.sh architecture` | 過 py-floor 後繼續;卡在 gate-twin exit 2(缺 markdown-it-py)。py-floor 本身仍記紅 |
| `scripts/check-integration-regression-guard.sh` | **41/41** |
| `scripts/check-stage67-enforcement.sh` | **60/60**(單獨跑;aggregator 還沒跑到) |
| `scripts/test-architecture-guards.sh` | **119/119**(對照 14/14、負向 105/105;單獨跑) |
| `scripts/check-version-sync.sh` | 5 處 **1.3.3** |
| `scripts/check-gate-tokens.sh` | 全過(未增刪粗體 token) |
| `hooks/gate-consistency.sh` | **14/14** |
| `hooks/selftest.sh` | **402/402**(本環境;提示的 f4 兩案紅未重現) |
| `hooks/devflow-exec.sh doctor` | COMPATIBLE;gauntlet 1.3.3 |
| `observability/run-tests.sh` | 125/125 |
| `scripts/check-methodology-corrections.sh` | **105/106**:renderer 缺 `python` / `markdown_it`。本輪沒動 7-review |
| `scripts/check-py-floor.sh` | **exit 2**:環境沒有 3.9–3.11。腳本本身未改 WARN |
| `scripts/devflow-check.sh all` | **exit 1**。contracts 5/5 綠(含 gauntlet **68/68**、memory 485、eval 23/23)。methodology 缺 `python`;architecture 先 parity 41/41、py-floor 記紅繼續、再 gate-twin exit 2;render 缺 `python`(exit 127) |
| `scripts/devflow-evidence-gauntlet.sh --help` | 用法錯誤 exit 2(腳本沒有 --help) |
| 平台 | 只證明 Linux。沒跑 macOS / Windows。**不要把上面的綠讀成 CI 已綠** —— CI 要等本輪 push |

## 給下一輪審查的輸入
- 最該被挑戰:P1 只特製 py-floor 缺席,沒把「exit 2 = 沒證據」推廣到整組。
  本機 `architecture` 下一張網是 gate-twin 缺套件,stage67 / 負向守衛在
  aggregator 裡照樣看不到。要不要對所有 exit 2 記紅繼續,只對 exit 1 fail-fast。
- 我不打算改的方向:不把 py-floor 改 WARN;不重開 1–3;G3 留著;Gauntlet 不跑專案測試
