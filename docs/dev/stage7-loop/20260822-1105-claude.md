# Claude 回應:SEED
- 回應的 review 檔名: SEED
- branch: claude/stage7-g3-hardening
- PR: https://github.com/rick546986/dev-flow/pull/16
- 本輪類型: 第一輪種子
- 新增 commit: 726b7ac fix(g3): Stage 7 出貨樹與 Required 層
- 本檔 raw URL: https://raw.githubusercontent.com/rick546986/dev-flow/claude/stage7-g3-hardening/docs/dev/stage7-loop/20260822-1105-claude.md
- PR comment: 已貼:PR #16 review body(gh issue comment 無權限)
- review 讀取: newest=SEED, last=無

## 採納並修好的 finding

### P0-1 審過的樹 = 出貨的樹
- 位置:`_templates/7-review.md` 執行清單 / Exit Checklist;README §7 第 1 點;guides 對帳
- 我查到的根因:頂註 2c 是 Final Fresh,整合回歸住 Exit Checklist。Verdict 之後還能合併 `INTEGRATION_SHA`,`ALREADY_SYNCED` 只寫「證據不算數」
- 舊實作下真的紅:`check-stage67-enforcement` 6 條 ST 失敗(integ=-1,fresh=4117);`test-evidence-gauntlet` 38/43,其中 P0-1 兩案紅
- 修法:整合回歸改為步 2c(Final Fresh 之前);Exit 不得再改程式碼;ALREADY_SYNCED 恢復路徑=重跑 Final Fresh 或 FAIL;Verdict 後改碼作廢 G3
- 測試:`scripts/check-stage67-enforcement.sh` ST 組(+8,MIN_CHECKS 52→60);`test-evidence-gauntlet.sh` P0-1;S67-ST mutation

### P0-2 Required 層從 4-spec 讀,漏帶即紅
- 位置:`scripts/devflow-evidence-gauntlet.sh` E7
- 我查到的根因:E7 只看 `--require-layer`。漏旗標時 Required=unverified 仍 exit 0(README §7 對照表自己承認假綠)
- 舊實作下真的紅:profile-unverified 漏旗標 exit 0、29 checks passed
- 修法:預設讀 sibling(或 `--profile`)4-spec Required;旗標只能加嚴。已列入且非 n-a 的 Conditional 必須 pass
- 測試:profile-unverified / profile-pass fixtures;example 不帶旗標仍綠。變異:關掉 4-spec 讀取 → 同一 fixture 回到 exit 0 / 29 checks

### P0-3 --review-file 漏帶 --source-sha 預設 HEAD
- 位置:同一支 gauntlet E2
- 我查到的根因:不帶 `--source-sha` 只驗宣告存在。good-review SHA=abc1234 ≠ HEAD 仍綠
- 舊實作下真的紅:exit 0、25 checks passed
- 修法:`--review-file` 且未顯式傳 SHA → `git rev-parse HEAD`。顯式 `--source-sha` 仍只比對該值(fixture / 歷史 example 不殺)
- 測試:`test-evidence-gauntlet.sh` P0-3

## 查證後不成立的 finding
無(本輪吃種子,沒有 review 檔)

## 刻意不做的 finding
- Windows 真機、STATUS 單寫者、第二個範例、engine-fence-masking 收尾文書、需求討論九條、記憶系統 / PR #15、重開 Stage 1–3、把 T review 與 G3 併層
- 未改 `docs/dev/STATUS.md`
- 未把 Gauntlet 改成去跑專案測試
- 未新增粗體 G3 token(出貨樹收進第 1 點全文 + 模板節序)

## 本輪未處理(下一輪候選,依優先序)
1. `--review-file` 加上顯式 `--source-sha` 且該值 ≠ 當下 HEAD 仍綠 —— 歷史 example 靠這條活;若要「任何程式碼 commit 自動作廢」在 CI 掃所有 7-review,會誤殺 example。要不要另開「僅當下 feature 目錄」模式,請 owner 裁
2. Conditional「未列入 = 未觸發」仍靠 Reviewer。要把 Profile 條件變成機械判斷,超出本輪
3. `check-py-floor` 在本環境找不到 3.9–3.11 直譯器(exit 2)。本機只有 3.12。main 上同一環境也會這樣,不是本輪回歸

## 需要 owner 裁決
- 上面第 1 條:要不要讓 `--review-file` **即使帶了** `--source-sha` 也強制等於 HEAD。會破 example 的歷史 SHA 示範命令

已在本 prompt 裁定的不列回:兩刀要做、不重開 1–3、不 merge、不動記憶、不拿掉 G3。

## 驗證結果
| 項 | 結果 |
|---|---|
| `scripts/test-evidence-gauntlet.sh` | 44/44(舊實作 38/43) |
| `scripts/check-stage67-enforcement.sh` | 60/60(舊實作 6 條 ST 紅) |
| `hooks/gate-consistency.sh` | 14/14 |
| `scripts/check-gate-tokens.sh` | 全過(八點 lockstep,第 1 點加長) |
| `scripts/check-version-sync.sh` | 5 處 1.3.0 |
| `scripts/check-methodology-corrections.sh` | 106/106 |
| `scripts/render-methodology-corrections.sh --check` | 6/6 |
| `scripts/test-architecture-guards.sh` | 全過(含 VS-* 1.3.0、S67-ST) |
| `hooks/selftest.sh` | 402/402(本環境;提示的 f4 兩案紅未重現) |
| `hooks/devflow-exec.sh doctor` | COMPATIBLE;gauntlet 1.3.0 |
| `observability/run-tests.sh` | 125/125 |
| `memory/run-tests.sh` | 485 + 評測 23/23(順便綠;本迴圈沒動 memory/) |
| `scripts/check-py-floor.sh` | 沒跑成:exit 2,環境沒有 3.9–3.11 |
| `scripts/devflow-check.sh all` | 未整包重跑(methodology 曾因 guides 未對帳紅過,已修;architecture 組會卡 py-floor) |
| `devflow-evidence-gauntlet.sh --help` | 用法錯誤 exit 2(腳本沒有 --help,符合既有契約) |
| 平台 | 只證明 Linux。沒跑 macOS / Windows |

變異:把 `if profile_path` 改成 `if False` → profile-unverified 回到 exit 0 / 29 checks;還原後 E7 兩條紅。

## 給下一輪審查的輸入
- 最該被挑戰的設計決策:
  1. Conditional 未列入視為未觸發 —— 漏列已觸發層仍假綠
  2. 顯式 `--source-sha` 不綁 HEAD —— 出貨樹機械作廢只覆蓋「漏帶旗標」這條洞
- 我不打算改的方向:不重開 1–3;G3 留著;現象證據 / Walkthrough 仍是 Reviewer 的事;Gauntlet 不跑專案測試
