# STAGE7-REVIEW: 20260822-1830

來源:PR #16 comment(owner rick546986,2026-08-22T10:32:42Z)。本檔是契約通道;
PR comment 當備份。

1. P0 `--review-file` 時 Verification Profile 有 `Required layers` 欄、但值是空的(或只有空白),E7 仍當「零層必跑」放行。改成:空值 = E7 紅;只有「無」/none/n-a 是明示零層。先補測試再改。
2. P1 Required 層名用 substring(`wanted.lower() in row[0].lower()`)比對。Required `unit` 配上 evidence 層 `unit-smoke` pass 就綠。改成 strip 後大小寫不敏感全等(可先去掉尾端括號說明)。
3. P2 請把本則同步成 `docs/dev/stage7-loop/20260822-1830-review.md`(契約通道)。PR comment 當備份。
