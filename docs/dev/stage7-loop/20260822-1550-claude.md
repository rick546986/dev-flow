# Claude 回應:STAGE7-REVIEW-20260822-1230
- 回應的 review 檔名: STAGE7-REVIEW-20260822-1230
- branch: claude/stage7-g3-hardening
- PR: https://github.com/rick546986/dev-flow/pull/16
- 本輪類型: 回應 review
- 新增 commit: (push 後補 SHA)
- 本檔 raw URL: https://raw.githubusercontent.com/rick546986/dev-flow/claude/stage7-g3-hardening/docs/dev/stage7-loop/20260822-1550-claude.md
- PR comment: 未貼:push 後補
- review 讀取: newest=PR#16 comment STAGE7-REVIEW:20260822-1230, last=SEED

沒有任何 `docs/dev/stage7-loop/*-review.md`。上一輪 `20260822-1150-claude.md` 寫 last=SEED。
契約字面:無 review 檔 + 兩條種子 P0 已標修好 → 沒有新輸入。
本輪仍做了:owner 在 PR #16 的 `STAGE7-REVIEW: 20260822-1230` 三條,自己重現後當成建議輸入。
健康:`origin/main`(`4377d0a`)是 HEAD 祖先;`merge-tree --write-tree` exit 0;上一輪 CI 兩項 SUCCESS。健康綠,不佔整輪。

## 採納並修好的 finding

### 1230-P0 --review-file 找不到 Profile 不得退回 1.2.0
- 位置:`scripts/devflow-evidence-gauntlet.sh` E7;舊註解寫「找不到就不讀,旗標行為與 1.2.0 相同」
- 我查到的根因:sibling 4-spec 不在、也沒 `--profile` 時 `PROFILE` 空,E7 只看旗標。`good-review.md --review-file --source-sha abc1234` 漏帶仍綠。
- 舊實作下真的紅:`test-evidence-gauntlet` **48/51**。無 sibling:exit 0 / **27 checks**。sibling 無 Verification Profile 節:exit 0 / **27 checks**。
- 修法:`--review-file` 找不到檔或檔內無 `## Verification Profile` → E7 fail-closed。`--profile` 指向有效檔仍綠。Gauntlet 1.3.0 → **1.3.1**,五處版本錨 lockstep。
- 測試:`scripts/test-evidence-gauntlet.sh` 1230-P0。修後 **51/51**。變異:把 `if review_mode and (profile_missing…)` 改成 `if False and …` → **49/51**(兩條 P0 回到 exit 0 / 27 checks);還原後 51/51。

### 1230-P1 docs/dev/<feature>/7-review 顯式 SHA 也必須 = HEAD
- 位置:同一支 gauntlet E2
- 我查到的根因:顯式 `--source-sha` 只比對該值。live feature 宣告=abc1234、HEAD 已走,帶舊 SHA 仍綠。owner 1230 裁:只套 `docs/dev/<slug>/7-review.md`;example/ 與 scripts/fixtures/ 不套。
- 舊實作下真的紅:暫德 repo `docs/dev/live-feature/7-review.md` exit 0 / **29 checks**。
- 修法:該路徑 `--review-file` 強制 `SOURCE_SHA=$(git rev-parse HEAD)`。example/ 與 fixtures 仍只比對顯式值。
- 測試:1230-P1 四案(live 紅 / example 綠 / fixtures 綠 / 宣告=HEAD 綠)。變異:把 live 路徑判斷改 `if false &&` → **50/51**(live 回到 exit 0 / 29 checks);還原後 51/51。

### 1230-P2 接續紀錄必須貼 all 的真實 exit
- 本檔驗證欄貼 `scripts/devflow-check.sh all` 真實 exit,**exit 1**。不是「守衛全過」。原因:architecture 組 fail-fast 在 `check-py-floor` exit 2(本環境沒 3.9–3.11)。methodology / contracts / render 三組綠。`test-architecture-guards` 另外跑 **119/119**。CI 才是跨該環境洞的證明。

## 查證後不成立的 finding
無。三條都重現。

## 刻意不做的 finding
- Windows 真機、STATUS 單寫者、第二個範例、engine-fence-masking 收尾文書、需求討論九條、記憶 / PR #15、重開 1–3、拿掉 G3
- Conditional「未列入 = 未觸發」仍靠 Reviewer(已裁,不列回)
- 未改 `docs/dev/STATUS.md`
- 未開新 `*-review.md`(不冒充審查者);本輪輸入是 PR comment
- 未新增粗體 G3 token

## 本輪未處理(下一輪候選,依優先序)
1. 契約只認 `*-review.md`,owner 卻把 1230 寫在 PR comment —— 下一輪若要穩接,請寫 `docs/dev/stage7-loop/<YYYYMMDD-HHMM>-review.md`
2. Conditional 未列入 = 未觸發
3. 本環境 `check-py-floor` 沒 3.9–3.11(exit 2)。main 上同一環境也會這樣

## 需要 owner 裁決
無新項。1230 的 SHA=HEAD 範圍(只套 docs/dev/<feature>)已做完,不列回。

## 驗證結果
| 項 | 結果 |
|---|---|
| `scripts/test-evidence-gauntlet.sh` | 舊 48/51;修後 **51/51** |
| `scripts/check-stage67-enforcement.sh` | **60/60** |
| `scripts/check-version-sync.sh` | 5 處 **1.3.1** |
| `scripts/check-gate-tokens.sh` | 全過(未增刪粗體 token) |
| `hooks/gate-consistency.sh` | **14/14** |
| `hooks/selftest.sh` | **402/402**(本環境;提示的 f4 兩案紅未重現) |
| `hooks/devflow-exec.sh doctor` | COMPATIBLE;gauntlet 1.3.1 |
| `observability/run-tests.sh` | 125/125(包在 contracts) |
| `scripts/check-methodology-corrections.sh` | 改 7-review 後 guide 先 104/106;render --write 後 **106/106** |
| `scripts/render-methodology-corrections.sh --check` | **6/6** |
| `scripts/test-architecture-guards.sh` | **119/119**(對照 14/14、負向 105/105) |
| `scripts/check-py-floor.sh` | **exit 2**:環境沒有 3.9–3.11。與主機 main 相同 |
| `scripts/devflow-check.sh all` | **exit 1**。methodology 9/9 綠;contracts 5/5 綠;render 2/2 綠;architecture fail-fast 在 py-floor exit 2。本機 all 不能當 CI 綠的證明 |
| `scripts/devflow-evidence-gauntlet.sh --help` | 用法錯誤 exit 2(腳本沒有 --help) |
| 平台 | 只證明 Linux。沒跑 macOS / Windows |

## 給下一輪審查的輸入
- 最該被挑戰:把 PR comment 當 review 輸入,是為了不讓 1230 再空轉三小時。代價是契約的「沒有 *-review.md 就跳過」被我繞過。下一輪請用檔。
- 我不打算改的方向:不重開 1–3;G3 留著;example / fixtures 仍可用顯式歷史 SHA;Gauntlet 不跑專案測試
