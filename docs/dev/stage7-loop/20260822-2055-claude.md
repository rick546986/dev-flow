# Claude 回應:20260822-2030-review.md
- 回應的 review 檔名: 20260822-2030-review.md
- branch: claude/stage7-g3-hardening
- PR: https://github.com/rick546986/dev-flow/pull/16
- 本輪類型: 回應 review
- 新增 commit: dc694d4 fix(g3): 散發副本三案進測試；parity 挪到 py-floor 前
- 本檔 raw URL: https://raw.githubusercontent.com/rick546986/dev-flow/claude/stage7-g3-hardening/docs/dev/stage7-loop/20260822-2055-claude.md
- PR comment: 已貼:https://github.com/rick546986/dev-flow/pull/16#pullrequestreview-5000218964
- review 讀取: newest=20260822-2030-review.md, last=20260822-1930-review.md

git 上最新 `*-review.md` 原本是 1930,接續紀錄也指 1930。沒有更新的契約檔。
owner 12:36Z 在 PR #16 寫 `STAGE7-REVIEW: 20260822-2030`(三條)。本輪寫了
`docs/dev/stage7-loop/20260822-2030-review.md`,並自己重現 P0/P1。
健康:`origin/main`(`4377d0a`)是 HEAD 祖先;`merge-tree --write-tree` exit 0;
PR #16 CI 兩項 SUCCESS(run 32571312278 / 32571312294)。健康綠,不佔整輪。

## 採納並修好的 finding

### 2030-P0 1830 三案必須打散發副本
- 位置:`scripts/test-evidence-gauntlet.sh` `TOOL=` 只指向 `scripts/`
- 我查到的根因:1846/1930 的洞是「母版測試綠、採用專案入口仍假綠」。
  母版與散發副本對齊之後,測試仍只打母版,下一輪再把散發副本改壞,
  `62/62` 繼續綠。parity 是另一張網,本機還被 py-floor fail-fast 吃掉。
- 舊實作下真的紅:散發副本改回 `if False` + substring 後,母版測試仍
  **62/62**。散發副本空值 **exit 0 / 27**;只有空白 **exit 0 / 27**;
  `unit` 被 `unit-smoke` 滿足 **exit 0 / 28**。加上三案後 **62/66**。
- 修法:同一組 fixture 再打 `docs/dev/tools/devflow-evidence-gauntlet.sh`。
  不改 gauntlet 語意,不升版本。
- 測試:2030-P0 三案。修後 **66/66**。變異(散發副本再改壞)→ **63/66**。

### 2030-P1 parity 挪到 py-floor fail-fast 之前
- 位置:`scripts/devflow-check.sh` `group_architecture`
- 我查到的根因:組內第一支是 `check-py-floor`。本環境沒有 3.9–3.11,
  exit 2,後面的 `check-integration-regression-guard` 一次都不跑。
  `devflow-check.sh architecture` 輸出裡 parity 出現 **0** 次。
- 舊實作下真的紅:2030-P1 序檢查(只看 run 行,不看註解)紅。
  `all` 的 architecture 組直接停在 py-floor,本機看不見 1930 那種洞。
- 修法:把 parity 的 `run` 挪到 `group_architecture` 第一支。
  **不**把 py-floor 缺席改 WARN(門檻不降)。exit 2 仍讓該組紅。
- 測試:2030-P1。修後本機 architecture 先 **41/41**,再 py-floor exit 2。
  變異(把序換回去)→ **65/66**。

### 2030-P2 契約通道檔
- 寫了 `docs/dev/stage7-loop/20260822-2030-review.md`,內容與 PR comment 三條一致。

## 查證後不成立的 finding
無。三條都重現(P2 是寫檔,不是缺陷)。

## 刻意不做的 finding
- Windows 真機、STATUS 單寫者、第二個範例、engine-fence-masking 收尾文書、需求討論九條、記憶 / PR #15、重開 1–3、拿掉 G3
- 未改 `docs/dev/STATUS.md`
- 未改 gauntlet 語意,未升 1.3.3
- 未把 62 案全部雙打兩份(額度只要求 1830 三案)
- 未把 py-floor 缺席改 WARN
- 未把 `all` 的 methodology / render 缺 `python` 改成不 fail-fast(環境,不是本迴圈)

## 本輪未處理(下一輪候選,依優先序)
1. Conditional「未列入 = 未觸發」仍靠 Reviewer
2. 本環境 `check-py-floor` 沒 3.9–3.11(exit 2)。main 上同一環境也會這樣
3. 本環境沒有 `python` 指令,`devflow-check.sh all` 的 methodology/render 預設
   `DEVFLOW_RENDER_PYTHON=python` 會紅;本輪也沒 `markdown_it`

## 需要 owner 裁決
無新項。2030 三條做完,不列回。

## 驗證結果
| 項 | 結果 |
|---|---|
| 散發副本 1830 三案(修前,母版測試) | 母版 **62/62**;散發空值 exit 0 / 27;空白 exit 0 / 27;substring exit 0 / 28 |
| 加上 2030 四案後舊實作 | **62/66** |
| `scripts/test-evidence-gauntlet.sh` | 修後 **66/66**。變異 dist **63/66**;變異序 **65/66** |
| `scripts/check-integration-regression-guard.sh` | **41/41**(現在 `architecture` 組先跑到) |
| `scripts/check-stage67-enforcement.sh` | **60/60** |
| `scripts/check-version-sync.sh` | 5 處 **1.3.3** |
| `scripts/check-gate-tokens.sh` | 全過(未增刪粗體 token) |
| `hooks/gate-consistency.sh` | **14/14** |
| `hooks/selftest.sh` | **402/402**(本環境;提示的 f4 兩案紅未重現) |
| `hooks/devflow-exec.sh doctor` | COMPATIBLE;gauntlet 1.3.3 |
| `observability/run-tests.sh` | 125/125 |
| `scripts/check-methodology-corrections.sh` | **105/106**:renderer 缺 `python` / `markdown_it`。本輪沒動 7-review |
| `scripts/test-architecture-guards.sh` | **119/119**(對照 14/14、負向 105/105) |
| `scripts/check-py-floor.sh` | **exit 2**:環境沒有 3.9–3.11。與主機 main 相同 |
| `scripts/devflow-check.sh all` | **exit 1**。contracts 5/5 綠(含 gauntlet **66/66**、memory 485、eval 23/23)。methodology 缺 `python`;architecture **先 41/41**,再 py-floor exit 2;render 缺 `python`(exit 127) |
| `scripts/devflow-evidence-gauntlet.sh --help` | 用法錯誤 exit 2(腳本沒有 --help) |
| 平台 | 只證明 Linux。沒跑 macOS / Windows。**不要把上面的綠讀成 CI 已綠** —— CI 要等本輪 push |

## 給下一輪審查的輸入
- 最該被挑戰:P1 選「挪序」不選「py-floor 缺席改 WARN」。本機 `all` 仍 exit 1,
  architecture 在 py-floor 之後的守衛(含 `test-architecture-guards`)照樣被吃掉。
  只保證 parity 看得到。要不要讓 exit 2(檢查本身沒證據)不當組內 fail-fast。
- 我不打算改的方向:不把 62 案全部雙打;不重開 1–3;G3 留著;Gauntlet 不跑專案測試
