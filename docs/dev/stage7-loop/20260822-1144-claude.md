# Claude 回應:SEED
- 回應的 review 檔名: SEED
- branch: claude/stage7-g3-hardening
- PR: https://github.com/rick546986/dev-flow/pull/16
- 本輪類型: branch 健康維護
- 新增 commit: 71f0886 fix(g3): lockstep S67-ST 負向案數 104→105（其後 c00a59d / 本補欄位 commit 只動本檔）
- 本檔 raw URL: https://raw.githubusercontent.com/rick546986/dev-flow/claude/stage7-g3-hardening/docs/dev/stage7-loop/20260822-1144-claude.md
- PR comment: 已貼:https://github.com/rick546986/dev-flow/pull/16#pullrequestreview-4998830929
- review 讀取: newest=SEED, last=SEED

沒有 `*-review.md`。上一輪 `20260822-1105-claude.md` 已把種子 P0-1 / P0-2 / P0-3 標修好。依契約:沒有新輸入,走 §2。

§2 健康:
- `origin/main`(`4377d0a`)是 HEAD 祖先;`merge-tree --write-tree` exit 0;`0 behind / 2 ahead`。跟 main 無衝突。
- PR #16 CI:`EXTERNAL_RUNTIME` 綠;`REPO_REFERENCE`(`scripts/devflow-check.sh all`)紅。run [32548112919](https://github.com/rick546986/dev-flow/actions/runs/32548112919)。
- 健康不良獨佔整輪。新輸入沒有,也不另開實作。

## 採納並修好的 finding

### CI-1 `EXPECTED_*` 沒跟 S67-ST lockstep
- 位置:`scripts/test-architecture-guards.sh` 頂部 `EXPECTED_NEGATIVES` / `EXPECTED_TOTAL`
- 我查到的根因:上一輪加了 S67-ST(頂註拿掉「整合回歸」必須紅),負向實跑變成 105、合計 119,釘死值仍是 104 / 118。個案例全 ✅,尾聲案數斷言把整包打紅。上一輪接續紀錄寫「test-architecture-guards 全過」是宣稱比證據強 —— CI 才是正本。
- 舊實作下真的紅:CI `architecture/test-architecture-guards` exit 1;`負向案數不符:實際跑了 105 個,釘死值 104`;`總案例數不符:實際跑了 119 個,釘死值 118`
- 修法:`EXPECTED_NEGATIVES` 104→105、`EXPECTED_TOTAL` 118→119。門檻只往上,跟已存在的 S67-ST 對帳。不新增案例、不改守衛。
- 測試:本機 `scripts/test-architecture-guards.sh` **119/119**(對照組 14/14、負向 105/105)

## 查證後不成立的 finding
無(本輪沒有新 review)

## 刻意不做的 finding
- 上一輪「本輪未處理」三條(顯式 `--source-sha` ≠ HEAD 仍綠、Conditional 未列入、本環境 py-floor)—— 健康修復獨佔,留給有新 review 或下一輪健康仍紅時
- Windows / STATUS 單寫者 / 第二個範例 / 記憶 / PR #15 / 重開 1–3 / 拿掉 G3
- 未改 `docs/dev/STATUS.md`
- 未開新 Automation、未設 :30 審查 timer —— 那是 owner 側的 harness,本迴圈沒那個工具,也不改自己的 routine

## 本輪未處理(下一輪候選,依優先序)
1. `--review-file` 加上顯式 `--source-sha` 且該值 ≠ 當下 HEAD 仍綠(上一輪已請 owner 裁)
2. Conditional「未列入 = 未觸發」仍靠 Reviewer
3. 本環境 `check-py-floor` 找不到 3.9–3.11(exit 2)。CI 有 3.9。main 上同一本機環境也會這樣

## 需要 owner 裁決
無新項。上一輪那條(顯式 `--source-sha` 要不要強制 = HEAD)不列回。

## 驗證結果
| 項 | 結果 |
|---|---|
| `scripts/test-architecture-guards.sh` | **119/119**(對照 14/14、負向 105/105)。修前 CI 105 vs 104 |
| `hooks/gate-consistency.sh` | 14/14 |
| `hooks/selftest.sh` | 402/402(本環境;提示的 f4 兩案紅未重現) |
| `hooks/devflow-exec.sh doctor` | COMPATIBLE;gauntlet 1.3.0 |
| `observability/run-tests.sh` | 125/125 |
| `scripts/check-methodology-corrections.sh` | 106/106(`DEVFLOW_RENDER_PYTHON=python3` + `markdown-it-py==4.0.0`) |
| `scripts/render-methodology-corrections.sh --check` | 6/6(同上) |
| `scripts/test-evidence-gauntlet.sh` | 44/44(包在 `devflow-check` contracts 組) |
| `scripts/devflow-evidence-gauntlet.sh --help` | 用法錯誤 exit 2(腳本沒有 --help) |
| `scripts/check-py-floor.sh` | **沒跑成**:exit 2,環境沒有 3.9–3.11。與上一輪、與本環境的 main 相同 |
| `scripts/devflow-check.sh all` | 本機三組紅,都是環境:methodology 初跑缺 `python` / `markdown_it`;architecture 卡 py-floor;render 同缺 `python`。裝套件並指定 `python3` 後 methodology/render 綠。architecture 組在本機仍卡 py-floor,**所以本機 `all` 不能當 CI 綠的證明**。CI 上一槍的紅是案數,不是這三個環境洞 |
| 平台 | 只證明 Linux。沒跑 macOS / Windows |

## 給下一輪審查的輸入
- 最該被挑戰的設計決策:上一輪接續紀錄可以寫「守衛全過」而 CI 同 SHA 紅 —— 接續紀錄的驗證欄若沒貼 `devflow-check.sh all` 的真實 exit,下一個小時會從錯的前提開始
- 我不打算改的方向:不重開 1–3;G3 留著;不把本輪拿去塞 Windows / 記憶 / 顯式 SHA=HEAD(等 owner)
