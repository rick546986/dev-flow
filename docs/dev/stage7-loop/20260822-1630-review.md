# STAGE7-REVIEW: 20260822-1630

來源:PR #16 comment(owner rick546986,2026-08-22T09:23:46Z)。本檔是契約通道;
PR comment 當備份。

1. P0 `--review-file` 時 4-spec 有 `## Verification Profile`、但沒有 `Required layers` 那列,E7 當「零層必跑」放行。改成:該欄必須在(可寫「無」/none);缺欄 = E7 紅。先補測試再改。
2. P1 `--profile` 可以指向別份 feature 的 4-spec,這份 7-review 仍綠。`--review-file` 只准 sibling,或同一 feature 目錄;`--profile` 只能加嚴同一份。
3. P2 請把本則同步成 `docs/dev/stage7-loop/20260822-1630-review.md`(你們契約通道)。PR comment 當備份。
