# Stage 7 第三刀接續紀錄(2026-08-24 14:15 UTC)

owner 在 PR #38 寫 STAGE7-REVIEW「第二刀已咬……**可開第三刀**」(13:37 UTC)。
本輪依序判定 → 第 4／5／6 站三刀已合進 main、第 7 站第二刀在
`claude/stage7-graph`(HEAD `eb1ed70`)且 owner 點名第三刀 → **同一分支做第三刀**。
不重開第 4／5／6 站,不開第 8 站。不要一次開完三刀 —— 本刀是最後一刀。

## 本刀咬了什麼(列檔)

| 檔 | 改了什麼 |
|---|---|
| `scripts/check-devstage7-graph.sh` | 兩顆新牙:`check_action_runtime_wired()`(hooks 必須真的呼叫 `check-devstage7-graph.sh --action`,只寫在註解不算)、`check_guide()`(`<h3 id="stage7">` 開頭必須對上十節點鏈;出現「Stage 7 還在單一 SKILL」必須紅) |
| `scripts/test-devstage7-graph.sh` | 50 → **68** 案(下限 48 → **64**):guide 三案、hooks 三案、prebash 實跑十二案 |
| `hooks/_prebash_impl.py` | 新增 `_devstage7_graph_action()`,掛在 `load_state` 早退**之前**,排在第 6 站之後 |
| `guides/guide-dev-flow.html` | `#stage7` 正文開頭補十節點鏈正本;附錄檔案地圖兩列補上 prebash 接線 |

`skills/dev-flow/stage7/graph.yaml` 與十個節點檔**一個字沒動** —— 第二刀的鏈
owner 已經咬過,第三刀只接 runtime 與導覽。

## 先紅後綠的數字

先寫牙,產品還沒進來:

```
scripts/check-devstage7-graph.sh   → exit 1、2 項
  - P0 --action 沒接到 runtime:hooks 沒有呼叫 check-devstage7-graph.sh --action
  - P0 guide 第 7 站開頭缺節點:N0-role,N1-matrix,S2-run,S2b-phenomena,
    S2c-integration,S2d-fresh,S2e-walkthrough,N3-axes,N4-author,N5-verdict
scripts/test-devstage7-graph.sh    → 63/68,5 案紅,全部紅在 prebash 未接
  (N0／N1／N3／N5 寫 7-review.md 該擋卻放行、未過 G2 該擋卻放行)
```

再寫產品 → `failures=0`、**68/68**。

## 編成的形狀(不是沙盒)

本機有 `.devstage7-cursor.json` 才編成。指令字面寫 `7-review.md`
(`>`／`>>`／`tee`／`open(`)→ `write_review`。裁決一律外包給
`check-devstage7-graph.sh --action`,prebash 自己不判。
**沒游標不攔截** —— 這是編成,不是沙盒。

六份游標各認自己的檔:第 7 站只編成 `7-review.md`,不編成 `4-spec.md`／
`5-tasks.md`／`6-implementation-notes.md`。第 1 站 `write_code`、第 2／3／4／5／6
站的編成一律零改動。prebash 實跑十二案裡有三案專門釘這件事(只有第 7 站游標時,
那三份檔不被第 7 站編成)。

N4 才准讀 6-notes(對應 review-unlock)—— 仍由 `--action` 裁決,圍欄③一個字沒改鬆。
整合回歸在 Final Fresh 之前、Verdict 後改碼作廢 G3 —— 第二刀已釘現有工具,本刀不重寫。

## 全套回歸

```
check/test-devstage7-graph          ✅ PASS + 68/68
check/test-devtalk-graph            ✅ 91/91(未改)
check/test-devstage2-graph          ✅ 29/29(未改)
check/test-devstage3-graph          ✅ 30/30(未改)
check/test-devstage4-graph          ✅ 60/60(未改)
check/test-devstage5-graph          ✅ 63/63(未改)
check/test-devstage6-graph          ✅ 57/57(未改)
devflow-check.sh methodology        ✅ exit 0 —— REPO_REFERENCE_PASS 23 組全過
check-file-map                      ✅ PASS(151)
hooks/selftest.sh                   ✅ 402/402
hooks/gate-consistency.sh           ✅ 14/14
```

`architecture/check-py-floor` / renderer 在這台需要本機 `python` symlink 才跑得動 ——
**環境缺件,repo 零改動**(補上之後 methodology 23/23 全過,與本刀無關)。

## 刻意不做

- 不改 `skills/dev-flow/stage7/graph.yaml` 與十個節點檔、不改 `skills/dev-flow/SKILL.md`
  (第一刀那行指標已經在了)。
- 不改 `_templates/` 任何正文(乘客清單正本仍是 `_templates/7-review.md` 頂註 0–5)。
- 不改 Gauntlet、Evidence 契約、空欄擋、層名全等、「出貨樹=審過的樹」、
  Final Fresh 綁 SHA、`devflow-exec.sh`、`_guard_impl.py`。圍欄③一個字沒改鬆。
- 不重做第 1／2／3／4／5／6 站:那六支 `_devtalk_graph_action`／
  `_devstage*_graph_action`、各站 check + test、guide `#stage1`–`#stage6`
  全部零改動。
- 不動 `memory/`、`.dev-flow/`。圍欄①②③ 一個字沒動。
- 不追加 `HISTORY.md`、不 merge、不推 main、不開第 8 站、不重開 hardening。
- 沒有 `via` 字串當 hop;`kind: skill-legacy` 第二刀已經必須紅。

## 等什麼

推 `claude/stage7-graph`,更新 draft PR #38。**停**,等 STAGE7-REVIEW
「第三刀已咬」。第 7 站三刀齊了,下一步是 owner／半點審 merge;合進 main
才算第 4–7 站收束。不要再開下一站。
