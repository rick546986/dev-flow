# Claude 回應:Stage 3 第一刀種子
- 回應的 review 檔名: 無(`docs/dev/stage3-loop/` 先前不存在;無 STAGE3-REVIEW)。本輪做第一刀。
- branch: claude/stage3-graph
- PR: (開 draft 後補)
- 本輪類型: 種子 / 第一刀
- 新增 commit: (推完補 SHA)
- 本檔 raw URL: (推完補)
- PR comment: 無(本輪是實作,不是審查)
- review 讀取: newest=無, last=無;第一刀種子為準

健康:`origin/main`(`a525425`,#25 已合)是 HEAD 祖先。
#18 / #21 / #23 / #25 已 MERGED。不重做第 1／2 站。不 merge、不推 main。

## 本刀咬了什麼(列檔)

- `scripts/check-devstage3-graph.sh`
- `scripts/test-devstage3-graph.sh`
- `scripts/fixtures/devstage3-graph/good/`(最小合法樹)
- `scripts/fixtures/devstage3-graph/old-skill/`(舊實作:有 SKILL、沒 graph.yaml)
- `scripts/fixtures/devstage3-graph/actions/`(`write_prototype` / `write_spec`)
- `skills/dev-flow/stage3/graph.yaml`
- `skills/dev-flow/stage3/nodes/N1-trigger.md`
- `skills/dev-flow/stage3/nodes/N-skip.md`
- `skills/dev-flow/stage3/nodes/N3-write-md.md`
- `skills/dev-flow/stage3/nodes/N5-end.md`
- `skills/dev-flow/SKILL.md`(第 3 站那列只加「節點鏈見 `skills/dev-flow/stage3/`」)
- `scripts/devflow-check.sh`(methodology 組兩行,寫在會執行的行上)
- `guides/guide-dev-flow.html`(檔案地圖兩列;不改 `#stage3` 正文)
- `.gitignore`(`.devstage3-cursor.json`)
- `scripts/check-file-map.sh` / `scripts/test-architecture-guards.sh`(EXPECTED_MAPPED_FILES 141→143)

## 舊實作下先紅(TDD,改產品前)

對 current main 形狀(單一 `skills/dev-flow/SKILL.md`、沒有 `stage3/graph.yaml` / `nodes/`)
先落地檢查腳本再跑 `scripts/check-devstage3-graph.sh`:

- exit **1**
- failures=**9**(涵蓋指定 6 項)
  1. 沒有 `skills/dev-flow/stage3/graph.yaml`
  2. 真節點缺「進條件」或「完成條件」(N1-trigger)
  3. 真節點缺「進條件」或「完成條件」(N-skip)
  4. 真節點缺「進條件」或「完成條件」(N3-write-md)
  5. 真節點缺「進條件」或「完成條件」(N5-end)
  6. 同 slug 第二份 `3-prototype*.md`
  7. `write_mode≠overwrite`
  8. 九條觸發全未命中，卻存在 `3-prototype.md`
  9. 有命中、無 skip OC、游標不在第 3 站允許節點，卻 `write_spec`(寫 `4-spec.md`)

同批先落地的負向牙 `scripts/test-devstage3-graph.sh` 在產品未改時已是 **12/12**
(對照組綠;舊 SKILL fixture 紅;缺進條件 / 缺完成條件 / 第二份 md / 另存 /
0 命中卻建檔 / N1 寫 4-spec / N1／N-skip 寫原型 / N3 有命中可寫 /
`--write-cursor` 真的寫檔)。

## 採納並修好的 finding

### 第一刀:觸發判定 + 跳過 hop
- 位置:`skills/dev-flow/stage3/graph.yaml`;
  `nodes/N1-trigger.md` / `N-skip.md` / `N3-write-md.md` / `N5-end.md`;
  `scripts/check-devstage3-graph.sh`;`scripts/test-devstage3-graph.sh`
- 舊實作下真的紅:上表 9 項 + fixture 十二顆牙
- 修法:鏈是 `N1-trigger → N-skip`(九條全未命中)與
  `N1-trigger → skill-legacy-0-2 → N3-write-md → skill-legacy-3-4 → N5-end`
  (任一命中)。分叉用 `next` + `fork_required`,不是 `via` 字串
  (第 1 站 0030 假綠不重演)。暫留步是 `kind: skill-legacy` 真節點。
  N3 只覆寫 `docs/dev/<slug>/3-prototype.md`(`write_mode: overwrite`)。
  `--action write_prototype` 只有 N3 放行;N1／N-skip 一律 deny。
  `--action write_spec` 本刀一律 deny。
  `--write-cursor` 寫 `.devstage3-cursor.json`(gitignore,不進 Git)。
  不接 prebash。
- 測試:live `check-devstage3-graph.sh` 綠(`nodes_required=4`);
  `test-devstage3-graph.sh` **12/12**

### SKILL 第 3 站只加一行指標
- 位置:`skills/dev-flow/SKILL.md` 階段表第 3 列
- 修法:加「節點鏈見 `skills/dev-flow/stage3/`」。gate 欄原文不動。
- 測試:`hooks/gate-consistency.sh` **14/14**

## 查證後不成立的 finding
無。沒有既有 review 檔可推翻。

## 刻意不做的 finding
- 不把 legacy 0／1／2／3／4 拆成真節點檔(第二刀;`kind: skill-legacy` 本刀合法)
- 不接 `hooks/_prebash_impl.py`(第三刀)
- 不改 `guides/guide-dev-flow.html` `#stage3` 正文節點鏈(第三刀;本輪只加檔案地圖兩列)
- 不改 `_templates/3-prototype.md` 正文(乘客清單仍是頂註 0–4)
- 不改 `hooks/_stage3_impl.py`(Human verdict 拒 Agent 自填 ACCEPTED,呼叫即可)
- 不改 `check-devtalk-graph.sh` / `check-devstage2-graph.sh` 那套
- 不改 memory/、Gauntlet、G3 Evidence、`devflow-exec.sh`、`.dev-flow/`
- 不追加 HISTORY.md
- 不重做第 1／2 站
- 不開第 4 站、不開 `claude/stage4-graph`
- 不 merge、不推 main

## 本輪未處理(下一輪候選,依優先序)
1. 下一刀是第二刀:legacy 0–4 變真節點。等 STAGE3-REVIEW「已咬／可開第二刀」
2. 第三刀:prebash 編成 write_prototype / 擋 4-spec;guide `#stage3` 對上第二刀鏈
3. 本環境缺 `python` 指令時 renderer 會紅;缺 markdown-it-py 時 gate-twin 會紅;不是本輪產品洞

## 需要 owner 裁決
無新項。觸發判定 + 跳過 hop 切法已在種子裁定,照做。

## 驗證結果
| 項 | 結果 |
|---|---|
| `scripts/check-devstage3-graph.sh`(舊實作) | **exit 1 / 9 項**(改前;6 項指定牙齒都在) |
| `scripts/check-devstage3-graph.sh`(新實作) | **綠**(`nodes_required=4`) |
| `scripts/test-devstage3-graph.sh` | **12/12** |
| `scripts/check-devtalk-graph.sh` | **綠**(`nodes_required=13`;未改) |
| `scripts/test-devtalk-graph.sh` | **91/91**(未改) |
| `scripts/check-devstage2-graph.sh` | **綠**(`nodes_required=9`;未改) |
| `scripts/test-devstage2-graph.sh` | **29/29**(未改) |
| `scripts/check-file-map.sh` | scanned=**143** |
| `hooks/selftest.sh` | **402/402** |
| `hooks/gate-consistency.sh` | **14/14** |
| 平台 | 只證明 Linux。沒跑 macOS / Windows |

## 給下一輪審查的輸入
- 最該被挑戰:第一刀把「0 命中」拆成 `N-skip`(不准建檔),把「任一命中」拆成
  `skill-legacy-0-2` 真 hop。模板頂註 0–4 原文沒動。
- 我不打算改的方向:不一次拆完 0–4;不接 prebash;不改 `.dev-flow` 儲存;
  不重開 Stage 1 / 2 / 4;不 merge
- 停。等 STAGE3-REVIEW「已咬／可開第二刀」。
