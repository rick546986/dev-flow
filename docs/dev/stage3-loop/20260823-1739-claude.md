# Claude 回應:STAGE3-REVIEW 第一刀已咬 → 第二刀
- 回應的 review 檔名: PR #27 owner comment 17:34Z「第一刀已落地」「可開第二刀」。無對應 `*-review.md`
- branch: claude/stage3-graph
- PR: https://github.com/rick546986/dev-flow/pull/27 (draft)
- 本輪類型: 第二刀
- 新增 commit: ef5ee71 feat(flow): Stage 3 第二刀 — legacy 0–4 成真節點；本檔補列 SHA / PR
- 本檔 raw URL: https://raw.githubusercontent.com/rick546986/dev-flow/claude/stage3-graph/docs/dev/stage3-loop/20260823-1739-claude.md
- PR comment: 無(本輪是實作,不是審查)
- review 讀取: newest=PR #27 owner comment 17:34Z「已咬 / 可開第二刀 / legacy 0–4 成真節點」。以較新的 PR comment 為準。cron 模板仍寫第一刀種子

健康:`origin/main`(`a525425`,#25 已合)是 HEAD 祖先。
`git merge-tree --write-tree origin/main HEAD` exit 0。
#18 / #21 / #23 / #25 已 MERGED。不重做第 1／2 站。不 merge、不推 main。
PR #27 CI 第一刀已落地。本輪不開健康刀、不從 main 重做第一刀。

## 本刀咬了什麼(列檔)

- `scripts/check-devstage3-graph.sh`(契約改成九真節點;`kind: skill-legacy` 必須紅)
- `scripts/test-devstage3-graph.sh`(21 案;地板 16)
- `scripts/fixtures/devstage3-graph/good/`(對照組改成九真節點)
- `scripts/fixtures/devstage3-graph/actions/`(`write_decision` 四份)
- `skills/dev-flow/stage3/graph.yaml`(`fork_required: S0-question`)
- `skills/dev-flow/stage3/nodes/S0-question.md`
- `skills/dev-flow/stage3/nodes/S1-experiment.md`
- `skills/dev-flow/stage3/nodes/S2-evidence.md`
- `skills/dev-flow/stage3/nodes/S3-writeback.md`
- `skills/dev-flow/stage3/nodes/S4-close.md`
- `skills/dev-flow/stage3/nodes/N1-trigger.md`(下一跳改 S0-question)
- `skills/dev-flow/stage3/nodes/N3-write-md.md`(下一跳改 S3-writeback)

## 舊實作下先紅(TDD,改產品前)

對第一刀形狀(四真節點 + `skill-legacy-0-2` / `3-4`)先改檢查器契約再跑
`scripts/check-devstage3-graph.sh`:

- exit **1**
- failures=**15**
  1. N1 下一跳沒同時出現 N-skip 與 S0-question
  2. 缺 `nodes/S0-question.md`
  3. 缺 `nodes/S1-experiment.md`
  4. 缺 `nodes/S2-evidence.md`
  5. 缺 `nodes/S3-writeback.md`
  6. 缺 `nodes/S4-close.md`
  7. `skill-legacy-0-2` 仍是團塊
  8. `skill-legacy-3-4` 仍是團塊
  9. N1 `fork_required` 仍是 `skill-legacy-0-2`,必須是 `S0-question`
  10–12. graph 缺 S0 / S1 / S2
  13. N3 next 仍是 `skill-legacy-3-4`,必須是 `S3-writeback`
  14–15. graph 缺 S3 / S4

## 採納並修好的 finding

### 第二刀:legacy 0／1／2／3／4 成真節點
- 位置:`skills/dev-flow/stage3/graph.yaml`;
  `nodes/S0-question.md` / `S1-experiment.md` / `S2-evidence.md` /
  `S3-writeback.md` / `S4-close.md`;
  `scripts/check-devstage3-graph.sh`;`scripts/test-devstage3-graph.sh`
- 舊實作下真的紅:上表 15 項
- 修法:鏈是
  `N1-trigger → N-skip`(0 命中)與
  `N1-trigger → S0-question → S1-experiment → S2-evidence → N3-write-md → S3-writeback → S4-close → N5-end`
  (任一命中)。`fork_required` 改成 `S0-question`。
  每個真節點「做什麼」呼叫 `--write-cursor <本節點 id>`。
  `kind: skill-legacy` 必須紅。
  S3-writeback 才 `allow: write_decision`(同一份 `2-decision.md` 確認紀錄行 + 對應條,overwrite)。
  不要放寬 N1／N-skip／S0／S1／S2／N3／N5。
  檔 `.devstage3-cursor.json` 不進 Git。乘客地圖仍 0–4。
  不改模板步 3 原文。不接 prebash。
- 測試:live `check-devstage3-graph.sh` 綠(`nodes_required=9`);
  `test-devstage3-graph.sh` **21/21**

## 查證後不成立的 finding
無。17:34 comment 叫開第二刀、不要重做第一刀。第一刀鏈與牙都還在,只把
legacy hop 換成真節點檔。cron 種子仍寫「先做第一刀」——以較新的 PR comment 為準。

## 刻意不做的 finding
- 不接 `hooks/_prebash_impl.py`(第三刀)
- 不改 `guides/guide-dev-flow.html` `#stage3` 正文節點鏈(第三刀)
- 不改 `_templates/3-prototype.md` 正文(乘客清單仍是頂註 0–4;步 3 原文不動)
- 不改 `hooks/_stage3_impl.py`
- 不改 `check-devtalk-graph.sh` / `check-devstage2-graph.sh` 那套
- 不改 memory/、Gauntlet、G3 Evidence、`devflow-exec.sh`、`.dev-flow/`
- 不追加 HISTORY.md
- 不重做第 1／2 站、不重做第一刀
- 不自己開第三刀、不開第 4 站、不開 `claude/stage4-graph`
- 不 merge、不推 main

## 本輪未處理(下一輪候選,依優先序)
1. 第三刀:prebash 編成 `write_prototype` / 擋 `4-spec`;guide `#stage3` 對上第二刀鏈。
   等第二刀 review 說咬到
2. `--action` 還只活在檢查腳本(第三刀)
3. 本環境缺 `python` 指令時 renderer 會紅;缺 markdown-it-py 時 renderer 會紅;不是本輪產品洞

## 需要 owner 裁決
無新項。第二刀切法已在種子與 17:34 review 裁定,照做。

## 驗證結果
| 項 | 結果 |
|---|---|
| `scripts/check-devstage3-graph.sh`(第一刀形狀) | **exit 1 / 15 項**(改前) |
| `scripts/check-devstage3-graph.sh`(新實作) | **綠**(`nodes_required=9`) |
| `scripts/test-devstage3-graph.sh` | **21/21** |
| `scripts/check-devtalk-graph.sh` | **綠**(`nodes_required=13`;未改) |
| `scripts/test-devtalk-graph.sh` | **91/91**(未改) |
| `scripts/check-devstage2-graph.sh` | **綠**(`nodes_required=9`;未改) |
| `scripts/test-devstage2-graph.sh` | **29/29**(未改) |
| `scripts/check-file-map.sh` | scanned=**143** |
| `hooks/selftest.sh` | **402/402** |
| `hooks/gate-consistency.sh` | **14/14** |
| `scripts/devflow-check.sh methodology` | renderer 紅(`python` / `markdown-it` 缺件;環境洞,第一刀已記) |
| 平台 | 只證明 Linux。沒跑 macOS / Windows |

## 給下一輪審查的輸入
- 最該被挑戰:第一刀把步 0–2 / 3–4 留成 skill-legacy;第二刀拆成 S0–S4,
  模板頂註 0–4 原文沒動。S3 只准改同一份 2-decision 的確認紀錄行 + 對應條。
- 我不打算改的方向:不接 prebash;不改 `.dev-flow` 儲存;
  不重開 Stage 1 / 2 / 4;不 merge
- 停。等 STAGE3-REVIEW「第二刀已咬／可開第三刀」。
