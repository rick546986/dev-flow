# Stage 4 第三刀接續紀錄 — 2026-08-24 01:57 UTC

分支 `claude/stage4-graph`(同一分支,接在第二刀 `145b196` 後面)。
PR [#31](https://github.com/rick546986/dev-flow/pull/31),草稿,不 merge、不推 main。
依據 owner 在 #31 的 STAGE4-REVIEW:「第二刀已咬 …… 可開第三刀」。
本刀是第 4 站最後一刀,做完就停,等 STAGE4-REVIEW 決定合不合。

本刀 commit:`951243e`(先紅:prebash 接線 + guide 鏈契約)、
`fd4965e`(後綠:`_devstage4_graph_action` + guide 十一節點鏈)、本檔。

## 本刀咬了什麼(列檔)

改動:

- `hooks/_prebash_impl.py` —— 新增 `_devstage4_graph_action(command)`,
  掛在 `load_state` **早退之前**(緊接在 `_devstage3_graph_action(cmd)` 後面)。
  本機有 `.devstage4-cursor.json` 才編成;指令字面寫 `4-spec.md`
  (`>`／`>>`／`tee`／`open(`)→ `write_spec`,寫 `5-tasks.md` → `write_tasks`,
  一律交給 `scripts/check-devstage4-graph.sh --action` 裁決,deny 就 `L.die` +
  `_obs_deny("devstage4-graph", action)`。沒游標**不攔截** —— 不是沙盒。
- `guides/guide-dev-flow.html` —— `<h3 id="stage4">` 正文開頭補上節點鏈正本
  那一段(語氣抄 `#stage2`／`#stage3` 同位置);附錄「檔案地圖」
  `check-devstage4-graph.sh`／`test-devstage4-graph.sh` 兩列由第一刀的
  「只有 N5 可寫／暫留步是 skill-legacy」改寫成十一真節點 + prebash 接線的事實。
- `scripts/check-devstage4-graph.sh` —— 新增 `check_action_runtime_wired()` 與
  `check_guide()`,接進 `check_live()`;檔頭契約補第 9 條,拿掉「本刀不掃 prebash」。
- `scripts/test-devstage4-graph.sh` —— 44 → 60 案,下限 28 → 56。

**沒動**:`_templates/` 任何正文、`scripts/check-spec-gate.sh`、
`scripts/build-gate-twin.py`、`docs/dev/tools/build-gate-twin.py`、
`hooks/_stage3_impl.py`、`skills/dev-flow/SKILL.md`、
`skills/dev-flow/stage4/graph.yaml` 與四＋七個節點檔、
`scripts/devflow-check.sh`、`.gitignore`(`.devstage4-cursor.json` 第一刀就進去了)、
`scripts/check-file-map.sh`(本刀零新腳本,`EXPECTED_MAPPED_FILES=145` 不必動)。

## 這一刀的兩顆新牙

1. **`--action` 必須接到 runtime**。`check_action_runtime_wired()` 掃 `hooks/`
   底下 `.py`／`.sh`,**先剝掉 `#` 之後的註解**再找同時出現
   `check-devstage4-graph` 與 `--action` 的檔。只寫在註解不算接線 —— 這顆牙
   專門防「`--action` 永遠只活在 test fixture」的假綠(第 1 站 0030 那種)。
2. **guide 第 4 站開頭必須對上十一節點鏈**。`check_guide()` 抓
   `<h3 id="stage4">` 到下一個 `<h3` 之間的前 2500 字,`CHAIN` 十一個 id 缺一即紅;
   全文出現「Stage 4 還在單一 SKILL」也紅。guide 檔不存在時回空 ——
   fixture 樹沒有 `guides/` 不會被誤判。

## 先紅後綠數字

先紅(產品還是第二刀的樣子:prebash 沒接、guide `#stage4` 只有散文):

`scripts/check-devstage4-graph.sh` → **exit 1、2 項紅**

1. `P0 --action 沒接到 runtime:hooks 沒有呼叫 check-devstage4-graph.sh --action`
2. `P0 guide 第 4 站開頭缺節點:N1-handoff,S1-requirements,S2-scenarios,
   S3a-close,S3b-profile,S3c-stage3,S4-dd,N5-write-md,S5-gate,N6-g2,N7-end`

`scripts/test-devstage4-graph.sh` → **60 案中 4 案紅**(全部紅在 prebash 未接):
游標 `N1-handoff` 寫 `4-spec.md` 沒擋、游標 `N7-end` 沒擋、未過 G1 沒擋、
G2 前寫 `5-tasks.md` 沒擋 —— 四案 rc 都是 0(該 2)。
guide／hooks 那 6 個新案在 fixture 樹上本來就綠(治具自帶壞樣本),
真正紅在 live 樹的是上面 check 的 2 項。

後綠:

| 腳本 | 結果 |
|---|---|
| `scripts/test-devstage4-graph.sh` | 60/60 綠(下限 56) |
| `scripts/check-devstage4-graph.sh` | exit 0,failures=0 |
| `scripts/test-devtalk-graph.sh` | 91/91 綠(未改) |
| `scripts/check-devtalk-graph.sh` | exit 0(未改) |
| `scripts/test-devstage2-graph.sh` | 29/29 綠(未改) |
| `scripts/check-devstage2-graph.sh` | exit 0(未改) |
| `scripts/test-devstage3-graph.sh` | 30/30 綠(未改) |
| `scripts/check-devstage3-graph.sh` | exit 0(未改) |
| `scripts/devflow-check.sh methodology` | 17 組全過 |
| `hooks/selftest.sh` | 402/402 全過 |
| `hooks/gate-consistency.sh` | 14/14 全過 |
| `scripts/devflow-check.sh architecture` | 19 組過(含 `check-file-map` 145 未動、`check-hooks-accounting`);`check-py-floor` exit 2 = 本 VM 只有 3.12、沒有 3.9–3.11 直譯器(環境缺件,非本刀) |

環境備註(跟第一／二刀同一份,repo 內零檔案為此變動):本 VM 沒有 `python`
(只有 `python3`)也沒有 `markdown-it-py`,`check-methodology-corrections` 的
renderer fixed point 一開始紅在 `python: command not found`。裝上
`scripts/requirements-methodology-render.txt` 並補一個 `python → python3` 的
本機 symlink 後全綠。`check-py-floor` 需要的 3.9–3.11 直譯器本 VM 的 apt
沒有候選版,裝不上 —— **環境缺件,不是本刀的改動**。

## 三份游標不互相拆掉

第 2／3／4 站都會編成 `4-spec.md`,但**各認自己的游標檔**:
`_devstage2_graph_action` 沒 `.devstage2-cursor.json` 就 return、
`_devstage3_graph_action` 沒 `.devstage3-cursor.json` 就 return、
第 4 站同理。所以

- 只有第 4 站游標在場時,寫 `2-decision.md`／`3-prototype.md` 不會被第 4 站編成
  (兩案已釘在 `test-devstage4-graph.sh`)。
- 第 2／3 站的 prebash 案全綠、兩支 check 全綠、腳本一個字沒改。
- 第 4 站的 prebash 測試開場會把 `.devtalk-cursor.json`／
  `.devstage2-cursor.json`／`.devstage3-cursor.json` 收走、`finally` 原樣寫回;
  自己的游標檔與兩個 tmp slug 也在 `finally` 清掉(跑完 `git status` 乾淨)。

## 第 4 站三刀到這裡齊了

| 刀 | 咬到什麼 | commit |
|---|---|---|
| 第一刀 | `N5-write-md` + `skill-legacy` 暫留 hop、四真節點、兩處註冊、游標檔 | `1c4c258` |
| 第二刀 | 乘客步 1／2／3a-c／4／5 變真節點,`kind: skill-legacy` 翻成必須紅 | `145b196` |
| 第三刀 | prebash 編成 + guide `#stage4` 十一節點鏈 | 本刀 |

下一輪要等 owner 在 #31 的 STAGE4-REVIEW。**第 4 站合進 main 之前不開
`claude/stage5-graph`**;owner 寫「可開下一站第一刀」且第 4 站已在 main,
下一輪才開第 5 站第一刀(附錄:乘客 `_templates/5-tasks.md` 0–6、
游標 `.devstage5-cursor.json`、鏈 `N1-handoff → skill-legacy-1-3 →
N4-write-md → skill-legacy-4 → N5-twin → N6-end`)。

## 刻意不做

- 不改 `skills/dev-flow/stage4/graph.yaml` 與十一個節點檔 —— 第二刀已經定形,
  第三刀只接 runtime 與 guide,不趁機動鏈。
- 不改 `skills/dev-flow/SKILL.md`(第一刀那一行指標就夠)、不動 gate 摘要、
  不動反模糊三律原文。
- 不改 `_templates/` 任何正文(乘客清單正本是 `_templates/4-spec.md` 頂註 0–6)。
- 不改 `scripts/check-spec-gate.sh`、`scripts/build-gate-twin.py`、
  `docs/dev/tools/build-gate-twin.py`、`hooks/_stage3_impl.py`(一律只呼叫)。
- 不重做第 1／2／3 站:`skills/dev-talk/`、`skills/dev-flow/stage2/`、
  `skills/dev-flow/stage3/`、四支 check + 三支 test、`_devtalk_graph_action`／
  `_devstage2_graph_action`／`_devstage3_graph_action`、guide `#stage1`–`#stage3`
  全部零改動。
- 不動 `memory/`、Gauntlet、G3 Evidence 契約、`devflow-exec.sh`、`.dev-flow/`。
  圍欄①②③ 一個字沒動。
- 不開第 5 站、不追加 `HISTORY.md`、不 merge、不推 main。
