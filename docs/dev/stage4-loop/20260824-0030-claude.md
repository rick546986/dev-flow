# Stage 4 第一刀接續紀錄 — 2026-08-24 00:30 UTC

分支 `claude/stage4-graph`(從 `origin/main` 開,`2ad9f99` = Stage 3 graph #27 已合)。
草稿 PR,不 merge、不推 main。等 STAGE4-REVIEW「已咬／可開第二刀」。

## 本刀咬了什麼(列檔)

新增:

- `scripts/check-devstage4-graph.sh` —— Stage 4 節點鏈的機械契約。
  三種用法:live check(`[root]`)、`--action FILE [root]`、
  `--write-cursor NODE [SLUG] [root]`。
- `scripts/test-devstage4-graph.sh` —— 上面那支的負向牙,29 案。
- `scripts/fixtures/devstage4-graph/good/` —— 最小合法樹(四真節點 + 兩暫留 hop
  + `docs/dev/fixture-slug/{2-decision,4-spec}.md` + 未過 G1 的
  `docs/dev/pending-slug/2-decision.md`)。
- `scripts/fixtures/devstage4-graph/old-skill/` —— 舊實作:有 SKILL、沒
  `stage4/graph.yaml`。
- `scripts/fixtures/devstage4-graph/actions/` —— 9 份 `--action` JSON。
- `skills/dev-flow/stage4/graph.yaml` —— 下一跳唯一正本。
- `skills/dev-flow/stage4/nodes/N1-handoff.md`
- `skills/dev-flow/stage4/nodes/N5-write-md.md`
- `skills/dev-flow/stage4/nodes/N6-g2.md`
- `skills/dev-flow/stage4/nodes/N7-end.md`

改動:

- `scripts/devflow-check.sh` —— `group_methodology` 在 stage3 兩行後面加
  `check-devstage4-graph` / `test-devstage4-graph`(寫在會執行的行上)。
- `guides/guide-dev-flow.html` —— 附錄「檔案地圖」補兩列。**#stage4 正文節點鏈沒動**
  (那是第三刀)。
- `scripts/check-file-map.sh` —— `EXPECTED_MAPPED_FILES` 143 → 145。
- `scripts/test-architecture-guards.sh` —— 同一常數的靜態互釘同步改成 145。
- `.gitignore` —— 加 `.devstage4-cursor.json`。
- `skills/dev-flow/SKILL.md` —— 第 4 站那列只加「節點鏈見
  `skills/dev-flow/stage4/`」。gate 摘要與反模糊三律原文沒動。

## 鏈

```
N1-handoff → skill-legacy-1-4 → N5-write-md → skill-legacy-5 → N6-g2 → N7-end
```

`entry: N1-handoff`。暫留步是 `kind: skill-legacy` 真節點(`entry:
_templates/4-spec.md` + `steps`),**不是 via 字串** —— 有 `via` 必須紅
(第 1 站 0030 假綠的來源)。單一產物 `docs/dev/<slug>/4-spec.md`,
`write_mode: overwrite`,只有 `N5-write-md` `allow: write_spec`;
`write_tasks` 本刀任何節點都未 allow。本機游標 `.devstage4-cursor.json`,不進 Git。

## 先紅後綠數字

先紅(產品還沒進來,`skills/dev-flow/stage4/` 不存在):
`scripts/check-devstage4-graph.sh` → **exit 1、10 項紅**,涵蓋要求的六項:

1. 缺 `skills/dev-flow/stage4/graph.yaml`
2. 真節點缺「進條件」或「完成條件」(四個節點檔各一項,共 4 項)
3. 同 slug 第二份 `4-spec*.md` 無法證明會被擋
4. 沒有 N5 覆寫契約,`write_mode≠overwrite` 無法證明會被擋
5. 缺 `N5-write-md`(第一刀必須有寫檔節點,不要只做 handoff)
6. 未過 G1(`2-decision` 不是 approved)卻 `write_spec` 無法證明會被擋

(外加第 7 項:G2 前寫 `5-tasks.md`(`write_tasks`)無法證明會被擋。)

後綠:

| 腳本 | 結果 |
|---|---|
| `scripts/test-devstage4-graph.sh` | 29/29 綠(下限寫死 8) |
| `scripts/check-devstage4-graph.sh` | exit 0,failures=0 |
| `scripts/test-devtalk-graph.sh` | 91/91 綠(未改) |
| `scripts/check-devtalk-graph.sh` | exit 0(未改) |
| `scripts/test-devstage2-graph.sh` | 29/29 綠(未改) |
| `scripts/check-devstage2-graph.sh` | exit 0(未改) |
| `scripts/test-devstage3-graph.sh` | 30/30 綠(未改) |
| `scripts/check-devstage3-graph.sh` | exit 0(未改) |
| `scripts/devflow-check.sh methodology` | 17 組全過 |
| `hooks/selftest.sh` | 402/402 全過 |
| `hooks/gate-consistency.sh` | 14/14 全過 |
| `scripts/devflow-check.sh architecture` | 18 組過;`check-py-floor` exit 2 = 本 VM 沒有 3.9–3.11 直譯器(環境缺件,非本刀;本刀零 `.py` 新檔) |
| `scripts/devflow-check.sh render` | 2 組全過 |

`scripts/devflow-check.sh architecture` 一開始紅在
`test-architecture-guards.sh`:`check-file-map.sh` 的
`EXPECTED_MAPPED_FILES=143` 對不上新增兩支腳本後的實得 145 —— 那正是它該抓的
(清單沒跟著長),兩處常數同步上修後 134/134 全綠。

29 案清單(對照組 4 綠 / 負向 25 紅):`G-0`、`G-old`(含「沒有 graph」)、
缺進條件、缺完成條件、第二份 `4-spec*.md`、`write_mode≠overwrite`、
缺 `N5-write-md`、有 `via`、暫留步不是 `kind: skill-legacy`、
`entry` 不是 `N1-handoff`、鏈跳過 `skill-legacy-5`、缺 `--write-cursor`、
N5 之外 `allow: write_spec`、N5 缺 `allow: write_spec`、任何節點
`allow: write_tasks`、第 3 站必要卻缺 approved 又無 skip OC(N1 不是入口)、
有 skip OC 必須綠、第 3 站已 approved 必須綠、`--action` N1／N6／N7／
`skill-legacy-1-4` 做 `write_spec` 各一紅、N5 + `2-decision` approved 綠、
未過 G1 紅、`write_tasks` N5／N1 各一紅、graph 沒這節點 → exit 2、
舊實作跑 `--action` → exit 2、`G-cursor --write-cursor N1-handoff`。

## 下一刀是第二刀(同一分支,等 review 點名)

legacy 1／2／3a-c／4／5 變真節點,每個 `--write-cursor`;
`kind: skill-legacy` 改成必須紅。鏈改成:

```
N1-handoff → S1-requirements → S2-scenarios → S3a-close → S3b-profile
  → S3c-stage3 → S4-dd → N5-write-md → S5-gate → N6-g2 → N7-end
```

S1–S5 與 N5 `allow: write_spec`(同一份 `4-spec.md` overwrite);
不放寬 N1／N6／N7。S5 只准呼叫現有 `scripts/check-spec-gate.sh`,不准改那支。
不接 prebash、不改模板步原文。

第三刀:`hooks/_prebash_impl.py` 在 `load_state` 早退之前接
`_devstage4_graph_action`;有 `.devstage4-cursor.json` 才編成;沒游標不攔截;
G2 前寫 `5-tasks.md` 必須擋;guide `<h3 id="stage4">` 開頭對上第二刀那條鏈。

## 刻意不做

- 不接 prebash(`--action` 本刀只活在檢查腳本;第三刀才接)。
- 不改 guide `#stage4` 正文節點鏈(第三刀)。
- 不開第 5 站(第 4 站三刀未合進 main 之前不開 `claude/stage5-graph`)。
- 不改 `_templates/` 任何正文(乘客清單是頂註正本)。
- 不改 `scripts/check-spec-gate.sh`、`scripts/build-gate-twin.py`、
  `docs/dev/tools/build-gate-twin.py`(N6 呼叫即可)。
- 不改 `hooks/_stage3_impl.py`(G2 Demo verdict 機械檢查,呼叫即可)。
- 不重做第 1／2／3 站:`skills/dev-talk/`、`skills/dev-flow/stage2/`、
  `skills/dev-flow/stage3/` 與對應 check/test 腳本、`_devstage3_graph_action`、
  guide `#stage3` 全部零改動。
- 不動 `memory/`、Gauntlet、G3 Evidence 契約、`devflow-exec.sh`、`.dev-flow/`。
- 不追加 `HISTORY.md`。不 merge、不推 main。
