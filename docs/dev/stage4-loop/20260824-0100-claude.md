# Stage 4 第二刀接續紀錄 — 2026-08-24 01:00 UTC

分支 `claude/stage4-graph`(同一分支,接在第一刀 `1c4c258` 後面)。
PR [#31](https://github.com/rick546986/dev-flow/pull/31),草稿,不 merge、不推 main。
依據 owner 在 #31 的 STAGE4-REVIEW:「第一刀已咬 …… 可開第二刀」。
本刀做完就停,等 STAGE4-REVIEW「已咬／可開第三刀」。

## 本刀咬了什麼(列檔)

新增:

- `skills/dev-flow/stage4/nodes/S1-requirements.md` —— 雙源列 R
- `skills/dev-flow/stage4/nodes/S2-scenarios.md` —— 逐 R 展開 S(每 S 帶觀測欄)
- `skills/dev-flow/stage4/nodes/S3a-close.md` —— 收尾四小節
- `skills/dev-flow/stage4/nodes/S3b-profile.md` —— Verification Profile +
  Design Boundary Contract
- `skills/dev-flow/stage4/nodes/S3c-stage3.md` —— Stage 3 對帳
- `skills/dev-flow/stage4/nodes/S4-dd.md` —— Drafting Decisions 清點
- `skills/dev-flow/stage4/nodes/S5-gate.md` —— 自檢(先跑既有
  `scripts/check-spec-gate.sh`,再做人工鏈檢)
- `scripts/fixtures/devstage4-graph/good/skills/dev-flow/stage4/nodes/S*.md`
  —— 對照組同步七檔
- `scripts/fixtures/devstage4-graph/actions/` 新增 6 份 JSON:
  `s1-write-spec-ok` / `s3b-write-spec-ok` / `s5-write-spec-ok` /
  `s1-write-spec-pending` / `s2-write-tasks` / `s5-write-tasks`

改動:

- `skills/dev-flow/stage4/graph.yaml` —— 兩個 `skill-legacy` 暫留 hop 拆成七個真節點;
  S1–S5 與 N5 各自 `write: docs/dev/<slug>/4-spec.md` + `write_mode: overwrite` +
  `allow: write_spec`。
- `skills/dev-flow/stage4/nodes/N1-handoff.md` —— 下一跳 `skill-legacy-1-4`
  → `S1-requirements`。
- `skills/dev-flow/stage4/nodes/N5-write-md.md` —— 進條件改成「S1–S4 都完成、
  游標在 S4-dd」,下一跳 `skill-legacy-5` → `S5-gate`。
- `skills/dev-flow/stage4/nodes/N6-g2.md` —— 進條件改成「S5-gate 完成、游標在 S5-gate」。
- `scripts/check-devstage4-graph.sh` —— 契約改成十一真節點(細節見下)。
- `scripts/test-devstage4-graph.sh` —— 負向牙補到 44 案,下限 8 → 28。

刪除:

- `scripts/fixtures/devstage4-graph/actions/legacy-write-spec.json`
  —— `skill-legacy-1-4` 這個節點本刀之後不存在了。

**沒動**:`_templates/` 任何正文、`scripts/check-spec-gate.sh`、
`scripts/build-gate-twin.py`、`docs/dev/tools/build-gate-twin.py`、
`hooks/_stage3_impl.py`、`hooks/_prebash_impl.py`、`guides/guide-dev-flow.html`、
`skills/dev-flow/SKILL.md`、`scripts/devflow-check.sh`、`.gitignore`、
`scripts/check-file-map.sh`(本刀零新腳本,`EXPECTED_MAPPED_FILES=145` 不必動)。

## 鏈

```
N1-handoff → S1-requirements → S2-scenarios → S3a-close → S3b-profile
  → S3c-stage3 → S4-dd → N5-write-md → S5-gate → N6-g2 → N7-end
```

`entry: N1-handoff`。**沒有 `kind: skill-legacy`,沒有 `via`** ——
兩者現在都必須紅。單一產物 `docs/dev/<slug>/4-spec.md`,
S1–S5 與 `N5-write-md` 共寫同一份、`write_mode: overwrite`;
`N1-handoff`／`N6-g2`／`N7-end` 維持 `forbid: write_spec`,放寬即紅。
`write_tasks` 全鏈仍未 allow(G2 前不准寫 `5-tasks.md`)。
本機游標 `.devstage4-cursor.json`,不進 Git。

## 檢查器這一刀改了哪幾顆牙

1. `kind: skill-legacy` 從「本刀合法」翻成**必須紅**(第一刀註解就寫好了要翻)。
2. `REQUIRED_NODES` 從 4 檔變 11 檔,`EXPECTED_NEXT` 換成上面那條鏈。
3. `SPEC_ALLOWED_NODES` = S1／S2／S3a／S3b／S3c／S4／N5／S5;
   逐節點驗 `write` 剛好是正本、`write_mode: overwrite`,並逐節點模擬重跑
   (只准剩一份 `4-spec.md`)。清單外 `allow: write_spec` 即紅。
4. `S5-gate`「做什麼」必須點名既有 `check-spec-gate.sh` —— 缺了就紅。
   那支腳本本身**只呼叫、不改**。
5. 下一跳 id 正則加 `S\d+[a-z]?-`,不然 `S3a-close`／`S3b-profile` 讀不出來。
6. `--action` 的 `write_spec` 白名單從單一節點改成 S1–S5 + N5;
   `write_tasks` 仍一律 deny;缺 `cursor.node` 或 graph 沒這節點仍 exit 2。

## 先紅後綠數字

先紅(產品還是第一刀的 `skill-legacy` 鏈):
`scripts/check-devstage4-graph.sh` → **exit 1、19 項紅**:

1. 七個 S 節點檔不存在(`S1-requirements` … `S5-gate`,7 項)
2. `skill-legacy-1-4`／`skill-legacy-5` 仍是團塊(2 項)
3. graph 有鏈外節點(1 項)
4. `N1-handoff` next 是 `skill-legacy-1-4`、`N5-write-md` next 是
   `skill-legacy-5`(2 項)
5. graph 缺 `S1-requirements`／`S2-scenarios`／`S3a-close`／`S3b-profile`／
   `S3c-stage3`／`S4-dd`／`S5-gate`(7 項)

後綠:

| 腳本 | 結果 |
|---|---|
| `scripts/test-devstage4-graph.sh` | 44/44 綠(下限 28) |
| `scripts/check-devstage4-graph.sh` | exit 0,failures=0 |
| `scripts/test-devtalk-graph.sh` | 91/91 綠(未改) |
| `scripts/check-devtalk-graph.sh` | exit 0(未改) |
| `scripts/test-devstage2-graph.sh` | 29/29 綠(未改) |
| `scripts/check-devstage2-graph.sh` | exit 0(未改) |
| `scripts/test-devstage3-graph.sh` | 30/30 綠(未改) |
| `scripts/check-devstage3-graph.sh` | exit 0(未改) |
| `scripts/devflow-check.sh methodology` | 17 組全過 |
| `hooks/selftest.sh` | 402/402 全過 |
| `hooks/gate-consistency.sh` | 14/14 全過 |
| `scripts/devflow-check.sh architecture` | 19 組過(含 `check-file-map` 145 未動);`check-py-floor` exit 2 = 本 VM 沒有 3.9–3.11 直譯器(環境缺件,非本刀;本刀零 `.py` 新檔) |

環境備註:本 VM 沒有 `python`(只有 `python3`)也沒有 `markdown-it-py`,
`check-methodology-corrections` 的 renderer fixed point 一開始紅在
`python: command not found`。裝上 `scripts/requirements-methodology-render.txt`
並補一個 `python → python3` 的本機 symlink 後全綠 —— **環境缺件,不是本刀的改動**,
repo 內零檔案為此變動。

44 案清單(對照組 4 綠 / 負向 40 紅):`G-0`、`G-old`、缺進條件、缺完成條件、
第二份 `4-spec*.md`、S1 `write_mode≠overwrite`、N5 `write_mode≠overwrite`、
缺 `N5-write-md`、有 `via`、節點掛 `kind: skill-legacy`、把 1／2 收回團塊、
`entry` 不是 `N1-handoff`、鏈跳過 `S5-gate`、鏈跳過 `S3b-profile`、
S1／S4／S5／N6 各缺 `--write-cursor`(4 項)、缺 `S3b-profile.md` 節點檔、
`S5-gate` 沒呼叫 `check-spec-gate.sh`、N1 `allow: write_spec`、
N6 `allow: write_spec`、N5 缺 `allow`、S5 缺 `allow`、任何節點
`allow: write_tasks`、第 3 站必要卻缺 approved 又無 skip OC、有 skip OC 綠、
第 3 站已 approved 綠、`--action` N1／N6／N7 做 `write_spec` 各一紅、
S1／S3b／N5／S5 + approved 綠(4 項)、S1／N5 未過 G1 紅(2 項)、
N1／S2／N5／S5 做 `write_tasks` 紅(4 項)、graph 沒這節點 → exit 2、
舊實作跑 `--action` → exit 2、`G-cursor --write-cursor S3a-close`。

## 下一刀是第三刀(同一分支,等 review 點名)

- `.gitignore` 已有 `.devstage4-cursor.json`(第一刀就進去了)。
- `hooks/_prebash_impl.py` 在 `load_state` 早退之前接 `_devstage4_graph_action`。
- 本機有 `.devstage4-cursor.json` 才編成:指令字面寫 `4-spec.md`
  (`>`／`>>`／`tee`／`open(`)→ `write_spec`。沒游標不攔截(不是沙盒)。
- 不重做 `write_code`、不改第 2／3 站編成;兩份／三份游標不互相拆掉。
- G2 前(`4-spec` 未 approved)寫 `5-tasks.md` 必須擋。
- guide `<h3 id="stage4">` 開頭對上本刀這條十一節點鏈;
  出現「Stage 4 還在單一 SKILL」必須紅。

## 刻意不做

- 不接 prebash(`--action` 這一刀仍只活在檢查腳本;第三刀才接)。
- 不改 guide `#stage4` 正文節點鏈(第三刀)。
- 不開第 5 站(第 4 站三刀未合進 main 之前不開 `claude/stage5-graph`)。
- 不改 `_templates/` 任何正文 —— 七個新節點檔一律指回頂註,不抄乘客步原文。
- 不改 `scripts/check-spec-gate.sh`(S5-gate 只呼叫)、
  `scripts/build-gate-twin.py`、`docs/dev/tools/build-gate-twin.py`(N6 只呼叫)。
- 不改 `hooks/_stage3_impl.py`。
- 不重做第 1／2／3 站:`skills/dev-talk/`、`skills/dev-flow/stage2/`、
  `skills/dev-flow/stage3/` 與對應 check/test 腳本、`_devstage3_graph_action`、
  guide `#stage3` 全部零改動。
- 不動 `memory/`、Gauntlet、G3 Evidence 契約、`devflow-exec.sh`、`.dev-flow/`。
- 不追加 `HISTORY.md`。不 merge、不推 main。
