# Stage 6 第三刀接續紀錄(2026-08-24 11:26 UTC)

owner 在 PR #37 寫 STAGE6-REVIEW「第二刀已咬……**可開第三刀**」(10:34 UTC)。
本輪依序判定 → 第 5 站三刀已合進 main(`09b4482`,PR #36)、第 6 站第二刀在
`claude/stage6-graph`(HEAD `90a54d8`)且 owner 點名第三刀 → **同一分支做第三刀**,
不開第 7 站。第 4 站三刀早已在 main(`8cd17d9`,#31),不再重做。

## 本刀咬了什麼(列檔)

| 檔 | 改了什麼 |
|---|---|
| `scripts/check-devstage6-graph.sh` | 兩顆新牙:`check_action_runtime_wired()`(hooks 必須真的呼叫 `check-devstage6-graph.sh --action`,只寫在註解不算)、`check_guide()`(`<h3 id="stage6">` 開頭必須對上五節點鏈並寫明「逐 T 仍走引擎」;出現「Stage 6 還在單一 SKILL」必須紅) |
| `scripts/test-devstage6-graph.sh` | 40 → **57** 案(下限 40 → **57**):guide 四案、hooks 三案、prebash 實跑十案 |
| `hooks/_prebash_impl.py` | 新增 `_devstage6_graph_action()`,掛在 `load_state` 早退**之前**,排在第 5 站之後 |
| `guides/guide-dev-flow.html` | `#stage6` 正文開頭補節點鏈正本與「逐 T 仍走引擎」;附錄檔案地圖兩列補上 prebash 接線 |

`skills/dev-flow/stage6/graph.yaml` 與五個節點檔**一個字沒動** —— 第二刀的鏈
owner 已經咬過,第三刀只接 runtime 與導覽。

## 先紅後綠的數字

先寫牙(`5b428df`),產品還沒進來:

```
scripts/check-devstage6-graph.sh   → exit 1、3 項
  - P0 --action 沒接到 runtime:hooks 沒有呼叫 check-devstage6-graph.sh --action
  - P0 guide 第 6 站開頭缺節點:N1-arm,N2-handoff,S2-tdd,N4-selfcheck,N5-end
  - P0 guide 第 6 站開頭缺「逐 T 仍走引擎」
scripts/test-devstage6-graph.sh    → 52/57,5 案紅,全部紅在 prebash 未接
```

再寫產品(`3c76946`)→ `failures=0`、**57/57**。

## 編成的形狀(不是沙盒)

本機有 `.devstage6-cursor.json` 才編成。指令字面寫 `6-implementation-notes.md`
(`>`／`>>`／`tee`／`open(`)→ `write_notes`。裁決一律外包給
`check-devstage6-graph.sh --action`,prebash 自己不判。
**沒游標不攔截** —— 這是編成,不是沙盒。

五份游標各認自己的檔:第 5 站也編成 `6-implementation-notes.md`,但它讀的是
`.devstage5-cursor.json`,兩邊不互相拆掉。第 1 站 `write_code`、第 2／3／4／5
站的編成一律零改動。prebash 實跑十案裡有兩案專門釘這件事(只有第 6 站游標時,
`5-tasks.md` 與 `4-spec.md` 不被第 6 站編成)。

S2-tdd 仍是一顆 hop,**不是一 T 一 hop** —— 逐 T 仍走 README §5 動線 / 現有引擎。
guide `#stage6` 開頭寫明這件事;出現「Stage 6 還在單一 SKILL」必須紅。

## 全套回歸

```
check/test-devstage6-graph          ✅ PASS + 57/57
check/test-devtalk-graph            ✅ 91/91(未改)
check/test-devstage2-graph          ✅ 29/29(未改)
check/test-devstage3-graph          ✅ 30/30(未改)
check/test-devstage4-graph          ✅ 60/60(未改)
check/test-devstage5-graph          ✅ 63/63(未改)
devflow-check.sh methodology        ✅ exit 0 —— REPO_REFERENCE_PASS 21 組全過
check-file-map                      ✅ PASS
hooks/selftest.sh                   ✅ 402/402
hooks/gate-consistency.sh           ✅ 14/14
```

`architecture/check-py-floor` / renderer 在這台需要本機 `python` symlink 與
`markdown-it-py==4.0.0` 才跑得動 —— **環境缺件,repo 零改動**(補上之後
methodology 21/21 全過,與本刀無關)。

## 刻意不做

- 不改 `skills/dev-flow/stage6/graph.yaml` 與五個節點檔、不改 `skills/dev-flow/SKILL.md`
  (第一刀那行指標已經在了)。
- 不改 `_templates/` 任何正文(乘客清單正本仍是 `_templates/6-implementation-notes.md`
  頂註 0–4)。
- 不改 `hooks/devflow-exec.sh`、`hooks/_guard_impl.py`、Gauntlet、平行引擎。
  圍欄②一個字沒改鬆。
- 不重做第 1／2／3／4／5 站:那五支 `_devstage*_graph_action`／`_devtalk_graph_action`、
  各站 check + test、guide `#stage1`–`#stage5` 全部零改動。
- 不動 `memory/`、G3 Evidence 契約、`.dev-flow/`。圍欄①②③ 一個字沒動。
- 不追加 `HISTORY.md`、不 merge、不推 main、不開第 7 站。
- 沒有 `via` 字串當 hop;`kind: skill-legacy` 第二刀已經必須紅。逐 T 不是一 T 一 hop。

## 等什麼

推 `claude/stage6-graph`,更新 draft PR #37。**停**,等 STAGE6-REVIEW
「第三刀已咬」。第 6 站三刀齊了,下一步是 owner／半點審 merge;合進 main
之後才開 `claude/stage7-graph` 第一刀。不要一次開完下一站。
