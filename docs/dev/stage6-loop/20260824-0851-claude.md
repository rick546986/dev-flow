# Stage 6 第一刀接續紀錄(2026-08-24 08:51 UTC)

第 5 站三刀已合進 main(`09b4482`,PR #36),
owner 在 #36 寫「第三刀已咬，三刀齊，已合進 main。可開下一站第一刀」。
本輪依序判定 → 開 `claude/stage6-graph`,只做第 6 站**第一刀**。
第 4 站三刀早已在 main(`8cd17d9`,#31),不再重做。

## 本刀咬了什麼(列檔)

新增:

| 檔 | 是什麼 |
|---|---|
| `scripts/check-devstage6-graph.sh` | Stage 6 節點鏈的機械契約(live check / `--action` / `--write-cursor`) |
| `scripts/test-devstage6-graph.sh` | 上面那支的負向牙,26 案 |
| `scripts/fixtures/devstage6-graph/good/` | 最小合法樹(四真節點 + 一暫留 hop + 兩個 slug) |
| `scripts/fixtures/devstage6-graph/old-skill/` | 舊實作:有 SKILL、沒有 `stage6/graph.yaml` |
| `scripts/fixtures/devstage6-graph/actions/` | 7 份 `--action` JSON |
| `skills/dev-flow/stage6/graph.yaml` | 下一跳唯一正本 |
| `skills/dev-flow/stage6/nodes/N1-arm.md` | 起手武裝(0a／0b／0c;落 `FORK_INTEGRATION_SHA`) |
| `skills/dev-flow/stage6/nodes/N2-handoff.md` | 接手核對(4-spec／5-tasks;禁讀 1/2/3) |
| `skills/dev-flow/stage6/nodes/N4-selfcheck.md` | 自檢(模板步 4 八問;不重寫引擎) |
| `skills/dev-flow/stage6/nodes/N5-end.md` | 收尾(不寫 7-review) |

改動(只有註冊與一行指標):

| 檔 | 改了什麼 |
|---|---|
| `scripts/devflow-check.sh` | `group_methodology` 在 stage5 兩行後面加兩行(會執行的行,不是註解) |
| `guides/guide-dev-flow.html` | 附錄「檔案地圖」補兩列(`check-file-map.sh` 雙向盤點) |
| `scripts/check-file-map.sh` | `EXPECTED_MAPPED_FILES` 147 → 149 |
| `scripts/test-architecture-guards.sh` | 靜態互釘同步 147 → 149 |
| `.gitignore` | 加 `.devstage6-cursor.json` |
| `skills/dev-flow/SKILL.md` | 第 6 站那列只加「節點鏈見 `skills/dev-flow/stage6/`」 |

鏈(`graph.yaml` 正本):

```
N1-arm → N2-handoff → skill-legacy-T → N4-selfcheck → N5-end
```

暫留步是 `kind: skill-legacy` 真節點,`entry: _templates/6-implementation-notes.md`
+ `steps: "T"`,指向 README §5 動線,**逐 T 不是一 T 一 hop**,**沒有 via**。
單一產物 `docs/dev/<slug>/6-implementation-notes.md`,`write_mode: overwrite`。
只有 `N1-arm` `allow: write_notes`(第一筆 6-notes 在這裡落 `FORK_INTEGRATION_SHA`
40 碼;寫了不准改)。

## 先紅後綠的數字

先寫牙(`619a2cb`),產品還沒進來 —— 對舊實作(main 的樹,沒有
`skills/dev-flow/stage6/graph.yaml`)跑 `scripts/check-devstage6-graph.sh`:

```
[graph] nodes_required=4 failures=11
❌ FAIL:11 項 → exit 1
```

11 項涵蓋七項必紅:①沒有 `stage6/graph.yaml` ②四個真節點缺「進條件」/「完成條件」
③同 slug 第二份 `6-implementation-notes*.md` ④`write_mode≠overwrite` ⑤缺 `N1-arm`
⑥5-tasks 不是 approved 卻 `write_notes` ⑦沒武裝寫 6-notes / SHA 缺或被改寫。

再寫產品(`c5bef38`)→ `failures=0`,exit 0。

負向牙 `scripts/test-devstage6-graph.sh` = **26/26**(下限寫死 `< 8 → exit 2`)。
含 `G-0` 對照組綠、`G-old` 舊實作紅(輸出含「沒有 graph」)、
`G-cursor --write-cursor N1-arm` 寫出 `.devstage6-cursor.json` 且
`node=N1-arm`、`G-action N1-arm + 5-tasks approved → write_notes` 綠、
`N2`／`N4-selfcheck`／`N5-end`／`skill-legacy-T` 做 `write_notes` 各一槍紅、
未定案紅、SHA 缺紅、SHA 被改寫紅、鏈外節點 exit 2、舊實作跑 `--action` exit 2。

全套回歸(第 1／2／3／4／5 站零改動,仍綠):

```
test/check-devstage6-graph          ✅ 26/26; failures=0
test/check-devtalk-graph            ✅ 91/91(未改)
test/check-devstage2-graph          ✅ 29/29(未改)
test/check-devstage3-graph          ✅ 30/30(未改)
test/check-devstage4-graph          ✅ 60/60(未改)
test/check-devstage5-graph          ✅ 63/63(未改)
devflow-check.sh methodology        ✅ REPO_REFERENCE_PASS(21 組全過)
devflow-check.sh architecture       check-file-map / test-architecture-guards 皆 ✅
                                    check-py-floor exit 2 = 直譯器缺席(這台沒有 3.9–3.11)
hooks/selftest.sh                   ✅ 402/402
hooks/gate-consistency.sh           ✅ 14/14
```

`architecture/check-py-floor` 在本機 exit 2 = **直譯器缺席**(這台沒有 3.9–3.11),
`devflow-check.sh` 本來就把它記紅、組內繼續,與本刀無關。

## 下一刀是第二刀

每個真節點 `--write-cursor`(本刀已有)。`kind: skill-legacy` 翻成必須紅。
有游標才編成寫 6-notes。guide `#stage6` 寫明逐 T 仍走引擎。

第三刀:prebash 接 `_devstage6_graph_action`(本機有 `.devstage6-cursor.json`
才編成;沒游標不攔截)+ guide `#stage6` 正文對上鏈。圍欄②不要改鬆。
不准改 `devflow-exec.sh`、`_guard_impl.py`、Gauntlet、平行引擎。

## 刻意不做

- **不接 prebash**:`hooks/_prebash_impl.py` 零改動,`--action` 本刀只活在檢查腳本。
- **不改 guide `#stage6` 正文節點鏈**:只補附錄檔案地圖兩列(後兩刀才改正文)。
- **不開第 7 站**:第 6 站三刀未合進 main 之前不開 `claude/stage7-graph`。
- 不改 `_templates/` 任何正文 —— 乘客清單正本是 `_templates/6-implementation-notes.md`
  頂註 0–4,四個節點檔都只指過去、不重抄步原文;skill-legacy-T 指向 README §5,不抄原文。
- 不改 `hooks/devflow-exec.sh`、`hooks/_guard_impl.py`、Gauntlet、平行引擎、
  `scripts/check-spec-gate.sh`、`scripts/build-gate-twin.py`。圍欄②一個字沒改鬆。
- 不重做第 1／2／3／4／5 站:`skills/dev-talk/`、`skills/dev-flow/stage2/`–
  `stage5/`、六支 check + 五支 test、既有 `_devtalk_graph_action`／
  `_devstage2_graph_action`／`_devstage3_graph_action`／`_devstage4_graph_action`／
  `_devstage5_graph_action`、guide `#stage1`–`#stage5` 全部零改動。
- 不動 `memory/`、G3 Evidence 契約、`.dev-flow/`。圍欄①②③ 一個字沒動。
- 不追加 `HISTORY.md`、不 merge、不推 main。
- 不用 `via` 字串當 hop(第 1 站 0030 假綠的來源);暫留步是 `kind: skill-legacy`
  真節點。逐 T 不是一 T 一 hop。

## 等什麼

推 `claude/stage6-graph`,開／更新 draft PR。**停**,等 STAGE6-REVIEW
「已咬／可開第二刀」。不要一次開完三刀。
