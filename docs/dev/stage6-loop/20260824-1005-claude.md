# Stage 6 第二刀接續紀錄(2026-08-24 10:05 UTC)

owner 在 PR #37 寫 STAGE6-REVIEW「第一刀已咬……**可開第二刀**」(09:19 UTC)。
本輪依序判定 → 第 5 站三刀已合進 main(`09b4482`,PR #36)、第 6 站第一刀在
`claude/stage6-graph` 但**還沒三刀齊** → 同一分支做**第二刀**,不開第三刀、
不開第 7 站。第 4 站三刀早已在 main(`8cd17d9`,#31),不再重做。

## 本刀咬了什麼(列檔)

新增:

| 檔 | 是什麼 |
|---|---|
| `skills/dev-flow/stage6/nodes/S2-tdd.md` | 逐 T:一顆 hop,指向 README §5 / 現有引擎,不是一 T 一 hop |
| `scripts/fixtures/devstage6-graph/good/…/nodes/S2-tdd.md` | 治具的同名節點檔 |
| `scripts/fixtures/devstage6-graph/actions/s2-write-notes-ok.json` | S2-tdd + 5-tasks approved 的 allow 案 |
| `scripts/fixtures/devstage6-graph/actions/s2-write-notes-pending.json` | S2-tdd 未定案的 deny 案 |
| `scripts/fixtures/devstage6-graph/actions/legacy-gone.json` | 第一刀的 `skill-legacy-T` 節點名(鏈裡沒了 → exit 2) |

改動:

| 檔 | 改了什麼 |
|---|---|
| `scripts/check-devstage6-graph.sh` | 契約翻第二刀:五真節點鏈、`kind: skill-legacy` 必須紅、`NOTES_ALLOWED_NODES` 兩節點逐一驗 write／overwrite／模擬重跑、S2-tdd 必須點名 README §5 且寫明「不是一 T 一 hop」、一 T 一 hop(`T-1`)必須紅 |
| `scripts/test-devstage6-graph.sh` | 26 → **40** 案,下限 8 → **40** |
| `skills/dev-flow/stage6/graph.yaml` | `skill-legacy-T` 拆成 `S2-tdd`;N1 與 S2 共寫同一份 6-notes(overwrite) |
| `skills/dev-flow/stage6/nodes/N2-handoff.md` | 下一跳 `skill-legacy-T` → `S2-tdd` |
| `skills/dev-flow/stage6/nodes/N4-selfcheck.md` | 進條件／退回改成 `S2-tdd` |
| `guides/guide-dev-flow.html` | 只有附錄「檔案地圖」那兩列的描述對上第二刀 |

刪除:

- `scripts/fixtures/devstage6-graph/actions/legacy-write-notes.json`
  —— `skill-legacy-T` 這個節點本刀之後不存在了。

鏈(`graph.yaml` 正本):

```
N1-arm → N2-handoff → S2-tdd → N4-selfcheck → N5-end
```

`S2-tdd` 與 `N1-arm` 共寫同一份 `docs/dev/<slug>/6-implementation-notes.md`
(`write_mode: overwrite`、`allow: write_notes`);
`N2-handoff`／`N4-selfcheck`／`N5-end` 一律沒放寬。
`S2-tdd` 是一顆 hop,**不是一 T 一 hop** —— 逐 T 仍走 README §5 動線 / 現有引擎。
每個真節點的「做什麼」最後一行都 `--write-cursor <自己的 id>`。

## 先紅後綠的數字

先改檢查器契約,產品還是第一刀的樹 —— 跑
`scripts/check-devstage6-graph.sh`:

```
[graph] nodes_required=5 failures=5
❌ FAIL:5 項 → exit 1
```

5 項:缺 `S2-tdd.md`(1)、`skill-legacy-T` 仍是團塊(1)、鏈外節點(1)、
`N2-handoff` next 對不上(1)、graph 缺 `S2-tdd`(1)。

再寫產品 → `failures=0`,exit 0。

負向牙 `scripts/test-devstage6-graph.sh` = **40/40**(下限寫死 `< 40 → exit 2`)。
第二刀新增的槍:`kind: skill-legacy` 回頭必須紅、把 T 收回團塊必須紅、
一 T 一 hop(`T-1`)必須紅、`S2-tdd` 缺 `--write-cursor`／缺節點檔／沒點名
README §5／沒寫「不是一 T 一 hop」各一槍、`S2-tdd` 缺 `allow` 一槍、
S2 的 allow／未定案各一槍、第一刀節點名跑 `--action` → exit 2。

全套回歸(第 1／2／3／4／5 站零改動,仍綠):

```
test/check-devstage6-graph          ✅ 40/40 + PASS
test/check-devtalk-graph            ✅ 91/91(未改)
test/check-devstage2-graph          ✅ 29/29(未改)
test/check-devstage3-graph          ✅ 30/30(未改)
test/check-devstage4-graph          ✅ 60/60(未改)
test/check-devstage5-graph          ✅ 63/63(未改)
devflow-check.sh methodology        ✅ REPO_REFERENCE_PASS(21 組全過)
check-file-map                      ✅ forward 149 / reverse 151
hooks/selftest.sh                   ✅ 402/402
hooks/gate-consistency.sh           ✅ 14/14
```

`architecture/check-py-floor` 在這台需要本機 `python` symlink 與
`markdown-it-py==4.0.0` 才跑得動 renderer —— **環境缺件,repo 零改動**。

## 下一刀是第三刀

- `hooks/_prebash_impl.py` 在 `load_state` 早退之前接 `_devstage6_graph_action`:
  本機有 `.devstage6-cursor.json` 才編成,指令字面寫 `6-implementation-notes.md`
  (`>`／`>>`／`tee`／`open(`)→ `write_notes`;沒游標不攔截(不是沙盒)。
- guide `#stage6` 正文開頭對上本刀這條五節點鏈,並寫明逐 T 仍走引擎;
  出現「Stage 6 還在單一 SKILL」必須紅。
- 圍欄②不要改鬆。不准改 `devflow-exec.sh`、`_guard_impl.py`、Gauntlet、
  平行引擎。不重做 `write_code`、不改第 2／3／4／5 站的編成。
- 兩份／三份／四份游標不要互相拆掉。

## 刻意不做

- **不接 prebash**:`hooks/_prebash_impl.py` 零改動,`--action` 仍只活在檢查腳本。
- **不改 guide `#stage6` 正文節點鏈**:只動附錄檔案地圖兩列(第三刀才改正文)。
- **不開第三刀、不開第 7 站**:第 6 站三刀未合進 main 之前不開
  `claude/stage7-graph`。
- 不改 `_templates/` 任何正文 —— 乘客清單正本是 `_templates/6-implementation-notes.md`
  頂註 0–4,新節點檔只指過去、不重抄步原文;S2-tdd 指向 README §5,不抄原文。
- 不改 `hooks/devflow-exec.sh`、`hooks/_guard_impl.py`、Gauntlet、平行引擎、
  `scripts/check-spec-gate.sh`、`scripts/build-gate-twin.py`。圍欄②一個字沒改鬆。
- 不重做第 1／2／3／4／5 站:`skills/dev-talk/`、`skills/dev-flow/stage2/`–
  `stage5/`、那幾支 check + test、既有 `_devtalk_graph_action`／
  `_devstage2_graph_action`／`_devstage3_graph_action`／`_devstage4_graph_action`／
  `_devstage5_graph_action`、guide `#stage1`–`#stage5` 全部零改動。
- 不動 `memory/`、G3 Evidence 契約、`.dev-flow/`。圍欄①②③ 一個字沒動。
- 不追加 `HISTORY.md`、不 merge、不推 main。
- 沒有 `via` 字串當 hop;第一刀的 `kind: skill-legacy` 暫留 hop 到本刀已經拆成
  真節點檔,契約也翻成「仍是 skill-legacy 必須紅」。逐 T 不是一 T 一 hop。

## 等什麼

推 `claude/stage6-graph`,更新 draft PR #37。**停**,等 STAGE6-REVIEW
「已咬／可開第三刀」。不要一次開完三刀。
