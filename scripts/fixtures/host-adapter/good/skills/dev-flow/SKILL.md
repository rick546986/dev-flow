---
name: dev-flow
---
# dev-flow
方法包根目錄叫 `DEVFLOW_ROOT`（舊名 `CLAUDE_PLUGIN_ROOT` 當別名，不准刪）。找不到就停，不准猜。
- stage = 6 → 讀 `skills/dev-run/SKILL.md`（Claude 別名：Skill tool 載入 `dev-run`）
reviewer → 讀 `agents/devflow-reviewer.md`
