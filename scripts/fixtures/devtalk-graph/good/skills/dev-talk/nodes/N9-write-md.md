# N9-write-md

## 進條件

步 4–6 已完成。MEMORY_SESSION_ID 仍在。游標在 N9。

## 讀什麼

本場已核事實、Interview Log、產出骨架。

## 寫哪裡

只覆寫 `docs/dev/<slug>/1-discussion.md`,不另存。不得寫程式碼。不得 talk end。不直接改長期記憶檔。

## 做什麼

按骨架寫十節。重跑本節點仍覆寫同一檔。
跑 `scripts/check-devtalk-graph.sh --write-cursor N9-write-md "$MEMORY_SESSION_ID"`。

## 完成條件

該 slug 目錄只有一份 `1-discussion.md`。十節齊、無佔位符。

## 下一跳

S8-review
