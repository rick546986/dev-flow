# N7-g1 — 產 G1 twin 並送審

## 進條件

步 4–6 已完成。`2-decision.md` 已在。游標在 N7。
未裁決 OC 不得進下一跳。

## 讀什麼

`2-decision.md`(Decision / Approaches / Owner Calls)。
G1 條件正本 README §7。twin 規格見模板頂註「G1 twin 是審查介面」。

## 寫哪裡

產同目錄 `2-decision.html`(G1 twin)。不另存第二份 `2-decision*.md`。
三連動不在本節點(正本 `nodes/N8-end.md`)。

## 做什麼

產 G1 html twin,送審。未裁決 OC 不得過。
審查者依序:適格人類 reviewer → fresh-context reviewer Agent →
owner 自審(有記錄的最後手段)。
跑 `${CLAUDE_PLUGIN_ROOT}/scripts/check-devstage2-graph.sh --write-cursor N7-g1`。

## 完成條件

twin 已產。已送審。沒有未裁決 OC。verdict 已記錄。

## 下一跳

N8-end
