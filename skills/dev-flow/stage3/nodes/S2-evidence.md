# S2-evidence — 收證

## 進條件

S1 已完成。游標在 S2。不要寫第二份 `3-prototype*.md`。

## 讀什麼

實驗結果。步 2 清單正本是 `_templates/3-prototype.md` 頂註,
本檔不抄乘客步原文。

## 寫哪裡

不覆寫 `3-prototype.md`(寫檔在 N3)。禁止第二份 `3-prototype*.md`。
本機游標只留在 `.devstage3-cursor.json`,不進 Git。

## 做什麼

依模板頂註步 2 收證,給後續 N3 落檔。
跑 `${DEVFLOW_ROOT}/scripts/check-devstage3-graph.sh --write-cursor S2-evidence`。

## 完成條件

模板頂註步 2 的完成條件已達成。本機游標在 S2-evidence。

## 下一跳

N3-write-md
