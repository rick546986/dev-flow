# S1-experiment — 實驗

## 進條件

S0 已完成。游標在 S1。code 進 throwaway branch,禁進 main。
不要寫第二份 `3-prototype*.md`。

## 讀什麼

S0 已定義的疑問。步 1 清單正本是 `_templates/3-prototype.md` 頂註,
本檔不抄乘客步原文。純資料實驗 → scratchpad。

## 寫哪裡

不覆寫 `3-prototype.md`(寫檔在 N3)。禁止第二份 `3-prototype*.md`。
本機游標只留在 `.devstage3-cursor.json`,不進 Git。

## 做什麼

依模板頂註步 1 做實驗,給後續 N3 落檔。
跑 `${DEVFLOW_ROOT}/scripts/check-devstage3-graph.sh --write-cursor S1-experiment`。

## 完成條件

模板頂註步 1 的完成條件已達成。本機游標在 S1-experiment。

## 下一跳

S2-evidence
