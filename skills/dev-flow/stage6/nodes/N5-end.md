# N5-end — 收尾

## 進條件

N4-selfcheck 完成。不要新開檔,不要寫 `7-review.md`。

## 讀什麼

`6-implementation-notes.md` frontmatter 與各節。通過條件正本在
`skills/dev-flow/SKILL.md` 第 6 站那列(每 T review PASS + 全 S 綠),
本檔不重抄 gate 摘要。`graph.yaml` 是下一跳正本。

## 寫哪裡

不新開檔。禁止 `6-implementation-notes-*.md`。不寫 `7-review.md`。
本機游標不進 Git。不改 `.dev-flow`。

## 做什麼

核 Files Changed、全節齊無佔位、frontmatter status 已更新。
跑 `${DEVFLOW_ROOT}/scripts/check-devstage6-graph.sh --write-cursor N5-end`。

## 完成條件

節齊 + status 已更新。游標在 N5-end。

## 下一跳

無
