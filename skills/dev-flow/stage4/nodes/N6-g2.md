# N6-g2 — G2 送審形狀

## 進條件

S5-gate 完成:機械關卡 exit 0(`scripts/check-spec-gate.sh` 已綠,那支腳本不改)、
鏈檢清零。游標在 S5-gate。`4-spec.md` 已落檔。不要寫 `4-spec.md`。

## 讀什麼

已落檔的 `4-spec.md`。G2 審查關鍵條件正本在 README §7,本檔不重抄條文。
twin 三要求(動線頂區五格／待審逐條可勾／背景資料摺疊)正本在
相對 DEVFLOW_ROOT 的 `_templates/4-spec.md` 頂註。`graph.yaml` 是下一跳正本。

## 寫哪裡

不覆寫 `4-spec.md`(產 html twin 用現有工具,輸出 `4-spec.html`)。
禁止第二份 `4-spec*.md`。本機游標不進 Git。

## 做什麼

呼叫現有 `scripts/build-gate-twin.py`(或 `docs/dev/tools/build-gate-twin.py`)
產 `4-spec.html` —— 不改那支腳本。未裁決的 Drafting Decisions、缺 Verification
Profile、Demo verdict 不合(有 Stage 3 trigger 卻非人類 ACCEPTED)都不得過。
跑 `${DEVFLOW_ROOT}/scripts/check-devstage4-graph.sh --write-cursor N6-g2`。

## 完成條件

G2 可送審的形狀齊:twin 在、DD 無待裁決、Verification Profile 可解析。
游標在 N6-g2。

## 下一跳

N7-end
