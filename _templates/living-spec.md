# <domain> — Living Spec(系統現況)

> 用途:此 domain「**現在**」的行為真相。讀:任何人 / AI 動這塊之前必讀。
> 寫:**只有** 7-review 的 Exit checklist 把 4-spec delta 併入
> (ADDED 貼入、MODIFIED 改寫原條文、REMOVED 刪除)。
> 手動直改 = 破壞真相鏈,禁止。格式同 4-spec 但無 delta 標記。
> 使用:複製本模板為 `docs/specs/<domain>.md`(檔名 = 領域名)。
> 每條 R 標 source(哪個 feature 帶進來的),可考古回 docs/dev/<feature>/。

## Requirements

### R: 系統 SHALL <行為>(source: <feature-slug>)
#### Scenario
- GIVEN
- WHEN
- THEN
