# 改版歷史索引

> **只增不改(append-only),最新的在最下面。**
> 本檔是**索引**,不是內容:每筆三到五行講清楚「做了什麼/為什麼/落在哪」,
> 細節住個別檔 —— 長期決策 `docs/adr/NNNN-slug.md`、
> 過程文檔 `docs/dev/<slug>/`、執行報告與稽核紀錄各自留在原地。
> 同一件事之後又變了,**不要改舊條目**,追加新的一筆並在該筆註明推翻了哪一筆。
>
> ⚠️ **不要直接編輯本檔。**用唯一寫入口(路徑依你在哪一側,兩邊不同):
> ```
> 採用專案  → docs/dev/tools/history-append.sh(dev-setup 散發)
> 方法論母版 → scripts/history-append.sh
>
> <上面那支> --slug <代號> --what <做了什麼> --why <為什麼> --where <落在哪>
> ```
> 理由:同一個專案可能同時有多個 session 在寫,直接編輯會讓後寫的把先寫的蓋掉,
> 而且不會報錯。上面那支腳本有目錄鎖 + 重試,並且只做追加。

<!--
每筆的格式(由上面的唯一寫入口 history-append.sh 產生,不要手打):

## YYYY-MM-DD · <代號> [· vX.Y.Z]
- 做了什麼:一句話,講可觀測的結果,不是「調整了某某」
- 為什麼:一句話,講當初的痛點 —— 半年後只有這句救得了你
- 落在哪:改動落在哪些檔/目錄
- 長期決策:NNNN(選填,對應 docs/adr/NNNN-*.md)
- 詳細:連結或檔案路徑(選填 —— 發版說明、執行報告、稽核紀錄)

什麼該進這裡:一件做完了的改版(feature ship、發版、重構收尾、稽核結案)。
什麼不該進:進行中的工作(→ STATUS.md 的 Active)、還沒做的(→ STATUS.md 的 Backlog)、
           踩坑教訓(→ 專案自己的教訓檔或 .claude/rules/)。

同一個功能之後又改了怎麼辦:追加新的一筆,並在「為什麼」裡寫明
「推翻 YYYY-MM-DD 的 <代號>,原因是⋯」。舊那筆留著不動 —— 看得到轉折才叫歷史。
-->
